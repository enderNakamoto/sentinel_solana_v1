# Sentinel Premium Pricing Agent — Python dependencies (Phase 22).
# Pinned to recent stable minors for reproducibility.

# Web service
fastapi~=0.115.0
uvicorn[standard]~=0.32.0
pydantic~=2.9.0

# ML pipeline (matches the Kaggle notebook stack: refrence_models/model_1.ipynb)
scikit-learn~=1.5.0
xgboost~=2.1.0
pandas~=2.2.0
numpy~=1.26.0
joblib~=1.4.0

# Tests (FastAPI TestClient pulls in httpx)
pytest~=8.3.0
httpx~=0.27.0
