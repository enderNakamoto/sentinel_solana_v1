/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const CLAIMABLE_BALANCE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([211, 2, 251, 123, 91, 61, 146, 116]);

export function getClaimableBalanceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLAIMABLE_BALANCE_DISCRIMINATOR,
  );
}

export type ClaimableBalance = {
  discriminator: ReadonlyUint8Array;
  owner: Address;
  amount: bigint;
  bump: number;
};

export type ClaimableBalanceArgs = {
  owner: Address;
  amount: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link ClaimableBalanceArgs} account data. */
export function getClaimableBalanceEncoder(): FixedSizeEncoder<ClaimableBalanceArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["owner", getAddressEncoder()],
      ["amount", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLAIMABLE_BALANCE_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ClaimableBalance} account data. */
export function getClaimableBalanceDecoder(): FixedSizeDecoder<ClaimableBalance> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["owner", getAddressDecoder()],
    ["amount", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ClaimableBalance} account data. */
export function getClaimableBalanceCodec(): FixedSizeCodec<
  ClaimableBalanceArgs,
  ClaimableBalance
> {
  return combineCodec(
    getClaimableBalanceEncoder(),
    getClaimableBalanceDecoder(),
  );
}

export function decodeClaimableBalance<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ClaimableBalance, TAddress>;
export function decodeClaimableBalance<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ClaimableBalance, TAddress>;
export function decodeClaimableBalance<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<ClaimableBalance, TAddress>
  | MaybeAccount<ClaimableBalance, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getClaimableBalanceDecoder(),
  );
}

export async function fetchClaimableBalance<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ClaimableBalance, TAddress>> {
  const maybeAccount = await fetchMaybeClaimableBalance(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeClaimableBalance<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ClaimableBalance, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeClaimableBalance(maybeAccount);
}

export async function fetchAllClaimableBalance(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ClaimableBalance>[]> {
  const maybeAccounts = await fetchAllMaybeClaimableBalance(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeClaimableBalance(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ClaimableBalance>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeClaimableBalance(maybeAccount),
  );
}

export function getClaimableBalanceSize(): number {
  return 49;
}
