/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findShareMintPda,
  findVaultStatePda,
  findWithdrawalQueuePda,
} from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const CANCEL_WITHDRAWAL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([183, 104, 181, 250, 28, 128, 210, 70]);

export function getCancelWithdrawalDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CANCEL_WITHDRAWAL_DISCRIMINATOR,
  );
}

export type CancelWithdrawalInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountWithdrawalQueue extends string | AccountMeta<string> = string,
  TAccountShareMint extends string | AccountMeta<string> = string,
  TAccountRequesterShareAccount extends string | AccountMeta<string> = string,
  TAccountRequester extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountWithdrawalQueue extends string
        ? WritableAccount<TAccountWithdrawalQueue>
        : TAccountWithdrawalQueue,
      TAccountShareMint extends string
        ? WritableAccount<TAccountShareMint>
        : TAccountShareMint,
      TAccountRequesterShareAccount extends string
        ? WritableAccount<TAccountRequesterShareAccount>
        : TAccountRequesterShareAccount,
      TAccountRequester extends string
        ? WritableSignerAccount<TAccountRequester> &
            AccountSignerMeta<TAccountRequester>
        : TAccountRequester,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CancelWithdrawalInstructionData = {
  discriminator: ReadonlyUint8Array;
  queueIndex: number;
};

export type CancelWithdrawalInstructionDataArgs = { queueIndex: number };

export function getCancelWithdrawalInstructionDataEncoder(): FixedSizeEncoder<CancelWithdrawalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["queueIndex", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CANCEL_WITHDRAWAL_DISCRIMINATOR }),
  );
}

export function getCancelWithdrawalInstructionDataDecoder(): FixedSizeDecoder<CancelWithdrawalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["queueIndex", getU32Decoder()],
  ]);
}

export function getCancelWithdrawalInstructionDataCodec(): FixedSizeCodec<
  CancelWithdrawalInstructionDataArgs,
  CancelWithdrawalInstructionData
> {
  return combineCodec(
    getCancelWithdrawalInstructionDataEncoder(),
    getCancelWithdrawalInstructionDataDecoder(),
  );
}

export type CancelWithdrawalAsyncInput<
  TAccountVaultState extends string = string,
  TAccountWithdrawalQueue extends string = string,
  TAccountShareMint extends string = string,
  TAccountRequesterShareAccount extends string = string,
  TAccountRequester extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  withdrawalQueue?: Address<TAccountWithdrawalQueue>;
  shareMint?: Address<TAccountShareMint>;
  requesterShareAccount: Address<TAccountRequesterShareAccount>;
  requester: TransactionSigner<TAccountRequester>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  queueIndex: CancelWithdrawalInstructionDataArgs["queueIndex"];
};

export async function getCancelWithdrawalInstructionAsync<
  TAccountVaultState extends string,
  TAccountWithdrawalQueue extends string,
  TAccountShareMint extends string,
  TAccountRequesterShareAccount extends string,
  TAccountRequester extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CancelWithdrawalAsyncInput<
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CancelWithdrawalInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    withdrawalQueue: { value: input.withdrawalQueue ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    requesterShareAccount: {
      value: input.requesterShareAccount ?? null,
      isWritable: true,
    },
    requester: { value: input.requester ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }
  if (!accounts.withdrawalQueue.value) {
    accounts.withdrawalQueue.value = await findWithdrawalQueuePda();
  }
  if (!accounts.shareMint.value) {
    accounts.shareMint.value = await findShareMintPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("withdrawalQueue", accounts.withdrawalQueue),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("requesterShareAccount", accounts.requesterShareAccount),
      getAccountMeta("requester", accounts.requester),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCancelWithdrawalInstructionDataEncoder().encode(
      args as CancelWithdrawalInstructionDataArgs,
    ),
    programAddress,
  } as CancelWithdrawalInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type CancelWithdrawalInput<
  TAccountVaultState extends string = string,
  TAccountWithdrawalQueue extends string = string,
  TAccountShareMint extends string = string,
  TAccountRequesterShareAccount extends string = string,
  TAccountRequester extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  withdrawalQueue: Address<TAccountWithdrawalQueue>;
  shareMint: Address<TAccountShareMint>;
  requesterShareAccount: Address<TAccountRequesterShareAccount>;
  requester: TransactionSigner<TAccountRequester>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  queueIndex: CancelWithdrawalInstructionDataArgs["queueIndex"];
};

export function getCancelWithdrawalInstruction<
  TAccountVaultState extends string,
  TAccountWithdrawalQueue extends string,
  TAccountShareMint extends string,
  TAccountRequesterShareAccount extends string,
  TAccountRequester extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CancelWithdrawalInput<
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CancelWithdrawalInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountWithdrawalQueue,
  TAccountShareMint,
  TAccountRequesterShareAccount,
  TAccountRequester,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    withdrawalQueue: { value: input.withdrawalQueue ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    requesterShareAccount: {
      value: input.requesterShareAccount ?? null,
      isWritable: true,
    },
    requester: { value: input.requester ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("withdrawalQueue", accounts.withdrawalQueue),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("requesterShareAccount", accounts.requesterShareAccount),
      getAccountMeta("requester", accounts.requester),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCancelWithdrawalInstructionDataEncoder().encode(
      args as CancelWithdrawalInstructionDataArgs,
    ),
    programAddress,
  } as CancelWithdrawalInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedCancelWithdrawalInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    withdrawalQueue: TAccountMetas[1];
    shareMint: TAccountMetas[2];
    requesterShareAccount: TAccountMetas[3];
    requester: TAccountMetas[4];
    tokenProgram: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: CancelWithdrawalInstructionData;
};

export function parseCancelWithdrawalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelWithdrawalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      withdrawalQueue: getNextAccount(),
      shareMint: getNextAccount(),
      requesterShareAccount: getNextAccount(),
      requester: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCancelWithdrawalInstructionDataDecoder().decode(instruction.data),
  };
}
