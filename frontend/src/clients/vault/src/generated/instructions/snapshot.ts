/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findSnapshotRecordPda, findVaultStatePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const SNAPSHOT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  144, 236, 6, 133, 233, 160, 21, 94,
]);

export function getSnapshotDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SNAPSHOT_DISCRIMINATOR);
}

export type SnapshotInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountShareMint extends string | AccountMeta<string> = string,
  TAccountSnapshotRecord extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TAccountRentPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountShareMint extends string
        ? ReadonlyAccount<TAccountShareMint>
        : TAccountShareMint,
      TAccountSnapshotRecord extends string
        ? WritableAccount<TAccountSnapshotRecord>
        : TAccountSnapshotRecord,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      TAccountRentPayer extends string
        ? WritableSignerAccount<TAccountRentPayer> &
            AccountSignerMeta<TAccountRentPayer>
        : TAccountRentPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SnapshotInstructionData = {
  discriminator: ReadonlyUint8Array;
  day: bigint;
};

export type SnapshotInstructionDataArgs = { day: number | bigint };

export function getSnapshotInstructionDataEncoder(): FixedSizeEncoder<SnapshotInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["day", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SNAPSHOT_DISCRIMINATOR }),
  );
}

export function getSnapshotInstructionDataDecoder(): FixedSizeDecoder<SnapshotInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["day", getU64Decoder()],
  ]);
}

export function getSnapshotInstructionDataCodec(): FixedSizeCodec<
  SnapshotInstructionDataArgs,
  SnapshotInstructionData
> {
  return combineCodec(
    getSnapshotInstructionDataEncoder(),
    getSnapshotInstructionDataDecoder(),
  );
}

export type SnapshotAsyncInput<
  TAccountVaultState extends string = string,
  TAccountShareMint extends string = string,
  TAccountSnapshotRecord extends string = string,
  TAccountController extends string = string,
  TAccountRentPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  shareMint: Address<TAccountShareMint>;
  snapshotRecord?: Address<TAccountSnapshotRecord>;
  /** Authority. PDA-signable via the controller PDA in production. */
  controller: TransactionSigner<TAccountController>;
  /**
   * Rent-payer for new `SnapshotRecord` PDAs. Must be a system-owned
   * signer (PDAs can't pay rent). In production this is the keeper
   * signer driving `execute_settlements`. See Phase 5 D18.
   */
  rentPayer: TransactionSigner<TAccountRentPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  day: SnapshotInstructionDataArgs["day"];
};

export async function getSnapshotInstructionAsync<
  TAccountVaultState extends string,
  TAccountShareMint extends string,
  TAccountSnapshotRecord extends string,
  TAccountController extends string,
  TAccountRentPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SnapshotAsyncInput<
    TAccountVaultState,
    TAccountShareMint,
    TAccountSnapshotRecord,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SnapshotInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountSnapshotRecord,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: false },
    snapshotRecord: { value: input.snapshotRecord ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
    rentPayer: { value: input.rentPayer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }
  if (!accounts.snapshotRecord.value) {
    accounts.snapshotRecord.value = await findSnapshotRecordPda({
      day: getNonNullResolvedInstructionInput("day", args.day),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("snapshotRecord", accounts.snapshotRecord),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("rentPayer", accounts.rentPayer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSnapshotInstructionDataEncoder().encode(
      args as SnapshotInstructionDataArgs,
    ),
    programAddress,
  } as SnapshotInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountSnapshotRecord,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >);
}

export type SnapshotInput<
  TAccountVaultState extends string = string,
  TAccountShareMint extends string = string,
  TAccountSnapshotRecord extends string = string,
  TAccountController extends string = string,
  TAccountRentPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  shareMint: Address<TAccountShareMint>;
  snapshotRecord: Address<TAccountSnapshotRecord>;
  /** Authority. PDA-signable via the controller PDA in production. */
  controller: TransactionSigner<TAccountController>;
  /**
   * Rent-payer for new `SnapshotRecord` PDAs. Must be a system-owned
   * signer (PDAs can't pay rent). In production this is the keeper
   * signer driving `execute_settlements`. See Phase 5 D18.
   */
  rentPayer: TransactionSigner<TAccountRentPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  day: SnapshotInstructionDataArgs["day"];
};

export function getSnapshotInstruction<
  TAccountVaultState extends string,
  TAccountShareMint extends string,
  TAccountSnapshotRecord extends string,
  TAccountController extends string,
  TAccountRentPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SnapshotInput<
    TAccountVaultState,
    TAccountShareMint,
    TAccountSnapshotRecord,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SnapshotInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountShareMint,
  TAccountSnapshotRecord,
  TAccountController,
  TAccountRentPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: false },
    snapshotRecord: { value: input.snapshotRecord ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
    rentPayer: { value: input.rentPayer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("snapshotRecord", accounts.snapshotRecord),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("rentPayer", accounts.rentPayer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getSnapshotInstructionDataEncoder().encode(
      args as SnapshotInstructionDataArgs,
    ),
    programAddress,
  } as SnapshotInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountSnapshotRecord,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >);
}

export type ParsedSnapshotInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    shareMint: TAccountMetas[1];
    snapshotRecord: TAccountMetas[2];
    /** Authority. PDA-signable via the controller PDA in production. */
    controller: TAccountMetas[3];
    /**
     * Rent-payer for new `SnapshotRecord` PDAs. Must be a system-owned
     * signer (PDAs can't pay rent). In production this is the keeper
     * signer driving `execute_settlements`. See Phase 5 D18.
     */
    rentPayer: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: SnapshotInstructionData;
};

export function parseSnapshotInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSnapshotInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      shareMint: getNextAccount(),
      snapshotRecord: getNextAccount(),
      controller: getNextAccount(),
      rentPayer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getSnapshotInstructionDataDecoder().decode(instruction.data),
  };
}
