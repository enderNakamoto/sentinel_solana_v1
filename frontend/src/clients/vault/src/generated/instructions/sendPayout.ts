/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultStatePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const SEND_PAYOUT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  113, 130, 45, 158, 96, 145, 152, 235,
]);

export function getSendPayoutDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(SEND_PAYOUT_DISCRIMINATOR);
}

export type SendPayoutInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TAccountStableTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      TAccountStableTokenProgram extends string
        ? ReadonlyAccount<TAccountStableTokenProgram>
        : TAccountStableTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SendPayoutInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type SendPayoutInstructionDataArgs = { amount: number | bigint };

export function getSendPayoutInstructionDataEncoder(): FixedSizeEncoder<SendPayoutInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SEND_PAYOUT_DISCRIMINATOR }),
  );
}

export function getSendPayoutInstructionDataDecoder(): FixedSizeDecoder<SendPayoutInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getSendPayoutInstructionDataCodec(): FixedSizeCodec<
  SendPayoutInstructionDataArgs,
  SendPayoutInstructionData
> {
  return combineCodec(
    getSendPayoutInstructionDataEncoder(),
    getSendPayoutInstructionDataDecoder(),
  );
}

export type SendPayoutAsyncInput<
  TAccountVaultState extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountRecipient extends string = string,
  TAccountStableMint extends string = string,
  TAccountController extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  recipient: Address<TAccountRecipient>;
  /** Stable mint — required as a `TransferChecked` account. */
  stableMint: Address<TAccountStableMint>;
  controller: TransactionSigner<TAccountController>;
  /** Stable token program — for the stable-coin `TransferChecked` CPI. */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  amount: SendPayoutInstructionDataArgs["amount"];
};

export async function getSendPayoutInstructionAsync<
  TAccountVaultState extends string,
  TAccountVaultTokenAccount extends string,
  TAccountRecipient extends string,
  TAccountStableMint extends string,
  TAccountController extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SendPayoutAsyncInput<
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SendPayoutInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountStableTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    recipient: { value: input.recipient ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    controller: { value: input.controller ?? null, isWritable: false },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getSendPayoutInstructionDataEncoder().encode(
      args as SendPayoutInstructionDataArgs,
    ),
    programAddress,
  } as SendPayoutInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountStableTokenProgram
  >);
}

export type SendPayoutInput<
  TAccountVaultState extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountRecipient extends string = string,
  TAccountStableMint extends string = string,
  TAccountController extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  recipient: Address<TAccountRecipient>;
  /** Stable mint — required as a `TransferChecked` account. */
  stableMint: Address<TAccountStableMint>;
  controller: TransactionSigner<TAccountController>;
  /** Stable token program — for the stable-coin `TransferChecked` CPI. */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  amount: SendPayoutInstructionDataArgs["amount"];
};

export function getSendPayoutInstruction<
  TAccountVaultState extends string,
  TAccountVaultTokenAccount extends string,
  TAccountRecipient extends string,
  TAccountStableMint extends string,
  TAccountController extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SendPayoutInput<
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SendPayoutInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountVaultTokenAccount,
  TAccountRecipient,
  TAccountStableMint,
  TAccountController,
  TAccountStableTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    recipient: { value: input.recipient ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    controller: { value: input.controller ?? null, isWritable: false },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getSendPayoutInstructionDataEncoder().encode(
      args as SendPayoutInstructionDataArgs,
    ),
    programAddress,
  } as SendPayoutInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountStableTokenProgram
  >);
}

export type ParsedSendPayoutInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    vaultTokenAccount: TAccountMetas[1];
    recipient: TAccountMetas[2];
    /** Stable mint — required as a `TransferChecked` account. */
    stableMint: TAccountMetas[3];
    controller: TAccountMetas[4];
    /** Stable token program — for the stable-coin `TransferChecked` CPI. */
    stableTokenProgram: TAccountMetas[5];
  };
  data: SendPayoutInstructionData;
};

export function parseSendPayoutInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSendPayoutInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 6,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      recipient: getNextAccount(),
      stableMint: getNextAccount(),
      controller: getNextAccount(),
      stableTokenProgram: getNextAccount(),
    },
    data: getSendPayoutInstructionDataDecoder().decode(instruction.data),
  };
}
