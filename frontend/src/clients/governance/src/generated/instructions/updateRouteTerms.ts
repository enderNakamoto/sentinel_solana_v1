/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findConfigPda,
  findDisableRouteAdminRecordPda,
  findRoutePda,
} from "../pdas";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";
import {
  getU32UpdateDecoder,
  getU32UpdateEncoder,
  getU64UpdateDecoder,
  getU64UpdateEncoder,
  type U32Update,
  type U32UpdateArgs,
  type U64Update,
  type U64UpdateArgs,
} from "../types";

export const UPDATE_ROUTE_TERMS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([190, 12, 86, 54, 138, 232, 180, 86]);

export function getUpdateRouteTermsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_ROUTE_TERMS_DISCRIMINATOR,
  );
}

export type UpdateRouteTermsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoute extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoute extends string
        ? WritableAccount<TAccountRoute>
        : TAccountRoute,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateRouteTermsInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  origin: string;
  destination: string;
  premium: U64Update;
  payoff: U64Update;
  delayHours: U32Update;
};

export type UpdateRouteTermsInstructionDataArgs = {
  flightId: string;
  origin: string;
  destination: string;
  premium: U64UpdateArgs;
  payoff: U64UpdateArgs;
  delayHours: U32UpdateArgs;
};

export function getUpdateRouteTermsInstructionDataEncoder(): Encoder<UpdateRouteTermsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["origin", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["destination", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["premium", getU64UpdateEncoder()],
      ["payoff", getU64UpdateEncoder()],
      ["delayHours", getU32UpdateEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_ROUTE_TERMS_DISCRIMINATOR }),
  );
}

export function getUpdateRouteTermsInstructionDataDecoder(): Decoder<UpdateRouteTermsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["origin", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["destination", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["premium", getU64UpdateDecoder()],
    ["payoff", getU64UpdateDecoder()],
    ["delayHours", getU32UpdateDecoder()],
  ]);
}

export function getUpdateRouteTermsInstructionDataCodec(): Codec<
  UpdateRouteTermsInstructionDataArgs,
  UpdateRouteTermsInstructionData
> {
  return combineCodec(
    getUpdateRouteTermsInstructionDataEncoder(),
    getUpdateRouteTermsInstructionDataDecoder(),
  );
}

export type UpdateRouteTermsAsyncInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountCaller extends string = string,
> = {
  config?: Address<TAccountConfig>;
  route?: Address<TAccountRoute>;
  adminRecord?: Address<TAccountAdminRecord>;
  caller: TransactionSigner<TAccountCaller>;
  flightId: UpdateRouteTermsInstructionDataArgs["flightId"];
  origin: UpdateRouteTermsInstructionDataArgs["origin"];
  destination: UpdateRouteTermsInstructionDataArgs["destination"];
  premium: UpdateRouteTermsInstructionDataArgs["premium"];
  payoff: UpdateRouteTermsInstructionDataArgs["payoff"];
  delayHours: UpdateRouteTermsInstructionDataArgs["delayHours"];
};

export async function getUpdateRouteTermsInstructionAsync<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TAccountAdminRecord extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: UpdateRouteTermsAsyncInput<
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateRouteTermsInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    route: { value: input.route ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    caller: { value: input.caller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.route.value) {
    accounts.route.value = await findRoutePda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      origin: getNonNullResolvedInstructionInput("origin", args.origin),
      destination: getNonNullResolvedInstructionInput(
        "destination",
        args.destination,
      ),
    });
  }
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findDisableRouteAdminRecordPda({
      caller: getAddressFromResolvedInstructionAccount(
        "caller",
        accounts.caller.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
      getAccountMeta("adminRecord", accounts.adminRecord),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getUpdateRouteTermsInstructionDataEncoder().encode(
      args as UpdateRouteTermsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateRouteTermsInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >);
}

export type UpdateRouteTermsInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountCaller extends string = string,
> = {
  config: Address<TAccountConfig>;
  route: Address<TAccountRoute>;
  adminRecord?: Address<TAccountAdminRecord>;
  caller: TransactionSigner<TAccountCaller>;
  flightId: UpdateRouteTermsInstructionDataArgs["flightId"];
  origin: UpdateRouteTermsInstructionDataArgs["origin"];
  destination: UpdateRouteTermsInstructionDataArgs["destination"];
  premium: UpdateRouteTermsInstructionDataArgs["premium"];
  payoff: UpdateRouteTermsInstructionDataArgs["payoff"];
  delayHours: UpdateRouteTermsInstructionDataArgs["delayHours"];
};

export function getUpdateRouteTermsInstruction<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TAccountAdminRecord extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: UpdateRouteTermsInput<
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateRouteTermsInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountRoute,
  TAccountAdminRecord,
  TAccountCaller
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    route: { value: input.route ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    caller: { value: input.caller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
      getAccountMeta("adminRecord", accounts.adminRecord),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getUpdateRouteTermsInstructionDataEncoder().encode(
      args as UpdateRouteTermsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateRouteTermsInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >);
}

export type ParsedUpdateRouteTermsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    route: TAccountMetas[1];
    adminRecord?: TAccountMetas[2] | undefined;
    caller: TAccountMetas[3];
  };
  data: UpdateRouteTermsInstructionData;
};

export function parseUpdateRouteTermsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateRouteTermsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GOVERNANCE_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      route: getNextAccount(),
      adminRecord: getNextOptionalAccount(),
      caller: getNextAccount(),
    },
    data: getUpdateRouteTermsInstructionDataDecoder().decode(instruction.data),
  };
}
