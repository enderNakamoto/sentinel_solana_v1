/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";

export const SET_DEFAULTS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  58, 181, 153, 49, 140, 16, 205, 251,
]);

export function getSetDefaultsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_DEFAULTS_DISCRIMINATOR,
  );
}

export type SetDefaultsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type SetDefaultsInstructionData = {
  discriminator: ReadonlyUint8Array;
  premium: bigint;
  payoff: bigint;
  delayHours: number;
};

export type SetDefaultsInstructionDataArgs = {
  premium: number | bigint;
  payoff: number | bigint;
  delayHours: number;
};

export function getSetDefaultsInstructionDataEncoder(): FixedSizeEncoder<SetDefaultsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["premium", getU64Encoder()],
      ["payoff", getU64Encoder()],
      ["delayHours", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_DEFAULTS_DISCRIMINATOR }),
  );
}

export function getSetDefaultsInstructionDataDecoder(): FixedSizeDecoder<SetDefaultsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["premium", getU64Decoder()],
    ["payoff", getU64Decoder()],
    ["delayHours", getU32Decoder()],
  ]);
}

export function getSetDefaultsInstructionDataCodec(): FixedSizeCodec<
  SetDefaultsInstructionDataArgs,
  SetDefaultsInstructionData
> {
  return combineCodec(
    getSetDefaultsInstructionDataEncoder(),
    getSetDefaultsInstructionDataDecoder(),
  );
}

export type SetDefaultsAsyncInput<
  TAccountConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  config?: Address<TAccountConfig>;
  owner: TransactionSigner<TAccountOwner>;
  premium: SetDefaultsInstructionDataArgs["premium"];
  payoff: SetDefaultsInstructionDataArgs["payoff"];
  delayHours: SetDefaultsInstructionDataArgs["delayHours"];
};

export async function getSetDefaultsInstructionAsync<
  TAccountConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: SetDefaultsAsyncInput<TAccountConfig, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetDefaultsInstruction<TProgramAddress, TAccountConfig, TAccountOwner>
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetDefaultsInstructionDataEncoder().encode(
      args as SetDefaultsInstructionDataArgs,
    ),
    programAddress,
  } as SetDefaultsInstruction<TProgramAddress, TAccountConfig, TAccountOwner>);
}

export type SetDefaultsInput<
  TAccountConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  config: Address<TAccountConfig>;
  owner: TransactionSigner<TAccountOwner>;
  premium: SetDefaultsInstructionDataArgs["premium"];
  payoff: SetDefaultsInstructionDataArgs["payoff"];
  delayHours: SetDefaultsInstructionDataArgs["delayHours"];
};

export function getSetDefaultsInstruction<
  TAccountConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: SetDefaultsInput<TAccountConfig, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): SetDefaultsInstruction<TProgramAddress, TAccountConfig, TAccountOwner> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetDefaultsInstructionDataEncoder().encode(
      args as SetDefaultsInstructionDataArgs,
    ),
    programAddress,
  } as SetDefaultsInstruction<TProgramAddress, TAccountConfig, TAccountOwner>);
}

export type ParsedSetDefaultsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: SetDefaultsInstructionData;
};

export function parseSetDefaultsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetDefaultsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { config: getNextAccount(), owner: getNextAccount() },
    data: getSetDefaultsInstructionDataDecoder().decode(instruction.data),
  };
}
