/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getFlightEntryDecoder,
  getFlightEntryEncoder,
  type FlightEntry,
  type FlightEntryArgs,
} from "../types";

export const ACTIVE_FLIGHT_LIST_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([96, 201, 242, 209, 187, 158, 165, 22]);

export function getActiveFlightListDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ACTIVE_FLIGHT_LIST_DISCRIMINATOR,
  );
}

export type ActiveFlightList = {
  discriminator: ReadonlyUint8Array;
  flights: Array<FlightEntry>;
  bump: number;
};

export type ActiveFlightListArgs = {
  flights: Array<FlightEntryArgs>;
  bump: number;
};

/** Gets the encoder for {@link ActiveFlightListArgs} account data. */
export function getActiveFlightListEncoder(): Encoder<ActiveFlightListArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flights", getArrayEncoder(getFlightEntryEncoder())],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ACTIVE_FLIGHT_LIST_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link ActiveFlightList} account data. */
export function getActiveFlightListDecoder(): Decoder<ActiveFlightList> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flights", getArrayDecoder(getFlightEntryDecoder())],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link ActiveFlightList} account data. */
export function getActiveFlightListCodec(): Codec<
  ActiveFlightListArgs,
  ActiveFlightList
> {
  return combineCodec(
    getActiveFlightListEncoder(),
    getActiveFlightListDecoder(),
  );
}

export function decodeActiveFlightList<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<ActiveFlightList, TAddress>;
export function decodeActiveFlightList<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ActiveFlightList, TAddress>;
export function decodeActiveFlightList<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<ActiveFlightList, TAddress>
  | MaybeAccount<ActiveFlightList, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getActiveFlightListDecoder(),
  );
}

export async function fetchActiveFlightList<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<ActiveFlightList, TAddress>> {
  const maybeAccount = await fetchMaybeActiveFlightList(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeActiveFlightList<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<ActiveFlightList, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeActiveFlightList(maybeAccount);
}

export async function fetchAllActiveFlightList(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<ActiveFlightList>[]> {
  const maybeAccounts = await fetchAllMaybeActiveFlightList(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeActiveFlightList(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<ActiveFlightList>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeActiveFlightList(maybeAccount),
  );
}
