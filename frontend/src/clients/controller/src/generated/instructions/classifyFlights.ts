/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findControllerConfigPda } from "../pdas";
import { CONTROLLER_PROGRAM_ADDRESS } from "../programs";

export const CLASSIFY_FLIGHTS_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([22, 19, 194, 250, 138, 1, 227, 105]);

export function getClassifyFlightsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLASSIFY_FLIGHTS_DISCRIMINATOR,
  );
}

export type ClassifyFlightsInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountControllerConfig extends string | AccountMeta<string> = string,
  TAccountOracleProgram extends string | AccountMeta<string> = string,
  TAccountOracleConfig extends string | AccountMeta<string> = string,
  TAccountKeeper extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountControllerConfig extends string
        ? ReadonlyAccount<TAccountControllerConfig>
        : TAccountControllerConfig,
      TAccountOracleProgram extends string
        ? ReadonlyAccount<TAccountOracleProgram>
        : TAccountOracleProgram,
      TAccountOracleConfig extends string
        ? ReadonlyAccount<TAccountOracleConfig>
        : TAccountOracleConfig,
      TAccountKeeper extends string
        ? ReadonlySignerAccount<TAccountKeeper> &
            AccountSignerMeta<TAccountKeeper>
        : TAccountKeeper,
      ...TRemainingAccounts,
    ]
  >;

export type ClassifyFlightsInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ClassifyFlightsInstructionDataArgs = {};

export function getClassifyFlightsInstructionDataEncoder(): FixedSizeEncoder<ClassifyFlightsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLASSIFY_FLIGHTS_DISCRIMINATOR }),
  );
}

export function getClassifyFlightsInstructionDataDecoder(): FixedSizeDecoder<ClassifyFlightsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getClassifyFlightsInstructionDataCodec(): FixedSizeCodec<
  ClassifyFlightsInstructionDataArgs,
  ClassifyFlightsInstructionData
> {
  return combineCodec(
    getClassifyFlightsInstructionDataEncoder(),
    getClassifyFlightsInstructionDataDecoder(),
  );
}

export type ClassifyFlightsAsyncInput<
  TAccountControllerConfig extends string = string,
  TAccountOracleProgram extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountKeeper extends string = string,
> = {
  controllerConfig?: Address<TAccountControllerConfig>;
  oracleProgram: Address<TAccountOracleProgram>;
  oracleConfig: Address<TAccountOracleConfig>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export async function getClassifyFlightsInstructionAsync<
  TAccountControllerConfig extends string,
  TAccountOracleProgram extends string,
  TAccountOracleConfig extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: ClassifyFlightsAsyncInput<
    TAccountControllerConfig,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountKeeper
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClassifyFlightsInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountKeeper
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: false,
    },
    oracleProgram: { value: input.oracleProgram ?? null, isWritable: false },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    keeper: { value: input.keeper ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.controllerConfig.value) {
    accounts.controllerConfig.value = await findControllerConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("oracleProgram", accounts.oracleProgram),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getClassifyFlightsInstructionDataEncoder().encode({}),
    programAddress,
  } as ClassifyFlightsInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountKeeper
  >);
}

export type ClassifyFlightsInput<
  TAccountControllerConfig extends string = string,
  TAccountOracleProgram extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountKeeper extends string = string,
> = {
  controllerConfig: Address<TAccountControllerConfig>;
  oracleProgram: Address<TAccountOracleProgram>;
  oracleConfig: Address<TAccountOracleConfig>;
  keeper: TransactionSigner<TAccountKeeper>;
};

export function getClassifyFlightsInstruction<
  TAccountControllerConfig extends string,
  TAccountOracleProgram extends string,
  TAccountOracleConfig extends string,
  TAccountKeeper extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: ClassifyFlightsInput<
    TAccountControllerConfig,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountKeeper
  >,
  config?: { programAddress?: TProgramAddress },
): ClassifyFlightsInstruction<
  TProgramAddress,
  TAccountControllerConfig,
  TAccountOracleProgram,
  TAccountOracleConfig,
  TAccountKeeper
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: false,
    },
    oracleProgram: { value: input.oracleProgram ?? null, isWritable: false },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: false },
    keeper: { value: input.keeper ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("oracleProgram", accounts.oracleProgram),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("keeper", accounts.keeper),
    ],
    data: getClassifyFlightsInstructionDataEncoder().encode({}),
    programAddress,
  } as ClassifyFlightsInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountKeeper
  >);
}

export type ParsedClassifyFlightsInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    controllerConfig: TAccountMetas[0];
    oracleProgram: TAccountMetas[1];
    oracleConfig: TAccountMetas[2];
    keeper: TAccountMetas[3];
  };
  data: ClassifyFlightsInstructionData;
};

export function parseClassifyFlightsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClassifyFlightsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      controllerConfig: getNextAccount(),
      oracleProgram: getNextAccount(),
      oracleConfig: getNextAccount(),
      keeper: getNextAccount(),
    },
    data: getClassifyFlightsInstructionDataDecoder().decode(instruction.data),
  };
}
