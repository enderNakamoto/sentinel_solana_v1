/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findControllerConfigPda } from "../pdas";
import { CONTROLLER_PROGRAM_ADDRESS } from "../programs";

export const REPAIR_ORACLE_CONFIG_POINTER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([7, 154, 89, 174, 154, 253, 13, 34]);

export function getRepairOracleConfigPointerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REPAIR_ORACLE_CONFIG_POINTER_DISCRIMINATOR,
  );
}

export type RepairOracleConfigPointerInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountControllerConfig extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountControllerConfig extends string
        ? WritableAccount<TAccountControllerConfig>
        : TAccountControllerConfig,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type RepairOracleConfigPointerInstructionData = {
  discriminator: ReadonlyUint8Array;
  newOracleConfig: Address;
};

export type RepairOracleConfigPointerInstructionDataArgs = {
  newOracleConfig: Address;
};

export function getRepairOracleConfigPointerInstructionDataEncoder(): FixedSizeEncoder<RepairOracleConfigPointerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newOracleConfig", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: REPAIR_ORACLE_CONFIG_POINTER_DISCRIMINATOR,
    }),
  );
}

export function getRepairOracleConfigPointerInstructionDataDecoder(): FixedSizeDecoder<RepairOracleConfigPointerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newOracleConfig", getAddressDecoder()],
  ]);
}

export function getRepairOracleConfigPointerInstructionDataCodec(): FixedSizeCodec<
  RepairOracleConfigPointerInstructionDataArgs,
  RepairOracleConfigPointerInstructionData
> {
  return combineCodec(
    getRepairOracleConfigPointerInstructionDataEncoder(),
    getRepairOracleConfigPointerInstructionDataDecoder(),
  );
}

export type RepairOracleConfigPointerAsyncInput<
  TAccountControllerConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  controllerConfig?: Address<TAccountControllerConfig>;
  owner: TransactionSigner<TAccountOwner>;
  newOracleConfig: RepairOracleConfigPointerInstructionDataArgs["newOracleConfig"];
};

export async function getRepairOracleConfigPointerInstructionAsync<
  TAccountControllerConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: RepairOracleConfigPointerAsyncInput<
    TAccountControllerConfig,
    TAccountOwner
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RepairOracleConfigPointerInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOwner
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: true,
    },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.controllerConfig.value) {
    accounts.controllerConfig.value = await findControllerConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getRepairOracleConfigPointerInstructionDataEncoder().encode(
      args as RepairOracleConfigPointerInstructionDataArgs,
    ),
    programAddress,
  } as RepairOracleConfigPointerInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOwner
  >);
}

export type RepairOracleConfigPointerInput<
  TAccountControllerConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  controllerConfig: Address<TAccountControllerConfig>;
  owner: TransactionSigner<TAccountOwner>;
  newOracleConfig: RepairOracleConfigPointerInstructionDataArgs["newOracleConfig"];
};

export function getRepairOracleConfigPointerInstruction<
  TAccountControllerConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: RepairOracleConfigPointerInput<
    TAccountControllerConfig,
    TAccountOwner
  >,
  config?: { programAddress?: TProgramAddress },
): RepairOracleConfigPointerInstruction<
  TProgramAddress,
  TAccountControllerConfig,
  TAccountOwner
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: true,
    },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getRepairOracleConfigPointerInstructionDataEncoder().encode(
      args as RepairOracleConfigPointerInstructionDataArgs,
    ),
    programAddress,
  } as RepairOracleConfigPointerInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOwner
  >);
}

export type ParsedRepairOracleConfigPointerInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    controllerConfig: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: RepairOracleConfigPointerInstructionData;
};

export function parseRepairOracleConfigPointerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRepairOracleConfigPointerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { controllerConfig: getNextAccount(), owner: getNextAccount() },
    data: getRepairOracleConfigPointerInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
