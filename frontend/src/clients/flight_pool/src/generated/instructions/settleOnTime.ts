/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPoolPda, findTreasuryAuthorityPda } from "../pdas";
import { FLIGHT_POOL_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_ON_TIME_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  32, 68, 238, 233, 129, 75, 192, 118,
]);

export function getSettleOnTimeDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_ON_TIME_DISCRIMINATOR,
  );
}

export type SettleOnTimeInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountPoolTreasury extends string | AccountMeta<string> = string,
  TAccountTreasuryAuthority extends string | AccountMeta<string> = string,
  TAccountRecipient extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountPoolTreasury extends string
        ? WritableAccount<TAccountPoolTreasury>
        : TAccountPoolTreasury,
      TAccountTreasuryAuthority extends string
        ? ReadonlyAccount<TAccountTreasuryAuthority>
        : TAccountTreasuryAuthority,
      TAccountRecipient extends string
        ? WritableAccount<TAccountRecipient>
        : TAccountRecipient,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type SettleOnTimeInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
};

export type SettleOnTimeInstructionDataArgs = {
  flightId: string;
  date: number | bigint;
};

export function getSettleOnTimeInstructionDataEncoder(): Encoder<SettleOnTimeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SETTLE_ON_TIME_DISCRIMINATOR }),
  );
}

export function getSettleOnTimeInstructionDataDecoder(): Decoder<SettleOnTimeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
  ]);
}

export function getSettleOnTimeInstructionDataCodec(): Codec<
  SettleOnTimeInstructionDataArgs,
  SettleOnTimeInstructionData
> {
  return combineCodec(
    getSettleOnTimeInstructionDataEncoder(),
    getSettleOnTimeInstructionDataDecoder(),
  );
}

export type SettleOnTimeAsyncInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountTreasuryAuthority extends string = string,
  TAccountRecipient extends string = string,
  TAccountStableMint extends string = string,
  TAccountController extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  pool?: Address<TAccountPool>;
  poolTreasury: Address<TAccountPoolTreasury>;
  treasuryAuthority?: Address<TAccountTreasuryAuthority>;
  /**
   * Recipient is trusted (D7) — controller is `has_one`-gated. We
   * constrain the mint to prevent accidental wrong-mint transfers.
   */
  recipient: Address<TAccountRecipient>;
  /**
   * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
   * account so the token program can verify mint + decimals.
   */
  stableMint: Address<TAccountStableMint>;
  controller: TransactionSigner<TAccountController>;
  tokenProgram?: Address<TAccountTokenProgram>;
  flightId: SettleOnTimeInstructionDataArgs["flightId"];
  date: SettleOnTimeInstructionDataArgs["date"];
};

export async function getSettleOnTimeInstructionAsync<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountPoolTreasury extends string,
  TAccountTreasuryAuthority extends string,
  TAccountRecipient extends string,
  TAccountStableMint extends string,
  TAccountController extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: SettleOnTimeAsyncInput<
    TAccountConfig,
    TAccountPool,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleOnTimeInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    treasuryAuthority: {
      value: input.treasuryAuthority ?? null,
      isWritable: false,
    },
    recipient: { value: input.recipient ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    controller: { value: input.controller ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      date: getNonNullResolvedInstructionInput("date", args.date),
    });
  }
  if (!accounts.treasuryAuthority.value) {
    accounts.treasuryAuthority.value = await findTreasuryAuthorityPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("treasuryAuthority", accounts.treasuryAuthority),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getSettleOnTimeInstructionDataEncoder().encode(
      args as SettleOnTimeInstructionDataArgs,
    ),
    programAddress,
  } as SettleOnTimeInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountTokenProgram
  >);
}

export type SettleOnTimeInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountTreasuryAuthority extends string = string,
  TAccountRecipient extends string = string,
  TAccountStableMint extends string = string,
  TAccountController extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  poolTreasury: Address<TAccountPoolTreasury>;
  treasuryAuthority: Address<TAccountTreasuryAuthority>;
  /**
   * Recipient is trusted (D7) — controller is `has_one`-gated. We
   * constrain the mint to prevent accidental wrong-mint transfers.
   */
  recipient: Address<TAccountRecipient>;
  /**
   * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
   * account so the token program can verify mint + decimals.
   */
  stableMint: Address<TAccountStableMint>;
  controller: TransactionSigner<TAccountController>;
  tokenProgram?: Address<TAccountTokenProgram>;
  flightId: SettleOnTimeInstructionDataArgs["flightId"];
  date: SettleOnTimeInstructionDataArgs["date"];
};

export function getSettleOnTimeInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountPoolTreasury extends string,
  TAccountTreasuryAuthority extends string,
  TAccountRecipient extends string,
  TAccountStableMint extends string,
  TAccountController extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: SettleOnTimeInput<
    TAccountConfig,
    TAccountPool,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): SettleOnTimeInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountPoolTreasury,
  TAccountTreasuryAuthority,
  TAccountRecipient,
  TAccountStableMint,
  TAccountController,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    treasuryAuthority: {
      value: input.treasuryAuthority ?? null,
      isWritable: false,
    },
    recipient: { value: input.recipient ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    controller: { value: input.controller ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("treasuryAuthority", accounts.treasuryAuthority),
      getAccountMeta("recipient", accounts.recipient),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getSettleOnTimeInstructionDataEncoder().encode(
      args as SettleOnTimeInstructionDataArgs,
    ),
    programAddress,
  } as SettleOnTimeInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountRecipient,
    TAccountStableMint,
    TAccountController,
    TAccountTokenProgram
  >);
}

export type ParsedSettleOnTimeInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    poolTreasury: TAccountMetas[2];
    treasuryAuthority: TAccountMetas[3];
    /**
     * Recipient is trusted (D7) — controller is `has_one`-gated. We
     * constrain the mint to prevent accidental wrong-mint transfers.
     */
    recipient: TAccountMetas[4];
    /**
     * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
     * account so the token program can verify mint + decimals.
     */
    stableMint: TAccountMetas[5];
    controller: TAccountMetas[6];
    tokenProgram: TAccountMetas[7];
  };
  data: SettleOnTimeInstructionData;
};

export function parseSettleOnTimeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleOnTimeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      poolTreasury: getNextAccount(),
      treasuryAuthority: getNextAccount(),
      recipient: getNextAccount(),
      stableMint: getNextAccount(),
      controller: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getSettleOnTimeInstructionDataDecoder().decode(instruction.data),
  };
}
