/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findBuyerRecordPda, findConfigPda, findPoolPda } from "../pdas";
import { FLIGHT_POOL_PROGRAM_ADDRESS } from "../programs";

export const ADD_BUYER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  185, 148, 138, 128, 244, 57, 117, 250,
]);

export function getAddBuyerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_BUYER_DISCRIMINATOR);
}

export type AddBuyerInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountBuyerRecord extends string | AccountMeta<string> = string,
  TAccountBuyerStableAccount extends string | AccountMeta<string> = string,
  TAccountPoolTreasury extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountBuyerRecord extends string
        ? WritableAccount<TAccountBuyerRecord>
        : TAccountBuyerRecord,
      TAccountBuyerStableAccount extends string
        ? WritableAccount<TAccountBuyerStableAccount>
        : TAccountBuyerStableAccount,
      TAccountPoolTreasury extends string
        ? WritableAccount<TAccountPoolTreasury>
        : TAccountPoolTreasury,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountBuyer extends string
        ? WritableSignerAccount<TAccountBuyer> &
            AccountSignerMeta<TAccountBuyer>
        : TAccountBuyer,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddBuyerInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
};

export type AddBuyerInstructionDataArgs = {
  flightId: string;
  date: number | bigint;
};

export function getAddBuyerInstructionDataEncoder(): Encoder<AddBuyerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_BUYER_DISCRIMINATOR }),
  );
}

export function getAddBuyerInstructionDataDecoder(): Decoder<AddBuyerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
  ]);
}

export function getAddBuyerInstructionDataCodec(): Codec<
  AddBuyerInstructionDataArgs,
  AddBuyerInstructionData
> {
  return combineCodec(
    getAddBuyerInstructionDataEncoder(),
    getAddBuyerInstructionDataDecoder(),
  );
}

export type AddBuyerAsyncInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountBuyerRecord extends string = string,
  TAccountBuyerStableAccount extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountStableMint extends string = string,
  TAccountBuyer extends string = string,
  TAccountController extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  pool?: Address<TAccountPool>;
  /**
   * `BuyerRecord` PDA — strict `init`. Re-purchase by the same buyer
   * for the same pool reverts via PDA collision (D6).
   */
  buyerRecord?: Address<TAccountBuyerRecord>;
  /**
   * Buyer's stable-coin ATA. Validated to match the configured mint and
   * to be owned by the buyer signer (pre-emptive auth, beyond Anchor's
   * transfer authority check).
   */
  buyerStableAccount: Address<TAccountBuyerStableAccount>;
  poolTreasury: Address<TAccountPoolTreasury>;
  /**
   * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
   * account so the token program can verify mint + decimals on every
   * transfer (Token-2022 deprecates plain `Transfer`).
   */
  stableMint: Address<TAccountStableMint>;
  buyer: TransactionSigner<TAccountBuyer>;
  controller: TransactionSigner<TAccountController>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: AddBuyerInstructionDataArgs["flightId"];
  date: AddBuyerInstructionDataArgs["date"];
};

export async function getAddBuyerInstructionAsync<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountBuyerRecord extends string,
  TAccountBuyerStableAccount extends string,
  TAccountPoolTreasury extends string,
  TAccountStableMint extends string,
  TAccountBuyer extends string,
  TAccountController extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: AddBuyerAsyncInput<
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountBuyer,
    TAccountController,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddBuyerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountBuyer,
    TAccountController,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    buyerRecord: { value: input.buyerRecord ?? null, isWritable: true },
    buyerStableAccount: {
      value: input.buyerStableAccount ?? null,
      isWritable: true,
    },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    buyer: { value: input.buyer ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      date: getNonNullResolvedInstructionInput("date", args.date),
    });
  }
  if (!accounts.buyerRecord.value) {
    accounts.buyerRecord.value = await findBuyerRecordPda({
      pool: getAddressFromResolvedInstructionAccount(
        "pool",
        accounts.pool.value,
      ),
      buyer: getAddressFromResolvedInstructionAccount(
        "buyer",
        accounts.buyer.value,
      ),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("buyerRecord", accounts.buyerRecord),
      getAccountMeta("buyerStableAccount", accounts.buyerStableAccount),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddBuyerInstructionDataEncoder().encode(
      args as AddBuyerInstructionDataArgs,
    ),
    programAddress,
  } as AddBuyerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountBuyer,
    TAccountController,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type AddBuyerInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountBuyerRecord extends string = string,
  TAccountBuyerStableAccount extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountStableMint extends string = string,
  TAccountBuyer extends string = string,
  TAccountController extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  /**
   * `BuyerRecord` PDA — strict `init`. Re-purchase by the same buyer
   * for the same pool reverts via PDA collision (D6).
   */
  buyerRecord: Address<TAccountBuyerRecord>;
  /**
   * Buyer's stable-coin ATA. Validated to match the configured mint and
   * to be owned by the buyer signer (pre-emptive auth, beyond Anchor's
   * transfer authority check).
   */
  buyerStableAccount: Address<TAccountBuyerStableAccount>;
  poolTreasury: Address<TAccountPoolTreasury>;
  /**
   * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
   * account so the token program can verify mint + decimals on every
   * transfer (Token-2022 deprecates plain `Transfer`).
   */
  stableMint: Address<TAccountStableMint>;
  buyer: TransactionSigner<TAccountBuyer>;
  controller: TransactionSigner<TAccountController>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: AddBuyerInstructionDataArgs["flightId"];
  date: AddBuyerInstructionDataArgs["date"];
};

export function getAddBuyerInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountBuyerRecord extends string,
  TAccountBuyerStableAccount extends string,
  TAccountPoolTreasury extends string,
  TAccountStableMint extends string,
  TAccountBuyer extends string,
  TAccountController extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: AddBuyerInput<
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountBuyer,
    TAccountController,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddBuyerInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountBuyerRecord,
  TAccountBuyerStableAccount,
  TAccountPoolTreasury,
  TAccountStableMint,
  TAccountBuyer,
  TAccountController,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    buyerRecord: { value: input.buyerRecord ?? null, isWritable: true },
    buyerStableAccount: {
      value: input.buyerStableAccount ?? null,
      isWritable: true,
    },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    buyer: { value: input.buyer ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("buyerRecord", accounts.buyerRecord),
      getAccountMeta("buyerStableAccount", accounts.buyerStableAccount),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAddBuyerInstructionDataEncoder().encode(
      args as AddBuyerInstructionDataArgs,
    ),
    programAddress,
  } as AddBuyerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountBuyer,
    TAccountController,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedAddBuyerInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    /**
     * `BuyerRecord` PDA — strict `init`. Re-purchase by the same buyer
     * for the same pool reverts via PDA collision (D6).
     */
    buyerRecord: TAccountMetas[2];
    /**
     * Buyer's stable-coin ATA. Validated to match the configured mint and
     * to be owned by the buyer signer (pre-emptive auth, beyond Anchor's
     * transfer authority check).
     */
    buyerStableAccount: TAccountMetas[3];
    poolTreasury: TAccountMetas[4];
    /**
     * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
     * account so the token program can verify mint + decimals on every
     * transfer (Token-2022 deprecates plain `Transfer`).
     */
    stableMint: TAccountMetas[5];
    buyer: TAccountMetas[6];
    controller: TAccountMetas[7];
    tokenProgram: TAccountMetas[8];
    systemProgram: TAccountMetas[9];
  };
  data: AddBuyerInstructionData;
};

export function parseAddBuyerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddBuyerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      buyerRecord: getNextAccount(),
      buyerStableAccount: getNextAccount(),
      poolTreasury: getNextAccount(),
      stableMint: getNextAccount(),
      buyer: getNextAccount(),
      controller: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddBuyerInstructionDataDecoder().decode(instruction.data),
  };
}
