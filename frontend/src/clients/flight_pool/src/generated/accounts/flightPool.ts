/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getSettlementStatusDecoder,
  getSettlementStatusEncoder,
  type SettlementStatus,
  type SettlementStatusArgs,
} from "../types";

export const FLIGHT_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  135, 234, 128, 88, 70, 18, 82, 176,
]);

export function getFlightPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FLIGHT_POOL_DISCRIMINATOR);
}

export type FlightPool = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
  premium: bigint;
  payoff: bigint;
  delayHours: number;
  buyerCount: number;
  claimedCount: number;
  status: SettlementStatus;
  claimExpiry: bigint;
  bump: number;
};

export type FlightPoolArgs = {
  flightId: string;
  date: number | bigint;
  premium: number | bigint;
  payoff: number | bigint;
  delayHours: number;
  buyerCount: number;
  claimedCount: number;
  status: SettlementStatusArgs;
  claimExpiry: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link FlightPoolArgs} account data. */
export function getFlightPoolEncoder(): Encoder<FlightPoolArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
      ["premium", getU64Encoder()],
      ["payoff", getU64Encoder()],
      ["delayHours", getU32Encoder()],
      ["buyerCount", getU32Encoder()],
      ["claimedCount", getU32Encoder()],
      ["status", getSettlementStatusEncoder()],
      ["claimExpiry", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FLIGHT_POOL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link FlightPool} account data. */
export function getFlightPoolDecoder(): Decoder<FlightPool> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
    ["premium", getU64Decoder()],
    ["payoff", getU64Decoder()],
    ["delayHours", getU32Decoder()],
    ["buyerCount", getU32Decoder()],
    ["claimedCount", getU32Decoder()],
    ["status", getSettlementStatusDecoder()],
    ["claimExpiry", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link FlightPool} account data. */
export function getFlightPoolCodec(): Codec<FlightPoolArgs, FlightPool> {
  return combineCodec(getFlightPoolEncoder(), getFlightPoolDecoder());
}

export function decodeFlightPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<FlightPool, TAddress>;
export function decodeFlightPool<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<FlightPool, TAddress>;
export function decodeFlightPool<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<FlightPool, TAddress> | MaybeAccount<FlightPool, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getFlightPoolDecoder(),
  );
}

export async function fetchFlightPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<FlightPool, TAddress>> {
  const maybeAccount = await fetchMaybeFlightPool(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeFlightPool<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<FlightPool, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeFlightPool(maybeAccount);
}

export async function fetchAllFlightPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<FlightPool>[]> {
  const maybeAccounts = await fetchAllMaybeFlightPool(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeFlightPool(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<FlightPool>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeFlightPool(maybeAccount));
}
