/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getActiveFlightListCodec,
  getControllerConfigCodec,
  type ActiveFlightList,
  type ActiveFlightListArgs,
  type ControllerConfig,
  type ControllerConfigArgs,
} from "../accounts";
import {
  getBuyInsuranceInstructionAsync,
  getClassifyFlightsInstructionAsync,
  getExecuteSettlementsInstructionAsync,
  getInitializeInstructionAsync,
  getRepairOracleConfigPointerInstructionAsync,
  getSetAuthorizedKeeperInstructionAsync,
  parseBuyInsuranceInstruction,
  parseClassifyFlightsInstruction,
  parseExecuteSettlementsInstruction,
  parseInitializeInstruction,
  parseRepairOracleConfigPointerInstruction,
  parseSetAuthorizedKeeperInstruction,
  type BuyInsuranceAsyncInput,
  type ClassifyFlightsAsyncInput,
  type ExecuteSettlementsAsyncInput,
  type InitializeAsyncInput,
  type ParsedBuyInsuranceInstruction,
  type ParsedClassifyFlightsInstruction,
  type ParsedExecuteSettlementsInstruction,
  type ParsedInitializeInstruction,
  type ParsedRepairOracleConfigPointerInstruction,
  type ParsedSetAuthorizedKeeperInstruction,
  type RepairOracleConfigPointerAsyncInput,
  type SetAuthorizedKeeperAsyncInput,
} from "../instructions";
import { findActiveFlightListPda, findControllerConfigPda } from "../pdas";

export const CONTROLLER_PROGRAM_ADDRESS =
  "G4v4i3LoLX7v3cEb3cehNGWMHbvHArRyPSEiZmg5VSot" as Address<"G4v4i3LoLX7v3cEb3cehNGWMHbvHArRyPSEiZmg5VSot">;

export enum ControllerAccount {
  ActiveFlightList,
  ControllerConfig,
}

export function identifyControllerAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): ControllerAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([96, 201, 242, 209, 187, 158, 165, 22]),
      ),
      0,
    )
  ) {
    return ControllerAccount.ActiveFlightList;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([185, 239, 115, 13, 26, 3, 55, 72]),
      ),
      0,
    )
  ) {
    return ControllerAccount.ControllerConfig;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "controller" },
  );
}

export enum ControllerInstruction {
  BuyInsurance,
  ClassifyFlights,
  ExecuteSettlements,
  Initialize,
  RepairOracleConfigPointer,
  SetAuthorizedKeeper,
}

export function identifyControllerInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): ControllerInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([20, 118, 1, 239, 176, 123, 243, 50]),
      ),
      0,
    )
  ) {
    return ControllerInstruction.BuyInsurance;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([22, 19, 194, 250, 138, 1, 227, 105]),
      ),
      0,
    )
  ) {
    return ControllerInstruction.ClassifyFlights;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([221, 116, 188, 62, 115, 7, 152, 208]),
      ),
      0,
    )
  ) {
    return ControllerInstruction.ExecuteSettlements;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237]),
      ),
      0,
    )
  ) {
    return ControllerInstruction.Initialize;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([7, 154, 89, 174, 154, 253, 13, 34]),
      ),
      0,
    )
  ) {
    return ControllerInstruction.RepairOracleConfigPointer;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([148, 91, 53, 179, 19, 2, 98, 42]),
      ),
      0,
    )
  ) {
    return ControllerInstruction.SetAuthorizedKeeper;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "controller" },
  );
}

export type ParsedControllerInstruction<
  TProgram extends string = "G4v4i3LoLX7v3cEb3cehNGWMHbvHArRyPSEiZmg5VSot",
> =
  | ({
      instructionType: ControllerInstruction.BuyInsurance;
    } & ParsedBuyInsuranceInstruction<TProgram>)
  | ({
      instructionType: ControllerInstruction.ClassifyFlights;
    } & ParsedClassifyFlightsInstruction<TProgram>)
  | ({
      instructionType: ControllerInstruction.ExecuteSettlements;
    } & ParsedExecuteSettlementsInstruction<TProgram>)
  | ({
      instructionType: ControllerInstruction.Initialize;
    } & ParsedInitializeInstruction<TProgram>)
  | ({
      instructionType: ControllerInstruction.RepairOracleConfigPointer;
    } & ParsedRepairOracleConfigPointerInstruction<TProgram>)
  | ({
      instructionType: ControllerInstruction.SetAuthorizedKeeper;
    } & ParsedSetAuthorizedKeeperInstruction<TProgram>);

export function parseControllerInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedControllerInstruction<TProgram> {
  const instructionType = identifyControllerInstruction(instruction);
  switch (instructionType) {
    case ControllerInstruction.BuyInsurance: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ControllerInstruction.BuyInsurance,
        ...parseBuyInsuranceInstruction(instruction),
      };
    }
    case ControllerInstruction.ClassifyFlights: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ControllerInstruction.ClassifyFlights,
        ...parseClassifyFlightsInstruction(instruction),
      };
    }
    case ControllerInstruction.ExecuteSettlements: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ControllerInstruction.ExecuteSettlements,
        ...parseExecuteSettlementsInstruction(instruction),
      };
    }
    case ControllerInstruction.Initialize: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ControllerInstruction.Initialize,
        ...parseInitializeInstruction(instruction),
      };
    }
    case ControllerInstruction.RepairOracleConfigPointer: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ControllerInstruction.RepairOracleConfigPointer,
        ...parseRepairOracleConfigPointerInstruction(instruction),
      };
    }
    case ControllerInstruction.SetAuthorizedKeeper: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: ControllerInstruction.SetAuthorizedKeeper,
        ...parseSetAuthorizedKeeperInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        {
          instructionType: instructionType as string,
          programName: "controller",
        },
      );
  }
}

export type ControllerPlugin = {
  accounts: ControllerPluginAccounts;
  instructions: ControllerPluginInstructions;
  pdas: ControllerPluginPdas;
};

export type ControllerPluginAccounts = {
  activeFlightList: ReturnType<typeof getActiveFlightListCodec> &
    SelfFetchFunctions<ActiveFlightListArgs, ActiveFlightList>;
  controllerConfig: ReturnType<typeof getControllerConfigCodec> &
    SelfFetchFunctions<ControllerConfigArgs, ControllerConfig>;
};

export type ControllerPluginInstructions = {
  buyInsurance: (
    input: BuyInsuranceAsyncInput,
  ) => ReturnType<typeof getBuyInsuranceInstructionAsync> &
    SelfPlanAndSendFunctions;
  classifyFlights: (
    input: ClassifyFlightsAsyncInput,
  ) => ReturnType<typeof getClassifyFlightsInstructionAsync> &
    SelfPlanAndSendFunctions;
  executeSettlements: (
    input: ExecuteSettlementsAsyncInput,
  ) => ReturnType<typeof getExecuteSettlementsInstructionAsync> &
    SelfPlanAndSendFunctions;
  initialize: (
    input: InitializeAsyncInput,
  ) => ReturnType<typeof getInitializeInstructionAsync> &
    SelfPlanAndSendFunctions;
  repairOracleConfigPointer: (
    input: RepairOracleConfigPointerAsyncInput,
  ) => ReturnType<typeof getRepairOracleConfigPointerInstructionAsync> &
    SelfPlanAndSendFunctions;
  setAuthorizedKeeper: (
    input: SetAuthorizedKeeperAsyncInput,
  ) => ReturnType<typeof getSetAuthorizedKeeperInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type ControllerPluginPdas = {
  controllerConfig: typeof findControllerConfigPda;
  activeFlightList: typeof findActiveFlightListPda;
};

export type ControllerPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function controllerProgram() {
  return <T extends ControllerPluginRequirements>(
    client: T,
  ): Omit<T, "controller"> & { controller: ControllerPlugin } => {
    return extendClient(client, {
      controller: <ControllerPlugin>{
        accounts: {
          activeFlightList: addSelfFetchFunctions(
            client,
            getActiveFlightListCodec(),
          ),
          controllerConfig: addSelfFetchFunctions(
            client,
            getControllerConfigCodec(),
          ),
        },
        instructions: {
          buyInsurance: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getBuyInsuranceInstructionAsync(input),
            ),
          classifyFlights: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getClassifyFlightsInstructionAsync(input),
            ),
          executeSettlements: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getExecuteSettlementsInstructionAsync(input),
            ),
          initialize: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeInstructionAsync(input),
            ),
          repairOracleConfigPointer: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getRepairOracleConfigPointerInstructionAsync(input),
            ),
          setAuthorizedKeeper: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getSetAuthorizedKeeperInstructionAsync(input),
            ),
        },
        pdas: {
          controllerConfig: findControllerConfigPda,
          activeFlightList: findActiveFlightListPda,
        },
      },
    });
  };
}
