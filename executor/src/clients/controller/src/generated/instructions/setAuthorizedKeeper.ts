/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findControllerConfigPda } from "../pdas";
import { CONTROLLER_PROGRAM_ADDRESS } from "../programs";

export const SET_AUTHORIZED_KEEPER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([148, 91, 53, 179, 19, 2, 98, 42]);

export function getSetAuthorizedKeeperDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_AUTHORIZED_KEEPER_DISCRIMINATOR,
  );
}

export type SetAuthorizedKeeperInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountControllerConfig extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountControllerConfig extends string
        ? WritableAccount<TAccountControllerConfig>
        : TAccountControllerConfig,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type SetAuthorizedKeeperInstructionData = {
  discriminator: ReadonlyUint8Array;
  newKeeper: Address;
};

export type SetAuthorizedKeeperInstructionDataArgs = { newKeeper: Address };

export function getSetAuthorizedKeeperInstructionDataEncoder(): FixedSizeEncoder<SetAuthorizedKeeperInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newKeeper", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_AUTHORIZED_KEEPER_DISCRIMINATOR,
    }),
  );
}

export function getSetAuthorizedKeeperInstructionDataDecoder(): FixedSizeDecoder<SetAuthorizedKeeperInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newKeeper", getAddressDecoder()],
  ]);
}

export function getSetAuthorizedKeeperInstructionDataCodec(): FixedSizeCodec<
  SetAuthorizedKeeperInstructionDataArgs,
  SetAuthorizedKeeperInstructionData
> {
  return combineCodec(
    getSetAuthorizedKeeperInstructionDataEncoder(),
    getSetAuthorizedKeeperInstructionDataDecoder(),
  );
}

export type SetAuthorizedKeeperAsyncInput<
  TAccountControllerConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  controllerConfig?: Address<TAccountControllerConfig>;
  owner: TransactionSigner<TAccountOwner>;
  newKeeper: SetAuthorizedKeeperInstructionDataArgs["newKeeper"];
};

export async function getSetAuthorizedKeeperInstructionAsync<
  TAccountControllerConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: SetAuthorizedKeeperAsyncInput<TAccountControllerConfig, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetAuthorizedKeeperInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOwner
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: true,
    },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.controllerConfig.value) {
    accounts.controllerConfig.value = await findControllerConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetAuthorizedKeeperInstructionDataEncoder().encode(
      args as SetAuthorizedKeeperInstructionDataArgs,
    ),
    programAddress,
  } as SetAuthorizedKeeperInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOwner
  >);
}

export type SetAuthorizedKeeperInput<
  TAccountControllerConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  controllerConfig: Address<TAccountControllerConfig>;
  owner: TransactionSigner<TAccountOwner>;
  newKeeper: SetAuthorizedKeeperInstructionDataArgs["newKeeper"];
};

export function getSetAuthorizedKeeperInstruction<
  TAccountControllerConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: SetAuthorizedKeeperInput<TAccountControllerConfig, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): SetAuthorizedKeeperInstruction<
  TProgramAddress,
  TAccountControllerConfig,
  TAccountOwner
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: true,
    },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetAuthorizedKeeperInstructionDataEncoder().encode(
      args as SetAuthorizedKeeperInstructionDataArgs,
    ),
    programAddress,
  } as SetAuthorizedKeeperInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountOwner
  >);
}

export type ParsedSetAuthorizedKeeperInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    controllerConfig: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: SetAuthorizedKeeperInstructionData;
};

export function parseSetAuthorizedKeeperInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetAuthorizedKeeperInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { controllerConfig: getNextAccount(), owner: getNextAccount() },
    data: getSetAuthorizedKeeperInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
