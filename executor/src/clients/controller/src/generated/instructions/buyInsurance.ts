/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findActiveFlightListPda, findControllerConfigPda } from "../pdas";
import { CONTROLLER_PROGRAM_ADDRESS } from "../programs";

export const BUY_INSURANCE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  20, 118, 1, 239, 176, 123, 243, 50,
]);

export function getBuyInsuranceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    BUY_INSURANCE_DISCRIMINATOR,
  );
}

export type BuyInsuranceInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountControllerConfig extends string | AccountMeta<string> = string,
  TAccountActiveFlightList extends string | AccountMeta<string> = string,
  TAccountGovernanceProgram extends string | AccountMeta<string> = string,
  TAccountGovernanceConfig extends string | AccountMeta<string> = string,
  TAccountRouteAccount extends string | AccountMeta<string> = string,
  TAccountOracleProgram extends string | AccountMeta<string> = string,
  TAccountOracleConfig extends string | AccountMeta<string> = string,
  TAccountFlightData extends string | AccountMeta<string> = string,
  TAccountFlightPoolProgram extends string | AccountMeta<string> = string,
  TAccountFlightPoolConfig extends string | AccountMeta<string> = string,
  TAccountFlightPool extends string | AccountMeta<string> = string,
  TAccountBuyerRecord extends string | AccountMeta<string> = string,
  TAccountBuyerStableAccount extends string | AccountMeta<string> = string,
  TAccountPoolTreasury extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountVaultProgram extends string | AccountMeta<string> = string,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountTraveler extends string | AccountMeta<string> = string,
  TAccountStableTokenProgram extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountControllerConfig extends string
        ? WritableAccount<TAccountControllerConfig>
        : TAccountControllerConfig,
      TAccountActiveFlightList extends string
        ? WritableAccount<TAccountActiveFlightList>
        : TAccountActiveFlightList,
      TAccountGovernanceProgram extends string
        ? ReadonlyAccount<TAccountGovernanceProgram>
        : TAccountGovernanceProgram,
      TAccountGovernanceConfig extends string
        ? ReadonlyAccount<TAccountGovernanceConfig>
        : TAccountGovernanceConfig,
      TAccountRouteAccount extends string
        ? ReadonlyAccount<TAccountRouteAccount>
        : TAccountRouteAccount,
      TAccountOracleProgram extends string
        ? ReadonlyAccount<TAccountOracleProgram>
        : TAccountOracleProgram,
      TAccountOracleConfig extends string
        ? WritableAccount<TAccountOracleConfig>
        : TAccountOracleConfig,
      TAccountFlightData extends string
        ? WritableAccount<TAccountFlightData>
        : TAccountFlightData,
      TAccountFlightPoolProgram extends string
        ? ReadonlyAccount<TAccountFlightPoolProgram>
        : TAccountFlightPoolProgram,
      TAccountFlightPoolConfig extends string
        ? ReadonlyAccount<TAccountFlightPoolConfig>
        : TAccountFlightPoolConfig,
      TAccountFlightPool extends string
        ? WritableAccount<TAccountFlightPool>
        : TAccountFlightPool,
      TAccountBuyerRecord extends string
        ? WritableAccount<TAccountBuyerRecord>
        : TAccountBuyerRecord,
      TAccountBuyerStableAccount extends string
        ? WritableAccount<TAccountBuyerStableAccount>
        : TAccountBuyerStableAccount,
      TAccountPoolTreasury extends string
        ? WritableAccount<TAccountPoolTreasury>
        : TAccountPoolTreasury,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountVaultProgram extends string
        ? ReadonlyAccount<TAccountVaultProgram>
        : TAccountVaultProgram,
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountTraveler extends string
        ? WritableSignerAccount<TAccountTraveler> &
            AccountSignerMeta<TAccountTraveler>
        : TAccountTraveler,
      TAccountStableTokenProgram extends string
        ? ReadonlyAccount<TAccountStableTokenProgram>
        : TAccountStableTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BuyInsuranceInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  origin: string;
  destination: string;
  date: bigint;
};

export type BuyInsuranceInstructionDataArgs = {
  flightId: string;
  origin: string;
  destination: string;
  date: number | bigint;
};

export function getBuyInsuranceInstructionDataEncoder(): Encoder<BuyInsuranceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["origin", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["destination", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BUY_INSURANCE_DISCRIMINATOR }),
  );
}

export function getBuyInsuranceInstructionDataDecoder(): Decoder<BuyInsuranceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["origin", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["destination", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
  ]);
}

export function getBuyInsuranceInstructionDataCodec(): Codec<
  BuyInsuranceInstructionDataArgs,
  BuyInsuranceInstructionData
> {
  return combineCodec(
    getBuyInsuranceInstructionDataEncoder(),
    getBuyInsuranceInstructionDataDecoder(),
  );
}

export type BuyInsuranceAsyncInput<
  TAccountControllerConfig extends string = string,
  TAccountActiveFlightList extends string = string,
  TAccountGovernanceProgram extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountRouteAccount extends string = string,
  TAccountOracleProgram extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFlightData extends string = string,
  TAccountFlightPoolProgram extends string = string,
  TAccountFlightPoolConfig extends string = string,
  TAccountFlightPool extends string = string,
  TAccountBuyerRecord extends string = string,
  TAccountBuyerStableAccount extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountStableMint extends string = string,
  TAccountVaultProgram extends string = string,
  TAccountVaultState extends string = string,
  TAccountTraveler extends string = string,
  TAccountStableTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  controllerConfig?: Address<TAccountControllerConfig>;
  activeFlightList?: Address<TAccountActiveFlightList>;
  governanceProgram: Address<TAccountGovernanceProgram>;
  governanceConfig: Address<TAccountGovernanceConfig>;
  routeAccount: Address<TAccountRouteAccount>;
  oracleProgram: Address<TAccountOracleProgram>;
  oracleConfig: Address<TAccountOracleConfig>;
  /**
   * (init'd there). Existence is checked in the handler to detect
   * first-buy. Owner enforced by oracle's strict init.
   */
  flightData: Address<TAccountFlightData>;
  flightPoolProgram: Address<TAccountFlightPoolProgram>;
  flightPoolConfig: Address<TAccountFlightPoolConfig>;
  /** flight_pool.register_pool when first-buy. */
  flightPool: Address<TAccountFlightPool>;
  buyerRecord: Address<TAccountBuyerRecord>;
  buyerStableAccount: Address<TAccountBuyerStableAccount>;
  poolTreasury: Address<TAccountPoolTreasury>;
  /**
   * Stable mint (PUSD / mock-PUSD). Forwarded to flight_pool.add_buyer's
   * `transfer_checked` CPI.
   */
  stableMint: Address<TAccountStableMint>;
  vaultProgram: Address<TAccountVaultProgram>;
  vaultState: Address<TAccountVaultState>;
  traveler: TransactionSigner<TAccountTraveler>;
  /**
   * Stable token program — Token-2022 in production (since PUSD is
   * Token-2022); the same field accepts classic SPL for parity.
   */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: BuyInsuranceInstructionDataArgs["flightId"];
  origin: BuyInsuranceInstructionDataArgs["origin"];
  destination: BuyInsuranceInstructionDataArgs["destination"];
  date: BuyInsuranceInstructionDataArgs["date"];
};

export async function getBuyInsuranceInstructionAsync<
  TAccountControllerConfig extends string,
  TAccountActiveFlightList extends string,
  TAccountGovernanceProgram extends string,
  TAccountGovernanceConfig extends string,
  TAccountRouteAccount extends string,
  TAccountOracleProgram extends string,
  TAccountOracleConfig extends string,
  TAccountFlightData extends string,
  TAccountFlightPoolProgram extends string,
  TAccountFlightPoolConfig extends string,
  TAccountFlightPool extends string,
  TAccountBuyerRecord extends string,
  TAccountBuyerStableAccount extends string,
  TAccountPoolTreasury extends string,
  TAccountStableMint extends string,
  TAccountVaultProgram extends string,
  TAccountVaultState extends string,
  TAccountTraveler extends string,
  TAccountStableTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: BuyInsuranceAsyncInput<
    TAccountControllerConfig,
    TAccountActiveFlightList,
    TAccountGovernanceProgram,
    TAccountGovernanceConfig,
    TAccountRouteAccount,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountFlightData,
    TAccountFlightPoolProgram,
    TAccountFlightPoolConfig,
    TAccountFlightPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountVaultProgram,
    TAccountVaultState,
    TAccountTraveler,
    TAccountStableTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  BuyInsuranceInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountActiveFlightList,
    TAccountGovernanceProgram,
    TAccountGovernanceConfig,
    TAccountRouteAccount,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountFlightData,
    TAccountFlightPoolProgram,
    TAccountFlightPoolConfig,
    TAccountFlightPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountVaultProgram,
    TAccountVaultState,
    TAccountTraveler,
    TAccountStableTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: true,
    },
    activeFlightList: {
      value: input.activeFlightList ?? null,
      isWritable: true,
    },
    governanceProgram: {
      value: input.governanceProgram ?? null,
      isWritable: false,
    },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: false,
    },
    routeAccount: { value: input.routeAccount ?? null, isWritable: false },
    oracleProgram: { value: input.oracleProgram ?? null, isWritable: false },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: true },
    flightData: { value: input.flightData ?? null, isWritable: true },
    flightPoolProgram: {
      value: input.flightPoolProgram ?? null,
      isWritable: false,
    },
    flightPoolConfig: {
      value: input.flightPoolConfig ?? null,
      isWritable: false,
    },
    flightPool: { value: input.flightPool ?? null, isWritable: true },
    buyerRecord: { value: input.buyerRecord ?? null, isWritable: true },
    buyerStableAccount: {
      value: input.buyerStableAccount ?? null,
      isWritable: true,
    },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    vaultProgram: { value: input.vaultProgram ?? null, isWritable: false },
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    traveler: { value: input.traveler ?? null, isWritable: true },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.controllerConfig.value) {
    accounts.controllerConfig.value = await findControllerConfigPda();
  }
  if (!accounts.activeFlightList.value) {
    accounts.activeFlightList.value = await findActiveFlightListPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("activeFlightList", accounts.activeFlightList),
      getAccountMeta("governanceProgram", accounts.governanceProgram),
      getAccountMeta("governanceConfig", accounts.governanceConfig),
      getAccountMeta("routeAccount", accounts.routeAccount),
      getAccountMeta("oracleProgram", accounts.oracleProgram),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("flightData", accounts.flightData),
      getAccountMeta("flightPoolProgram", accounts.flightPoolProgram),
      getAccountMeta("flightPoolConfig", accounts.flightPoolConfig),
      getAccountMeta("flightPool", accounts.flightPool),
      getAccountMeta("buyerRecord", accounts.buyerRecord),
      getAccountMeta("buyerStableAccount", accounts.buyerStableAccount),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("vaultProgram", accounts.vaultProgram),
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("traveler", accounts.traveler),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getBuyInsuranceInstructionDataEncoder().encode(
      args as BuyInsuranceInstructionDataArgs,
    ),
    programAddress,
  } as BuyInsuranceInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountActiveFlightList,
    TAccountGovernanceProgram,
    TAccountGovernanceConfig,
    TAccountRouteAccount,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountFlightData,
    TAccountFlightPoolProgram,
    TAccountFlightPoolConfig,
    TAccountFlightPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountVaultProgram,
    TAccountVaultState,
    TAccountTraveler,
    TAccountStableTokenProgram,
    TAccountSystemProgram
  >);
}

export type BuyInsuranceInput<
  TAccountControllerConfig extends string = string,
  TAccountActiveFlightList extends string = string,
  TAccountGovernanceProgram extends string = string,
  TAccountGovernanceConfig extends string = string,
  TAccountRouteAccount extends string = string,
  TAccountOracleProgram extends string = string,
  TAccountOracleConfig extends string = string,
  TAccountFlightData extends string = string,
  TAccountFlightPoolProgram extends string = string,
  TAccountFlightPoolConfig extends string = string,
  TAccountFlightPool extends string = string,
  TAccountBuyerRecord extends string = string,
  TAccountBuyerStableAccount extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountStableMint extends string = string,
  TAccountVaultProgram extends string = string,
  TAccountVaultState extends string = string,
  TAccountTraveler extends string = string,
  TAccountStableTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  controllerConfig: Address<TAccountControllerConfig>;
  activeFlightList: Address<TAccountActiveFlightList>;
  governanceProgram: Address<TAccountGovernanceProgram>;
  governanceConfig: Address<TAccountGovernanceConfig>;
  routeAccount: Address<TAccountRouteAccount>;
  oracleProgram: Address<TAccountOracleProgram>;
  oracleConfig: Address<TAccountOracleConfig>;
  /**
   * (init'd there). Existence is checked in the handler to detect
   * first-buy. Owner enforced by oracle's strict init.
   */
  flightData: Address<TAccountFlightData>;
  flightPoolProgram: Address<TAccountFlightPoolProgram>;
  flightPoolConfig: Address<TAccountFlightPoolConfig>;
  /** flight_pool.register_pool when first-buy. */
  flightPool: Address<TAccountFlightPool>;
  buyerRecord: Address<TAccountBuyerRecord>;
  buyerStableAccount: Address<TAccountBuyerStableAccount>;
  poolTreasury: Address<TAccountPoolTreasury>;
  /**
   * Stable mint (PUSD / mock-PUSD). Forwarded to flight_pool.add_buyer's
   * `transfer_checked` CPI.
   */
  stableMint: Address<TAccountStableMint>;
  vaultProgram: Address<TAccountVaultProgram>;
  vaultState: Address<TAccountVaultState>;
  traveler: TransactionSigner<TAccountTraveler>;
  /**
   * Stable token program — Token-2022 in production (since PUSD is
   * Token-2022); the same field accepts classic SPL for parity.
   */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: BuyInsuranceInstructionDataArgs["flightId"];
  origin: BuyInsuranceInstructionDataArgs["origin"];
  destination: BuyInsuranceInstructionDataArgs["destination"];
  date: BuyInsuranceInstructionDataArgs["date"];
};

export function getBuyInsuranceInstruction<
  TAccountControllerConfig extends string,
  TAccountActiveFlightList extends string,
  TAccountGovernanceProgram extends string,
  TAccountGovernanceConfig extends string,
  TAccountRouteAccount extends string,
  TAccountOracleProgram extends string,
  TAccountOracleConfig extends string,
  TAccountFlightData extends string,
  TAccountFlightPoolProgram extends string,
  TAccountFlightPoolConfig extends string,
  TAccountFlightPool extends string,
  TAccountBuyerRecord extends string,
  TAccountBuyerStableAccount extends string,
  TAccountPoolTreasury extends string,
  TAccountStableMint extends string,
  TAccountVaultProgram extends string,
  TAccountVaultState extends string,
  TAccountTraveler extends string,
  TAccountStableTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof CONTROLLER_PROGRAM_ADDRESS,
>(
  input: BuyInsuranceInput<
    TAccountControllerConfig,
    TAccountActiveFlightList,
    TAccountGovernanceProgram,
    TAccountGovernanceConfig,
    TAccountRouteAccount,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountFlightData,
    TAccountFlightPoolProgram,
    TAccountFlightPoolConfig,
    TAccountFlightPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountVaultProgram,
    TAccountVaultState,
    TAccountTraveler,
    TAccountStableTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BuyInsuranceInstruction<
  TProgramAddress,
  TAccountControllerConfig,
  TAccountActiveFlightList,
  TAccountGovernanceProgram,
  TAccountGovernanceConfig,
  TAccountRouteAccount,
  TAccountOracleProgram,
  TAccountOracleConfig,
  TAccountFlightData,
  TAccountFlightPoolProgram,
  TAccountFlightPoolConfig,
  TAccountFlightPool,
  TAccountBuyerRecord,
  TAccountBuyerStableAccount,
  TAccountPoolTreasury,
  TAccountStableMint,
  TAccountVaultProgram,
  TAccountVaultState,
  TAccountTraveler,
  TAccountStableTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CONTROLLER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    controllerConfig: {
      value: input.controllerConfig ?? null,
      isWritable: true,
    },
    activeFlightList: {
      value: input.activeFlightList ?? null,
      isWritable: true,
    },
    governanceProgram: {
      value: input.governanceProgram ?? null,
      isWritable: false,
    },
    governanceConfig: {
      value: input.governanceConfig ?? null,
      isWritable: false,
    },
    routeAccount: { value: input.routeAccount ?? null, isWritable: false },
    oracleProgram: { value: input.oracleProgram ?? null, isWritable: false },
    oracleConfig: { value: input.oracleConfig ?? null, isWritable: true },
    flightData: { value: input.flightData ?? null, isWritable: true },
    flightPoolProgram: {
      value: input.flightPoolProgram ?? null,
      isWritable: false,
    },
    flightPoolConfig: {
      value: input.flightPoolConfig ?? null,
      isWritable: false,
    },
    flightPool: { value: input.flightPool ?? null, isWritable: true },
    buyerRecord: { value: input.buyerRecord ?? null, isWritable: true },
    buyerStableAccount: {
      value: input.buyerStableAccount ?? null,
      isWritable: true,
    },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    vaultProgram: { value: input.vaultProgram ?? null, isWritable: false },
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    traveler: { value: input.traveler ?? null, isWritable: true },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("controllerConfig", accounts.controllerConfig),
      getAccountMeta("activeFlightList", accounts.activeFlightList),
      getAccountMeta("governanceProgram", accounts.governanceProgram),
      getAccountMeta("governanceConfig", accounts.governanceConfig),
      getAccountMeta("routeAccount", accounts.routeAccount),
      getAccountMeta("oracleProgram", accounts.oracleProgram),
      getAccountMeta("oracleConfig", accounts.oracleConfig),
      getAccountMeta("flightData", accounts.flightData),
      getAccountMeta("flightPoolProgram", accounts.flightPoolProgram),
      getAccountMeta("flightPoolConfig", accounts.flightPoolConfig),
      getAccountMeta("flightPool", accounts.flightPool),
      getAccountMeta("buyerRecord", accounts.buyerRecord),
      getAccountMeta("buyerStableAccount", accounts.buyerStableAccount),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("vaultProgram", accounts.vaultProgram),
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("traveler", accounts.traveler),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getBuyInsuranceInstructionDataEncoder().encode(
      args as BuyInsuranceInstructionDataArgs,
    ),
    programAddress,
  } as BuyInsuranceInstruction<
    TProgramAddress,
    TAccountControllerConfig,
    TAccountActiveFlightList,
    TAccountGovernanceProgram,
    TAccountGovernanceConfig,
    TAccountRouteAccount,
    TAccountOracleProgram,
    TAccountOracleConfig,
    TAccountFlightData,
    TAccountFlightPoolProgram,
    TAccountFlightPoolConfig,
    TAccountFlightPool,
    TAccountBuyerRecord,
    TAccountBuyerStableAccount,
    TAccountPoolTreasury,
    TAccountStableMint,
    TAccountVaultProgram,
    TAccountVaultState,
    TAccountTraveler,
    TAccountStableTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedBuyInsuranceInstruction<
  TProgram extends string = typeof CONTROLLER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    controllerConfig: TAccountMetas[0];
    activeFlightList: TAccountMetas[1];
    governanceProgram: TAccountMetas[2];
    governanceConfig: TAccountMetas[3];
    routeAccount: TAccountMetas[4];
    oracleProgram: TAccountMetas[5];
    oracleConfig: TAccountMetas[6];
    /**
     * (init'd there). Existence is checked in the handler to detect
     * first-buy. Owner enforced by oracle's strict init.
     */
    flightData: TAccountMetas[7];
    flightPoolProgram: TAccountMetas[8];
    flightPoolConfig: TAccountMetas[9];
    /** flight_pool.register_pool when first-buy. */
    flightPool: TAccountMetas[10];
    buyerRecord: TAccountMetas[11];
    buyerStableAccount: TAccountMetas[12];
    poolTreasury: TAccountMetas[13];
    /**
     * Stable mint (PUSD / mock-PUSD). Forwarded to flight_pool.add_buyer's
     * `transfer_checked` CPI.
     */
    stableMint: TAccountMetas[14];
    vaultProgram: TAccountMetas[15];
    vaultState: TAccountMetas[16];
    traveler: TAccountMetas[17];
    /**
     * Stable token program — Token-2022 in production (since PUSD is
     * Token-2022); the same field accepts classic SPL for parity.
     */
    stableTokenProgram: TAccountMetas[18];
    systemProgram: TAccountMetas[19];
  };
  data: BuyInsuranceInstructionData;
};

export function parseBuyInsuranceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBuyInsuranceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 20) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 20,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      controllerConfig: getNextAccount(),
      activeFlightList: getNextAccount(),
      governanceProgram: getNextAccount(),
      governanceConfig: getNextAccount(),
      routeAccount: getNextAccount(),
      oracleProgram: getNextAccount(),
      oracleConfig: getNextAccount(),
      flightData: getNextAccount(),
      flightPoolProgram: getNextAccount(),
      flightPoolConfig: getNextAccount(),
      flightPool: getNextAccount(),
      buyerRecord: getNextAccount(),
      buyerStableAccount: getNextAccount(),
      poolTreasury: getNextAccount(),
      stableMint: getNextAccount(),
      vaultProgram: getNextAccount(),
      vaultState: getNextAccount(),
      traveler: getNextAccount(),
      stableTokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBuyInsuranceInstructionDataDecoder().decode(instruction.data),
  };
}
