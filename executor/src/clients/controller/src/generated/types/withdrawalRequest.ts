/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

/**
 * `pending_assets` is the PUSD owed to the underwriter, snapshotted at
 * `request_withdrawal` time using the virtual-offset formula on the
 * pre-burn supply + TMA. The drain credits the underwriter's
 * `ClaimableBalance` by this exact amount — no re-pricing at drain time —
 * so the value is locked in regardless of inter-request supply/TMA drift.
 * Cancellation re-mints the original `shares` and restores TMA by
 * `pending_assets`.
 */
export type WithdrawalRequest = {
  owner: Address;
  shares: bigint;
  pendingAssets: bigint;
  timestamp: bigint;
  claimable: Address;
};

export type WithdrawalRequestArgs = {
  owner: Address;
  shares: number | bigint;
  pendingAssets: number | bigint;
  timestamp: number | bigint;
  claimable: Address;
};

export function getWithdrawalRequestEncoder(): FixedSizeEncoder<WithdrawalRequestArgs> {
  return getStructEncoder([
    ["owner", getAddressEncoder()],
    ["shares", getU64Encoder()],
    ["pendingAssets", getU64Encoder()],
    ["timestamp", getI64Encoder()],
    ["claimable", getAddressEncoder()],
  ]);
}

export function getWithdrawalRequestDecoder(): FixedSizeDecoder<WithdrawalRequest> {
  return getStructDecoder([
    ["owner", getAddressDecoder()],
    ["shares", getU64Decoder()],
    ["pendingAssets", getU64Decoder()],
    ["timestamp", getI64Decoder()],
    ["claimable", getAddressDecoder()],
  ]);
}

export function getWithdrawalRequestCodec(): FixedSizeCodec<
  WithdrawalRequestArgs,
  WithdrawalRequest
> {
  return combineCodec(
    getWithdrawalRequestEncoder(),
    getWithdrawalRequestDecoder(),
  );
}
