/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";

export const IS_ROUTE_WHITELISTED_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([120, 226, 103, 248, 69, 103, 81, 206]);

export function getIsRouteWhitelistedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    IS_ROUTE_WHITELISTED_DISCRIMINATOR,
  );
}

export type IsRouteWhitelistedInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountRoute extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountRoute extends string
        ? ReadonlyAccount<TAccountRoute>
        : TAccountRoute,
      ...TRemainingAccounts,
    ]
  >;

export type IsRouteWhitelistedInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  origin: string;
  destination: string;
};

export type IsRouteWhitelistedInstructionDataArgs = {
  flightId: string;
  origin: string;
  destination: string;
};

export function getIsRouteWhitelistedInstructionDataEncoder(): Encoder<IsRouteWhitelistedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["origin", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["destination", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({
      ...value,
      discriminator: IS_ROUTE_WHITELISTED_DISCRIMINATOR,
    }),
  );
}

export function getIsRouteWhitelistedInstructionDataDecoder(): Decoder<IsRouteWhitelistedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["origin", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["destination", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getIsRouteWhitelistedInstructionDataCodec(): Codec<
  IsRouteWhitelistedInstructionDataArgs,
  IsRouteWhitelistedInstructionData
> {
  return combineCodec(
    getIsRouteWhitelistedInstructionDataEncoder(),
    getIsRouteWhitelistedInstructionDataDecoder(),
  );
}

export type IsRouteWhitelistedInput<TAccountRoute extends string = string> = {
  /**
   * Untyped — the route may not exist. The handler verifies the address
   * derives from the seeds and the owner is this program before reading.
   */
  route: Address<TAccountRoute>;
  flightId: IsRouteWhitelistedInstructionDataArgs["flightId"];
  origin: IsRouteWhitelistedInstructionDataArgs["origin"];
  destination: IsRouteWhitelistedInstructionDataArgs["destination"];
};

export function getIsRouteWhitelistedInstruction<
  TAccountRoute extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: IsRouteWhitelistedInput<TAccountRoute>,
  config?: { programAddress?: TProgramAddress },
): IsRouteWhitelistedInstruction<TProgramAddress, TAccountRoute> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    route: { value: input.route ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [getAccountMeta("route", accounts.route)],
    data: getIsRouteWhitelistedInstructionDataEncoder().encode(
      args as IsRouteWhitelistedInstructionDataArgs,
    ),
    programAddress,
  } as IsRouteWhitelistedInstruction<TProgramAddress, TAccountRoute>);
}

export type ParsedIsRouteWhitelistedInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Untyped — the route may not exist. The handler verifies the address
     * derives from the seeds and the owner is this program before reading.
     */
    route: TAccountMetas[0];
  };
  data: IsRouteWhitelistedInstructionData;
};

export function parseIsRouteWhitelistedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedIsRouteWhitelistedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 1,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { route: getNextAccount() },
    data: getIsRouteWhitelistedInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
