/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findConfigPda,
  findDisableRouteAdminRecordPda,
  findRoutePda,
} from "../pdas";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";

export const WHITELIST_ROUTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [46, 107, 91, 49, 248, 52, 253, 208],
);

export function getWhitelistRouteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    WHITELIST_ROUTE_DISCRIMINATOR,
  );
}

export type WhitelistRouteInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoute extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoute extends string
        ? WritableAccount<TAccountRoute>
        : TAccountRoute,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type WhitelistRouteInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  origin: string;
  destination: string;
  premium: Option<bigint>;
  payoff: Option<bigint>;
  delayHours: Option<number>;
};

export type WhitelistRouteInstructionDataArgs = {
  flightId: string;
  origin: string;
  destination: string;
  premium: OptionOrNullable<number | bigint>;
  payoff: OptionOrNullable<number | bigint>;
  delayHours: OptionOrNullable<number>;
};

export function getWhitelistRouteInstructionDataEncoder(): Encoder<WhitelistRouteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["origin", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["destination", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["premium", getOptionEncoder(getU64Encoder())],
      ["payoff", getOptionEncoder(getU64Encoder())],
      ["delayHours", getOptionEncoder(getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: WHITELIST_ROUTE_DISCRIMINATOR }),
  );
}

export function getWhitelistRouteInstructionDataDecoder(): Decoder<WhitelistRouteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["origin", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["destination", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["premium", getOptionDecoder(getU64Decoder())],
    ["payoff", getOptionDecoder(getU64Decoder())],
    ["delayHours", getOptionDecoder(getU32Decoder())],
  ]);
}

export function getWhitelistRouteInstructionDataCodec(): Codec<
  WhitelistRouteInstructionDataArgs,
  WhitelistRouteInstructionData
> {
  return combineCodec(
    getWhitelistRouteInstructionDataEncoder(),
    getWhitelistRouteInstructionDataDecoder(),
  );
}

export type WhitelistRouteAsyncInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountCaller extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  route?: Address<TAccountRoute>;
  /**
   * Optional admin record — required when caller != config.owner. Anchor
   * resolves this via the seeds; clients omit it (pass `None`/`null` in
   * generated TS) when calling as the owner.
   */
  adminRecord?: Address<TAccountAdminRecord>;
  caller: TransactionSigner<TAccountCaller>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: WhitelistRouteInstructionDataArgs["flightId"];
  origin: WhitelistRouteInstructionDataArgs["origin"];
  destination: WhitelistRouteInstructionDataArgs["destination"];
  premium: WhitelistRouteInstructionDataArgs["premium"];
  payoff: WhitelistRouteInstructionDataArgs["payoff"];
  delayHours: WhitelistRouteInstructionDataArgs["delayHours"];
};

export async function getWhitelistRouteInstructionAsync<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TAccountAdminRecord extends string,
  TAccountCaller extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: WhitelistRouteAsyncInput<
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  WhitelistRouteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    route: { value: input.route ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    caller: { value: input.caller ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.route.value) {
    accounts.route.value = await findRoutePda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      origin: getNonNullResolvedInstructionInput("origin", args.origin),
      destination: getNonNullResolvedInstructionInput(
        "destination",
        args.destination,
      ),
    });
  }
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findDisableRouteAdminRecordPda({
      caller: getAddressFromResolvedInstructionAccount(
        "caller",
        accounts.caller.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
      getAccountMeta("adminRecord", accounts.adminRecord),
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getWhitelistRouteInstructionDataEncoder().encode(
      args as WhitelistRouteInstructionDataArgs,
    ),
    programAddress,
  } as WhitelistRouteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller,
    TAccountSystemProgram
  >);
}

export type WhitelistRouteInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountCaller extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  route: Address<TAccountRoute>;
  /**
   * Optional admin record — required when caller != config.owner. Anchor
   * resolves this via the seeds; clients omit it (pass `None`/`null` in
   * generated TS) when calling as the owner.
   */
  adminRecord?: Address<TAccountAdminRecord>;
  caller: TransactionSigner<TAccountCaller>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: WhitelistRouteInstructionDataArgs["flightId"];
  origin: WhitelistRouteInstructionDataArgs["origin"];
  destination: WhitelistRouteInstructionDataArgs["destination"];
  premium: WhitelistRouteInstructionDataArgs["premium"];
  payoff: WhitelistRouteInstructionDataArgs["payoff"];
  delayHours: WhitelistRouteInstructionDataArgs["delayHours"];
};

export function getWhitelistRouteInstruction<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TAccountAdminRecord extends string,
  TAccountCaller extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: WhitelistRouteInput<
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): WhitelistRouteInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountRoute,
  TAccountAdminRecord,
  TAccountCaller,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    route: { value: input.route ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    caller: { value: input.caller ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
      getAccountMeta("adminRecord", accounts.adminRecord),
      getAccountMeta("caller", accounts.caller),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getWhitelistRouteInstructionDataEncoder().encode(
      args as WhitelistRouteInstructionDataArgs,
    ),
    programAddress,
  } as WhitelistRouteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller,
    TAccountSystemProgram
  >);
}

export type ParsedWhitelistRouteInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    route: TAccountMetas[1];
    /**
     * Optional admin record — required when caller != config.owner. Anchor
     * resolves this via the seeds; clients omit it (pass `None`/`null` in
     * generated TS) when calling as the owner.
     */
    adminRecord?: TAccountMetas[2] | undefined;
    caller: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: WhitelistRouteInstructionData;
};

export function parseWhitelistRouteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedWhitelistRouteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GOVERNANCE_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      route: getNextAccount(),
      adminRecord: getNextOptionalAccount(),
      caller: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getWhitelistRouteInstructionDataDecoder().decode(instruction.data),
  };
}
