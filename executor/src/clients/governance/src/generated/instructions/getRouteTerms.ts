/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findRoutePda } from "../pdas";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";

export const GET_ROUTE_TERMS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [52, 112, 52, 122, 135, 225, 108, 35],
);

export function getGetRouteTermsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GET_ROUTE_TERMS_DISCRIMINATOR,
  );
}

export type GetRouteTermsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoute extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoute extends string
        ? ReadonlyAccount<TAccountRoute>
        : TAccountRoute,
      ...TRemainingAccounts,
    ]
  >;

export type GetRouteTermsInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  origin: string;
  destination: string;
};

export type GetRouteTermsInstructionDataArgs = {
  flightId: string;
  origin: string;
  destination: string;
};

export function getGetRouteTermsInstructionDataEncoder(): Encoder<GetRouteTermsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["origin", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["destination", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: GET_ROUTE_TERMS_DISCRIMINATOR }),
  );
}

export function getGetRouteTermsInstructionDataDecoder(): Decoder<GetRouteTermsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["origin", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["destination", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getGetRouteTermsInstructionDataCodec(): Codec<
  GetRouteTermsInstructionDataArgs,
  GetRouteTermsInstructionData
> {
  return combineCodec(
    getGetRouteTermsInstructionDataEncoder(),
    getGetRouteTermsInstructionDataDecoder(),
  );
}

export type GetRouteTermsAsyncInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
> = {
  config?: Address<TAccountConfig>;
  route?: Address<TAccountRoute>;
  flightId: GetRouteTermsInstructionDataArgs["flightId"];
  origin: GetRouteTermsInstructionDataArgs["origin"];
  destination: GetRouteTermsInstructionDataArgs["destination"];
};

export async function getGetRouteTermsInstructionAsync<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: GetRouteTermsAsyncInput<TAccountConfig, TAccountRoute>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  GetRouteTermsInstruction<TProgramAddress, TAccountConfig, TAccountRoute>
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    route: { value: input.route ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.route.value) {
    accounts.route.value = await findRoutePda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      origin: getNonNullResolvedInstructionInput("origin", args.origin),
      destination: getNonNullResolvedInstructionInput(
        "destination",
        args.destination,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
    ],
    data: getGetRouteTermsInstructionDataEncoder().encode(
      args as GetRouteTermsInstructionDataArgs,
    ),
    programAddress,
  } as GetRouteTermsInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute
  >);
}

export type GetRouteTermsInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
> = {
  config: Address<TAccountConfig>;
  route: Address<TAccountRoute>;
  flightId: GetRouteTermsInstructionDataArgs["flightId"];
  origin: GetRouteTermsInstructionDataArgs["origin"];
  destination: GetRouteTermsInstructionDataArgs["destination"];
};

export function getGetRouteTermsInstruction<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: GetRouteTermsInput<TAccountConfig, TAccountRoute>,
  config?: { programAddress?: TProgramAddress },
): GetRouteTermsInstruction<TProgramAddress, TAccountConfig, TAccountRoute> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    route: { value: input.route ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
    ],
    data: getGetRouteTermsInstructionDataEncoder().encode(
      args as GetRouteTermsInstructionDataArgs,
    ),
    programAddress,
  } as GetRouteTermsInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute
  >);
}

export type ParsedGetRouteTermsInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    route: TAccountMetas[1];
  };
  data: GetRouteTermsInstructionData;
};

export function parseGetRouteTermsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedGetRouteTermsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { config: getNextAccount(), route: getNextAccount() },
    data: getGetRouteTermsInstructionDataDecoder().decode(instruction.data),
  };
}
