/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findConfigPda,
  findDisableRouteAdminRecordPda,
  findRoutePda,
} from "../pdas";
import { GOVERNANCE_PROGRAM_ADDRESS } from "../programs";

export const DISABLE_ROUTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  113, 226, 157, 91, 114, 105, 201, 208,
]);

export function getDisableRouteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DISABLE_ROUTE_DISCRIMINATOR,
  );
}

export type DisableRouteInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountRoute extends string | AccountMeta<string> = string,
  TAccountAdminRecord extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountRoute extends string
        ? WritableAccount<TAccountRoute>
        : TAccountRoute,
      TAccountAdminRecord extends string
        ? ReadonlyAccount<TAccountAdminRecord>
        : TAccountAdminRecord,
      TAccountCaller extends string
        ? ReadonlySignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      ...TRemainingAccounts,
    ]
  >;

export type DisableRouteInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  origin: string;
  destination: string;
};

export type DisableRouteInstructionDataArgs = {
  flightId: string;
  origin: string;
  destination: string;
};

export function getDisableRouteInstructionDataEncoder(): Encoder<DisableRouteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["origin", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["destination", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: DISABLE_ROUTE_DISCRIMINATOR }),
  );
}

export function getDisableRouteInstructionDataDecoder(): Decoder<DisableRouteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["origin", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["destination", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getDisableRouteInstructionDataCodec(): Codec<
  DisableRouteInstructionDataArgs,
  DisableRouteInstructionData
> {
  return combineCodec(
    getDisableRouteInstructionDataEncoder(),
    getDisableRouteInstructionDataDecoder(),
  );
}

export type DisableRouteAsyncInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountCaller extends string = string,
> = {
  config?: Address<TAccountConfig>;
  route?: Address<TAccountRoute>;
  adminRecord?: Address<TAccountAdminRecord>;
  caller: TransactionSigner<TAccountCaller>;
  flightId: DisableRouteInstructionDataArgs["flightId"];
  origin: DisableRouteInstructionDataArgs["origin"];
  destination: DisableRouteInstructionDataArgs["destination"];
};

export async function getDisableRouteInstructionAsync<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TAccountAdminRecord extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: DisableRouteAsyncInput<
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DisableRouteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    route: { value: input.route ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    caller: { value: input.caller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.route.value) {
    accounts.route.value = await findRoutePda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      origin: getNonNullResolvedInstructionInput("origin", args.origin),
      destination: getNonNullResolvedInstructionInput(
        "destination",
        args.destination,
      ),
    });
  }
  if (!accounts.adminRecord.value) {
    accounts.adminRecord.value = await findDisableRouteAdminRecordPda({
      caller: getAddressFromResolvedInstructionAccount(
        "caller",
        accounts.caller.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
      getAccountMeta("adminRecord", accounts.adminRecord),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getDisableRouteInstructionDataEncoder().encode(
      args as DisableRouteInstructionDataArgs,
    ),
    programAddress,
  } as DisableRouteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >);
}

export type DisableRouteInput<
  TAccountConfig extends string = string,
  TAccountRoute extends string = string,
  TAccountAdminRecord extends string = string,
  TAccountCaller extends string = string,
> = {
  config: Address<TAccountConfig>;
  route: Address<TAccountRoute>;
  adminRecord?: Address<TAccountAdminRecord>;
  caller: TransactionSigner<TAccountCaller>;
  flightId: DisableRouteInstructionDataArgs["flightId"];
  origin: DisableRouteInstructionDataArgs["origin"];
  destination: DisableRouteInstructionDataArgs["destination"];
};

export function getDisableRouteInstruction<
  TAccountConfig extends string,
  TAccountRoute extends string,
  TAccountAdminRecord extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof GOVERNANCE_PROGRAM_ADDRESS,
>(
  input: DisableRouteInput<
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >,
  config?: { programAddress?: TProgramAddress },
): DisableRouteInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountRoute,
  TAccountAdminRecord,
  TAccountCaller
> {
  // Program address.
  const programAddress = config?.programAddress ?? GOVERNANCE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    route: { value: input.route ?? null, isWritable: true },
    adminRecord: { value: input.adminRecord ?? null, isWritable: false },
    caller: { value: input.caller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("route", accounts.route),
      getAccountMeta("adminRecord", accounts.adminRecord),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getDisableRouteInstructionDataEncoder().encode(
      args as DisableRouteInstructionDataArgs,
    ),
    programAddress,
  } as DisableRouteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountRoute,
    TAccountAdminRecord,
    TAccountCaller
  >);
}

export type ParsedDisableRouteInstruction<
  TProgram extends string = typeof GOVERNANCE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    route: TAccountMetas[1];
    adminRecord?: TAccountMetas[2] | undefined;
    caller: TAccountMetas[3];
  };
  data: DisableRouteInstructionData;
};

export function parseDisableRouteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDisableRouteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  const getNextOptionalAccount = () => {
    const accountMeta = getNextAccount();
    return accountMeta.address === GOVERNANCE_PROGRAM_ADDRESS
      ? undefined
      : accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      route: getNextAccount(),
      adminRecord: getNextOptionalAccount(),
      caller: getNextAccount(),
    },
    data: getDisableRouteInstructionDataDecoder().decode(instruction.data),
  };
}
