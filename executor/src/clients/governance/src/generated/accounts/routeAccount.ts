/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const ROUTE_ACCOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  135, 89, 73, 184, 33, 21, 243, 86,
]);

export function getRouteAccountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ROUTE_ACCOUNT_DISCRIMINATOR,
  );
}

export type RouteAccount = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  origin: string;
  destination: string;
  premium: Option<bigint>;
  payoff: Option<bigint>;
  delayHours: Option<number>;
  approved: boolean;
  bump: number;
};

export type RouteAccountArgs = {
  flightId: string;
  origin: string;
  destination: string;
  premium: OptionOrNullable<number | bigint>;
  payoff: OptionOrNullable<number | bigint>;
  delayHours: OptionOrNullable<number>;
  approved: boolean;
  bump: number;
};

/** Gets the encoder for {@link RouteAccountArgs} account data. */
export function getRouteAccountEncoder(): Encoder<RouteAccountArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["origin", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["destination", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["premium", getOptionEncoder(getU64Encoder())],
      ["payoff", getOptionEncoder(getU64Encoder())],
      ["delayHours", getOptionEncoder(getU32Encoder())],
      ["approved", getBooleanEncoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ROUTE_ACCOUNT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link RouteAccount} account data. */
export function getRouteAccountDecoder(): Decoder<RouteAccount> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["origin", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["destination", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["premium", getOptionDecoder(getU64Decoder())],
    ["payoff", getOptionDecoder(getU64Decoder())],
    ["delayHours", getOptionDecoder(getU32Decoder())],
    ["approved", getBooleanDecoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link RouteAccount} account data. */
export function getRouteAccountCodec(): Codec<RouteAccountArgs, RouteAccount> {
  return combineCodec(getRouteAccountEncoder(), getRouteAccountDecoder());
}

export function decodeRouteAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<RouteAccount, TAddress>;
export function decodeRouteAccount<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<RouteAccount, TAddress>;
export function decodeRouteAccount<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<RouteAccount, TAddress> | MaybeAccount<RouteAccount, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getRouteAccountDecoder(),
  );
}

export async function fetchRouteAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<RouteAccount, TAddress>> {
  const maybeAccount = await fetchMaybeRouteAccount(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeRouteAccount<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<RouteAccount, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeRouteAccount(maybeAccount);
}

export async function fetchAllRouteAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<RouteAccount>[]> {
  const maybeAccounts = await fetchAllMaybeRouteAccount(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeRouteAccount(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<RouteAccount>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeRouteAccount(maybeAccount));
}
