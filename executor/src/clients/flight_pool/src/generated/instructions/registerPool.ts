/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPoolPda } from "../pdas";
import { FLIGHT_POOL_PROGRAM_ADDRESS } from "../programs";

export const REGISTER_POOL_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  85, 229, 114, 47, 75, 145, 166, 100,
]);

export function getRegisterPoolDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REGISTER_POOL_DISCRIMINATOR,
  );
}

export type RegisterPoolInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TAccountRentPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      TAccountRentPayer extends string
        ? WritableSignerAccount<TAccountRentPayer> &
            AccountSignerMeta<TAccountRentPayer>
        : TAccountRentPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RegisterPoolInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
  premium: bigint;
  payoff: bigint;
  delayHours: number;
};

export type RegisterPoolInstructionDataArgs = {
  flightId: string;
  date: number | bigint;
  premium: number | bigint;
  payoff: number | bigint;
  delayHours: number;
};

export function getRegisterPoolInstructionDataEncoder(): Encoder<RegisterPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
      ["premium", getU64Encoder()],
      ["payoff", getU64Encoder()],
      ["delayHours", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REGISTER_POOL_DISCRIMINATOR }),
  );
}

export function getRegisterPoolInstructionDataDecoder(): Decoder<RegisterPoolInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
    ["premium", getU64Decoder()],
    ["payoff", getU64Decoder()],
    ["delayHours", getU32Decoder()],
  ]);
}

export function getRegisterPoolInstructionDataCodec(): Codec<
  RegisterPoolInstructionDataArgs,
  RegisterPoolInstructionData
> {
  return combineCodec(
    getRegisterPoolInstructionDataEncoder(),
    getRegisterPoolInstructionDataDecoder(),
  );
}

export type RegisterPoolAsyncInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountController extends string = string,
  TAccountRentPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  pool?: Address<TAccountPool>;
  /**
   * Authority. In production this is the controller_program's
   * `ControllerConfig` PDA (signed via `invoke_signed`). In Phase 3 unit
   * tests it's just a regular keypair set by `set_controller`.
   */
  controller: TransactionSigner<TAccountController>;
  /**
   * Rent-payer for the new `FlightPool` PDA. Must be a system-owned
   * signer (a PDA cannot be a system_program::create_account payer).
   * In production this is the traveler making the first-buy. In Phase 3
   * unit tests it can be the same keypair as `controller`.
   * See Phase 5 D18.
   */
  rentPayer: TransactionSigner<TAccountRentPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: RegisterPoolInstructionDataArgs["flightId"];
  date: RegisterPoolInstructionDataArgs["date"];
  premium: RegisterPoolInstructionDataArgs["premium"];
  payoff: RegisterPoolInstructionDataArgs["payoff"];
  delayHours: RegisterPoolInstructionDataArgs["delayHours"];
};

export async function getRegisterPoolInstructionAsync<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountController extends string,
  TAccountRentPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: RegisterPoolAsyncInput<
    TAccountConfig,
    TAccountPool,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RegisterPoolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
    rentPayer: { value: input.rentPayer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      date: getNonNullResolvedInstructionInput("date", args.date),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("rentPayer", accounts.rentPayer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterPoolInstructionDataEncoder().encode(
      args as RegisterPoolInstructionDataArgs,
    ),
    programAddress,
  } as RegisterPoolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >);
}

export type RegisterPoolInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountController extends string = string,
  TAccountRentPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  /**
   * Authority. In production this is the controller_program's
   * `ControllerConfig` PDA (signed via `invoke_signed`). In Phase 3 unit
   * tests it's just a regular keypair set by `set_controller`.
   */
  controller: TransactionSigner<TAccountController>;
  /**
   * Rent-payer for the new `FlightPool` PDA. Must be a system-owned
   * signer (a PDA cannot be a system_program::create_account payer).
   * In production this is the traveler making the first-buy. In Phase 3
   * unit tests it can be the same keypair as `controller`.
   * See Phase 5 D18.
   */
  rentPayer: TransactionSigner<TAccountRentPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: RegisterPoolInstructionDataArgs["flightId"];
  date: RegisterPoolInstructionDataArgs["date"];
  premium: RegisterPoolInstructionDataArgs["premium"];
  payoff: RegisterPoolInstructionDataArgs["payoff"];
  delayHours: RegisterPoolInstructionDataArgs["delayHours"];
};

export function getRegisterPoolInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountController extends string,
  TAccountRentPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: RegisterPoolInput<
    TAccountConfig,
    TAccountPool,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RegisterPoolInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountController,
  TAccountRentPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
    rentPayer: { value: input.rentPayer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("controller", accounts.controller),
      getAccountMeta("rentPayer", accounts.rentPayer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRegisterPoolInstructionDataEncoder().encode(
      args as RegisterPoolInstructionDataArgs,
    ),
    programAddress,
  } as RegisterPoolInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountController,
    TAccountRentPayer,
    TAccountSystemProgram
  >);
}

export type ParsedRegisterPoolInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    /**
     * Authority. In production this is the controller_program's
     * `ControllerConfig` PDA (signed via `invoke_signed`). In Phase 3 unit
     * tests it's just a regular keypair set by `set_controller`.
     */
    controller: TAccountMetas[2];
    /**
     * Rent-payer for the new `FlightPool` PDA. Must be a system-owned
     * signer (a PDA cannot be a system_program::create_account payer).
     * In production this is the traveler making the first-buy. In Phase 3
     * unit tests it can be the same keypair as `controller`.
     * See Phase 5 D18.
     */
    rentPayer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: RegisterPoolInstructionData;
};

export function parseRegisterPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRegisterPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      controller: getNextAccount(),
      rentPayer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRegisterPoolInstructionDataDecoder().decode(instruction.data),
  };
}
