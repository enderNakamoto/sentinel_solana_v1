/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPoolPda } from "../pdas";
import { FLIGHT_POOL_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_CANCELLED_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([14, 205, 179, 210, 225, 144, 185, 249]);

export function getSettleCancelledDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SETTLE_CANCELLED_DISCRIMINATOR,
  );
}

export type SettleCancelledInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      ...TRemainingAccounts,
    ]
  >;

export type SettleCancelledInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
  claimExpiry: bigint;
};

export type SettleCancelledInstructionDataArgs = {
  flightId: string;
  date: number | bigint;
  claimExpiry: number | bigint;
};

export function getSettleCancelledInstructionDataEncoder(): Encoder<SettleCancelledInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
      ["claimExpiry", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SETTLE_CANCELLED_DISCRIMINATOR }),
  );
}

export function getSettleCancelledInstructionDataDecoder(): Decoder<SettleCancelledInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
    ["claimExpiry", getI64Decoder()],
  ]);
}

export function getSettleCancelledInstructionDataCodec(): Codec<
  SettleCancelledInstructionDataArgs,
  SettleCancelledInstructionData
> {
  return combineCodec(
    getSettleCancelledInstructionDataEncoder(),
    getSettleCancelledInstructionDataDecoder(),
  );
}

export type SettleCancelledAsyncInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountController extends string = string,
> = {
  config?: Address<TAccountConfig>;
  pool?: Address<TAccountPool>;
  controller: TransactionSigner<TAccountController>;
  flightId: SettleCancelledInstructionDataArgs["flightId"];
  date: SettleCancelledInstructionDataArgs["date"];
  claimExpiry: SettleCancelledInstructionDataArgs["claimExpiry"];
};

export async function getSettleCancelledInstructionAsync<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountController extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: SettleCancelledAsyncInput<
    TAccountConfig,
    TAccountPool,
    TAccountController
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SettleCancelledInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountController
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      date: getNonNullResolvedInstructionInput("date", args.date),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("controller", accounts.controller),
    ],
    data: getSettleCancelledInstructionDataEncoder().encode(
      args as SettleCancelledInstructionDataArgs,
    ),
    programAddress,
  } as SettleCancelledInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountController
  >);
}

export type SettleCancelledInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountController extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  controller: TransactionSigner<TAccountController>;
  flightId: SettleCancelledInstructionDataArgs["flightId"];
  date: SettleCancelledInstructionDataArgs["date"];
  claimExpiry: SettleCancelledInstructionDataArgs["claimExpiry"];
};

export function getSettleCancelledInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountController extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: SettleCancelledInput<TAccountConfig, TAccountPool, TAccountController>,
  config?: { programAddress?: TProgramAddress },
): SettleCancelledInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountController
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("controller", accounts.controller),
    ],
    data: getSettleCancelledInstructionDataEncoder().encode(
      args as SettleCancelledInstructionDataArgs,
    ),
    programAddress,
  } as SettleCancelledInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountController
  >);
}

export type ParsedSettleCancelledInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    controller: TAccountMetas[2];
  };
  data: SettleCancelledInstructionData;
};

export function parseSettleCancelledInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleCancelledInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      controller: getNextAccount(),
    },
    data: getSettleCancelledInstructionDataDecoder().decode(instruction.data),
  };
}
