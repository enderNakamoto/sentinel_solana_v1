/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findPoolPda } from "../pdas";
import { FLIGHT_POOL_PROGRAM_ADDRESS } from "../programs";

export const SWEEP_EXPIRED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  10, 72, 70, 57, 62, 128, 19, 22,
]);

export function getSweepExpiredDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SWEEP_EXPIRED_DISCRIMINATOR,
  );
}

export type SweepExpiredInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountCaller extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      ...TRemainingAccounts,
    ]
  >;

export type SweepExpiredInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
};

export type SweepExpiredInstructionDataArgs = {
  flightId: string;
  date: number | bigint;
};

export function getSweepExpiredInstructionDataEncoder(): Encoder<SweepExpiredInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SWEEP_EXPIRED_DISCRIMINATOR }),
  );
}

export function getSweepExpiredInstructionDataDecoder(): Decoder<SweepExpiredInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
  ]);
}

export function getSweepExpiredInstructionDataCodec(): Codec<
  SweepExpiredInstructionDataArgs,
  SweepExpiredInstructionData
> {
  return combineCodec(
    getSweepExpiredInstructionDataEncoder(),
    getSweepExpiredInstructionDataDecoder(),
  );
}

export type SweepExpiredAsyncInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountCaller extends string = string,
> = {
  config?: Address<TAccountConfig>;
  pool?: Address<TAccountPool>;
  caller: TransactionSigner<TAccountCaller>;
  flightId: SweepExpiredInstructionDataArgs["flightId"];
  date: SweepExpiredInstructionDataArgs["date"];
};

export async function getSweepExpiredInstructionAsync<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: SweepExpiredAsyncInput<TAccountConfig, TAccountPool, TAccountCaller>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SweepExpiredInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountCaller
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    caller: { value: input.caller ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      date: getNonNullResolvedInstructionInput("date", args.date),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getSweepExpiredInstructionDataEncoder().encode(
      args as SweepExpiredInstructionDataArgs,
    ),
    programAddress,
  } as SweepExpiredInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountCaller
  >);
}

export type SweepExpiredInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountCaller extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  caller: TransactionSigner<TAccountCaller>;
  flightId: SweepExpiredInstructionDataArgs["flightId"];
  date: SweepExpiredInstructionDataArgs["date"];
};

export function getSweepExpiredInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountCaller extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: SweepExpiredInput<TAccountConfig, TAccountPool, TAccountCaller>,
  config?: { programAddress?: TProgramAddress },
): SweepExpiredInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountCaller
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    pool: { value: input.pool ?? null, isWritable: true },
    caller: { value: input.caller ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("caller", accounts.caller),
    ],
    data: getSweepExpiredInstructionDataEncoder().encode(
      args as SweepExpiredInstructionDataArgs,
    ),
    programAddress,
  } as SweepExpiredInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountCaller
  >);
}

export type ParsedSweepExpiredInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    caller: TAccountMetas[2];
  };
  data: SweepExpiredInstructionData;
};

export function parseSweepExpiredInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSweepExpiredInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      caller: getNextAccount(),
    },
    data: getSweepExpiredInstructionDataDecoder().decode(instruction.data),
  };
}
