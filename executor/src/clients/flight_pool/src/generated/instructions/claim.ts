/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findClaimBuyerRecordPda,
  findConfigPda,
  findPoolPda,
  findTreasuryAuthorityPda,
} from "../pdas";
import { FLIGHT_POOL_PROGRAM_ADDRESS } from "../programs";

export const CLAIM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  62, 198, 214, 193, 213, 159, 108, 210,
]);

export function getClaimDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLAIM_DISCRIMINATOR);
}

export type ClaimInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountPool extends string | AccountMeta<string> = string,
  TAccountBuyerRecord extends string | AccountMeta<string> = string,
  TAccountBuyer extends string | AccountMeta<string> = string,
  TAccountPoolTreasury extends string | AccountMeta<string> = string,
  TAccountTreasuryAuthority extends string | AccountMeta<string> = string,
  TAccountTravelerStableAccount extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountTraveler extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountPool extends string
        ? WritableAccount<TAccountPool>
        : TAccountPool,
      TAccountBuyerRecord extends string
        ? WritableAccount<TAccountBuyerRecord>
        : TAccountBuyerRecord,
      TAccountBuyer extends string
        ? ReadonlyAccount<TAccountBuyer>
        : TAccountBuyer,
      TAccountPoolTreasury extends string
        ? WritableAccount<TAccountPoolTreasury>
        : TAccountPoolTreasury,
      TAccountTreasuryAuthority extends string
        ? ReadonlyAccount<TAccountTreasuryAuthority>
        : TAccountTreasuryAuthority,
      TAccountTravelerStableAccount extends string
        ? WritableAccount<TAccountTravelerStableAccount>
        : TAccountTravelerStableAccount,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountTraveler extends string
        ? WritableSignerAccount<TAccountTraveler> &
            AccountSignerMeta<TAccountTraveler>
        : TAccountTraveler,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ClaimInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
};

export type ClaimInstructionDataArgs = {
  flightId: string;
  date: number | bigint;
};

export function getClaimInstructionDataEncoder(): Encoder<ClaimInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CLAIM_DISCRIMINATOR }),
  );
}

export function getClaimInstructionDataDecoder(): Decoder<ClaimInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
  ]);
}

export function getClaimInstructionDataCodec(): Codec<
  ClaimInstructionDataArgs,
  ClaimInstructionData
> {
  return combineCodec(
    getClaimInstructionDataEncoder(),
    getClaimInstructionDataDecoder(),
  );
}

export type ClaimAsyncInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountBuyerRecord extends string = string,
  TAccountBuyer extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountTreasuryAuthority extends string = string,
  TAccountTravelerStableAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountTraveler extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  pool?: Address<TAccountPool>;
  buyerRecord?: Address<TAccountBuyerRecord>;
  buyer: Address<TAccountBuyer>;
  poolTreasury: Address<TAccountPoolTreasury>;
  treasuryAuthority?: Address<TAccountTreasuryAuthority>;
  /**
   * Strict `ATA(traveler, stable_mint)` (D8). `associated_token::
   * token_program` is explicit so the ATA derivation routes through
   * Token-2022 (matches how the client-side packed the ATA).
   */
  travelerStableAccount?: Address<TAccountTravelerStableAccount>;
  stableMint: Address<TAccountStableMint>;
  traveler: TransactionSigner<TAccountTraveler>;
  tokenProgram?: Address<TAccountTokenProgram>;
  flightId: ClaimInstructionDataArgs["flightId"];
  date: ClaimInstructionDataArgs["date"];
};

export async function getClaimInstructionAsync<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountBuyerRecord extends string,
  TAccountBuyer extends string,
  TAccountPoolTreasury extends string,
  TAccountTreasuryAuthority extends string,
  TAccountTravelerStableAccount extends string,
  TAccountStableMint extends string,
  TAccountTraveler extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: ClaimAsyncInput<
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyer,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountTravelerStableAccount,
    TAccountStableMint,
    TAccountTraveler,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ClaimInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyer,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountTravelerStableAccount,
    TAccountStableMint,
    TAccountTraveler,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    buyerRecord: { value: input.buyerRecord ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: false },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    treasuryAuthority: {
      value: input.treasuryAuthority ?? null,
      isWritable: false,
    },
    travelerStableAccount: {
      value: input.travelerStableAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    traveler: { value: input.traveler ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.pool.value) {
    accounts.pool.value = await findPoolPda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      date: getNonNullResolvedInstructionInput("date", args.date),
    });
  }
  if (!accounts.buyerRecord.value) {
    accounts.buyerRecord.value = await findClaimBuyerRecordPda({
      pool: getAddressFromResolvedInstructionAccount(
        "pool",
        accounts.pool.value,
      ),
      traveler: getAddressFromResolvedInstructionAccount(
        "traveler",
        accounts.traveler.value,
      ),
    });
  }
  if (!accounts.treasuryAuthority.value) {
    accounts.treasuryAuthority.value = await findTreasuryAuthorityPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.travelerStableAccount.value) {
    accounts.travelerStableAccount.value = await getProgramDerivedAddress({
      programAddress:
        "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL" as Address<"ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL">,
      seeds: [
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "traveler",
            accounts.traveler.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "tokenProgram",
            accounts.tokenProgram.value,
          ),
        ),
        getAddressEncoder().encode(
          getAddressFromResolvedInstructionAccount(
            "stableMint",
            accounts.stableMint.value,
          ),
        ),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("buyerRecord", accounts.buyerRecord),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("treasuryAuthority", accounts.treasuryAuthority),
      getAccountMeta("travelerStableAccount", accounts.travelerStableAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("traveler", accounts.traveler),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getClaimInstructionDataEncoder().encode(
      args as ClaimInstructionDataArgs,
    ),
    programAddress,
  } as ClaimInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyer,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountTravelerStableAccount,
    TAccountStableMint,
    TAccountTraveler,
    TAccountTokenProgram
  >);
}

export type ClaimInput<
  TAccountConfig extends string = string,
  TAccountPool extends string = string,
  TAccountBuyerRecord extends string = string,
  TAccountBuyer extends string = string,
  TAccountPoolTreasury extends string = string,
  TAccountTreasuryAuthority extends string = string,
  TAccountTravelerStableAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountTraveler extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  pool: Address<TAccountPool>;
  buyerRecord: Address<TAccountBuyerRecord>;
  buyer: Address<TAccountBuyer>;
  poolTreasury: Address<TAccountPoolTreasury>;
  treasuryAuthority: Address<TAccountTreasuryAuthority>;
  /**
   * Strict `ATA(traveler, stable_mint)` (D8). `associated_token::
   * token_program` is explicit so the ATA derivation routes through
   * Token-2022 (matches how the client-side packed the ATA).
   */
  travelerStableAccount: Address<TAccountTravelerStableAccount>;
  stableMint: Address<TAccountStableMint>;
  traveler: TransactionSigner<TAccountTraveler>;
  tokenProgram?: Address<TAccountTokenProgram>;
  flightId: ClaimInstructionDataArgs["flightId"];
  date: ClaimInstructionDataArgs["date"];
};

export function getClaimInstruction<
  TAccountConfig extends string,
  TAccountPool extends string,
  TAccountBuyerRecord extends string,
  TAccountBuyer extends string,
  TAccountPoolTreasury extends string,
  TAccountTreasuryAuthority extends string,
  TAccountTravelerStableAccount extends string,
  TAccountStableMint extends string,
  TAccountTraveler extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
>(
  input: ClaimInput<
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyer,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountTravelerStableAccount,
    TAccountStableMint,
    TAccountTraveler,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ClaimInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountPool,
  TAccountBuyerRecord,
  TAccountBuyer,
  TAccountPoolTreasury,
  TAccountTreasuryAuthority,
  TAccountTravelerStableAccount,
  TAccountStableMint,
  TAccountTraveler,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? FLIGHT_POOL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    pool: { value: input.pool ?? null, isWritable: true },
    buyerRecord: { value: input.buyerRecord ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: false },
    poolTreasury: { value: input.poolTreasury ?? null, isWritable: true },
    treasuryAuthority: {
      value: input.treasuryAuthority ?? null,
      isWritable: false,
    },
    travelerStableAccount: {
      value: input.travelerStableAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    traveler: { value: input.traveler ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("pool", accounts.pool),
      getAccountMeta("buyerRecord", accounts.buyerRecord),
      getAccountMeta("buyer", accounts.buyer),
      getAccountMeta("poolTreasury", accounts.poolTreasury),
      getAccountMeta("treasuryAuthority", accounts.treasuryAuthority),
      getAccountMeta("travelerStableAccount", accounts.travelerStableAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("traveler", accounts.traveler),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
    ],
    data: getClaimInstructionDataEncoder().encode(
      args as ClaimInstructionDataArgs,
    ),
    programAddress,
  } as ClaimInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountPool,
    TAccountBuyerRecord,
    TAccountBuyer,
    TAccountPoolTreasury,
    TAccountTreasuryAuthority,
    TAccountTravelerStableAccount,
    TAccountStableMint,
    TAccountTraveler,
    TAccountTokenProgram
  >);
}

export type ParsedClaimInstruction<
  TProgram extends string = typeof FLIGHT_POOL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    pool: TAccountMetas[1];
    buyerRecord: TAccountMetas[2];
    buyer: TAccountMetas[3];
    poolTreasury: TAccountMetas[4];
    treasuryAuthority: TAccountMetas[5];
    /**
     * Strict `ATA(traveler, stable_mint)` (D8). `associated_token::
     * token_program` is explicit so the ATA derivation routes through
     * Token-2022 (matches how the client-side packed the ATA).
     */
    travelerStableAccount: TAccountMetas[6];
    stableMint: TAccountMetas[7];
    traveler: TAccountMetas[8];
    tokenProgram: TAccountMetas[9];
  };
  data: ClaimInstructionData;
};

export function parseClaimInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedClaimInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 10) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 10,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      pool: getNextAccount(),
      buyerRecord: getNextAccount(),
      buyer: getNextAccount(),
      poolTreasury: getNextAccount(),
      treasuryAuthority: getNextAccount(),
      travelerStableAccount: getNextAccount(),
      stableMint: getNextAccount(),
      traveler: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getClaimInstructionDataDecoder().decode(instruction.data),
  };
}
