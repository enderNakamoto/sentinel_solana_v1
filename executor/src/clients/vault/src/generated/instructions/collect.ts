/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findClaimablePda, findVaultStatePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const COLLECT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  208, 47, 194, 155, 17, 98, 82, 236,
]);

export function getCollectDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(COLLECT_DISCRIMINATOR);
}

export type CollectInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountClaimable extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountCollectorStableAccount extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountCollector extends string | AccountMeta<string> = string,
  TAccountStableTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? ReadonlyAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountClaimable extends string
        ? WritableAccount<TAccountClaimable>
        : TAccountClaimable,
      TAccountOwner extends string
        ? ReadonlyAccount<TAccountOwner>
        : TAccountOwner,
      TAccountCollectorStableAccount extends string
        ? WritableAccount<TAccountCollectorStableAccount>
        : TAccountCollectorStableAccount,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountCollector extends string
        ? WritableSignerAccount<TAccountCollector> &
            AccountSignerMeta<TAccountCollector>
        : TAccountCollector,
      TAccountStableTokenProgram extends string
        ? ReadonlyAccount<TAccountStableTokenProgram>
        : TAccountStableTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CollectInstructionData = { discriminator: ReadonlyUint8Array };

export type CollectInstructionDataArgs = {};

export function getCollectInstructionDataEncoder(): FixedSizeEncoder<CollectInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: COLLECT_DISCRIMINATOR }),
  );
}

export function getCollectInstructionDataDecoder(): FixedSizeDecoder<CollectInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCollectInstructionDataCodec(): FixedSizeCodec<
  CollectInstructionDataArgs,
  CollectInstructionData
> {
  return combineCodec(
    getCollectInstructionDataEncoder(),
    getCollectInstructionDataDecoder(),
  );
}

export type CollectAsyncInput<
  TAccountVaultState extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountClaimable extends string = string,
  TAccountOwner extends string = string,
  TAccountCollectorStableAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountCollector extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  claimable?: Address<TAccountClaimable>;
  /** match the signer. */
  owner: Address<TAccountOwner>;
  collectorStableAccount: Address<TAccountCollectorStableAccount>;
  /** Stable mint — required as a `TransferChecked` account. */
  stableMint: Address<TAccountStableMint>;
  collector: TransactionSigner<TAccountCollector>;
  /**
   * Stable token program — for the stable-coin `TransferChecked` CPI.
   * No classic-SPL CPIs happen here (no share mint burn/mint), so the
   * classic `token_program` field is omitted entirely.
   */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
};

export async function getCollectInstructionAsync<
  TAccountVaultState extends string,
  TAccountVaultTokenAccount extends string,
  TAccountClaimable extends string,
  TAccountOwner extends string,
  TAccountCollectorStableAccount extends string,
  TAccountStableMint extends string,
  TAccountCollector extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CollectAsyncInput<
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountClaimable,
    TAccountOwner,
    TAccountCollectorStableAccount,
    TAccountStableMint,
    TAccountCollector,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CollectInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountClaimable,
    TAccountOwner,
    TAccountCollectorStableAccount,
    TAccountStableMint,
    TAccountCollector,
    TAccountStableTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: false },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    claimable: { value: input.claimable ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    collectorStableAccount: {
      value: input.collectorStableAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    collector: { value: input.collector ?? null, isWritable: true },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }
  if (!accounts.claimable.value) {
    accounts.claimable.value = await findClaimablePda({
      collector: getAddressFromResolvedInstructionAccount(
        "collector",
        accounts.collector.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("claimable", accounts.claimable),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("collectorStableAccount", accounts.collectorStableAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("collector", accounts.collector),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getCollectInstructionDataEncoder().encode({}),
    programAddress,
  } as CollectInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountClaimable,
    TAccountOwner,
    TAccountCollectorStableAccount,
    TAccountStableMint,
    TAccountCollector,
    TAccountStableTokenProgram
  >);
}

export type CollectInput<
  TAccountVaultState extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountClaimable extends string = string,
  TAccountOwner extends string = string,
  TAccountCollectorStableAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountCollector extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  claimable: Address<TAccountClaimable>;
  /** match the signer. */
  owner: Address<TAccountOwner>;
  collectorStableAccount: Address<TAccountCollectorStableAccount>;
  /** Stable mint — required as a `TransferChecked` account. */
  stableMint: Address<TAccountStableMint>;
  collector: TransactionSigner<TAccountCollector>;
  /**
   * Stable token program — for the stable-coin `TransferChecked` CPI.
   * No classic-SPL CPIs happen here (no share mint burn/mint), so the
   * classic `token_program` field is omitted entirely.
   */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
};

export function getCollectInstruction<
  TAccountVaultState extends string,
  TAccountVaultTokenAccount extends string,
  TAccountClaimable extends string,
  TAccountOwner extends string,
  TAccountCollectorStableAccount extends string,
  TAccountStableMint extends string,
  TAccountCollector extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: CollectInput<
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountClaimable,
    TAccountOwner,
    TAccountCollectorStableAccount,
    TAccountStableMint,
    TAccountCollector,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CollectInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountVaultTokenAccount,
  TAccountClaimable,
  TAccountOwner,
  TAccountCollectorStableAccount,
  TAccountStableMint,
  TAccountCollector,
  TAccountStableTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: false },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    claimable: { value: input.claimable ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
    collectorStableAccount: {
      value: input.collectorStableAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    collector: { value: input.collector ?? null, isWritable: true },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("claimable", accounts.claimable),
      getAccountMeta("owner", accounts.owner),
      getAccountMeta("collectorStableAccount", accounts.collectorStableAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("collector", accounts.collector),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getCollectInstructionDataEncoder().encode({}),
    programAddress,
  } as CollectInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountVaultTokenAccount,
    TAccountClaimable,
    TAccountOwner,
    TAccountCollectorStableAccount,
    TAccountStableMint,
    TAccountCollector,
    TAccountStableTokenProgram
  >);
}

export type ParsedCollectInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    vaultTokenAccount: TAccountMetas[1];
    claimable: TAccountMetas[2];
    /** match the signer. */
    owner: TAccountMetas[3];
    collectorStableAccount: TAccountMetas[4];
    /** Stable mint — required as a `TransferChecked` account. */
    stableMint: TAccountMetas[5];
    collector: TAccountMetas[6];
    /**
     * Stable token program — for the stable-coin `TransferChecked` CPI.
     * No classic-SPL CPIs happen here (no share mint burn/mint), so the
     * classic `token_program` field is omitted entirely.
     */
    stableTokenProgram: TAccountMetas[7];
  };
  data: CollectInstructionData;
};

export function parseCollectInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCollectInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      claimable: getNextAccount(),
      owner: getNextAccount(),
      collectorStableAccount: getNextAccount(),
      stableMint: getNextAccount(),
      collector: getNextAccount(),
      stableTokenProgram: getNextAccount(),
    },
    data: getCollectInstructionDataDecoder().decode(instruction.data),
  };
}
