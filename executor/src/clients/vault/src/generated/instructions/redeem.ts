/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findShareMintPda, findVaultStatePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const REDEEM_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  184, 12, 86, 149, 70, 196, 97, 225,
]);

export function getRedeemDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REDEEM_DISCRIMINATOR);
}

export type RedeemInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountShareMint extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountRedeemerShareAccount extends string | AccountMeta<string> = string,
  TAccountRedeemerStableAccount extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountRedeemer extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountStableTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountShareMint extends string
        ? WritableAccount<TAccountShareMint>
        : TAccountShareMint,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountRedeemerShareAccount extends string
        ? WritableAccount<TAccountRedeemerShareAccount>
        : TAccountRedeemerShareAccount,
      TAccountRedeemerStableAccount extends string
        ? WritableAccount<TAccountRedeemerStableAccount>
        : TAccountRedeemerStableAccount,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountRedeemer extends string
        ? WritableSignerAccount<TAccountRedeemer> &
            AccountSignerMeta<TAccountRedeemer>
        : TAccountRedeemer,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountStableTokenProgram extends string
        ? ReadonlyAccount<TAccountStableTokenProgram>
        : TAccountStableTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RedeemInstructionData = {
  discriminator: ReadonlyUint8Array;
  shares: bigint;
};

export type RedeemInstructionDataArgs = { shares: number | bigint };

export function getRedeemInstructionDataEncoder(): FixedSizeEncoder<RedeemInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shares", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REDEEM_DISCRIMINATOR }),
  );
}

export function getRedeemInstructionDataDecoder(): FixedSizeDecoder<RedeemInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shares", getU64Decoder()],
  ]);
}

export function getRedeemInstructionDataCodec(): FixedSizeCodec<
  RedeemInstructionDataArgs,
  RedeemInstructionData
> {
  return combineCodec(
    getRedeemInstructionDataEncoder(),
    getRedeemInstructionDataDecoder(),
  );
}

export type RedeemAsyncInput<
  TAccountVaultState extends string = string,
  TAccountShareMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountRedeemerShareAccount extends string = string,
  TAccountRedeemerStableAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountRedeemer extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  shareMint?: Address<TAccountShareMint>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  redeemerShareAccount: Address<TAccountRedeemerShareAccount>;
  redeemerStableAccount: Address<TAccountRedeemerStableAccount>;
  /** Stable mint — required as a `TransferChecked` account. */
  stableMint: Address<TAccountStableMint>;
  redeemer: TransactionSigner<TAccountRedeemer>;
  /** Classic SPL Token program — for the RVS share-mint `Burn` CPI. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** Stable token program — for the stable-coin `TransferChecked` CPI. */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  shares: RedeemInstructionDataArgs["shares"];
};

export async function getRedeemInstructionAsync<
  TAccountVaultState extends string,
  TAccountShareMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountRedeemerShareAccount extends string,
  TAccountRedeemerStableAccount extends string,
  TAccountStableMint extends string,
  TAccountRedeemer extends string,
  TAccountTokenProgram extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: RedeemAsyncInput<
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountRedeemerShareAccount,
    TAccountRedeemerStableAccount,
    TAccountStableMint,
    TAccountRedeemer,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RedeemInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountRedeemerShareAccount,
    TAccountRedeemerStableAccount,
    TAccountStableMint,
    TAccountRedeemer,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    redeemerShareAccount: {
      value: input.redeemerShareAccount ?? null,
      isWritable: true,
    },
    redeemerStableAccount: {
      value: input.redeemerStableAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    redeemer: { value: input.redeemer ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }
  if (!accounts.shareMint.value) {
    accounts.shareMint.value = await findShareMintPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("redeemerShareAccount", accounts.redeemerShareAccount),
      getAccountMeta("redeemerStableAccount", accounts.redeemerStableAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("redeemer", accounts.redeemer),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getRedeemInstructionDataEncoder().encode(
      args as RedeemInstructionDataArgs,
    ),
    programAddress,
  } as RedeemInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountRedeemerShareAccount,
    TAccountRedeemerStableAccount,
    TAccountStableMint,
    TAccountRedeemer,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >);
}

export type RedeemInput<
  TAccountVaultState extends string = string,
  TAccountShareMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountRedeemerShareAccount extends string = string,
  TAccountRedeemerStableAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountRedeemer extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  shareMint: Address<TAccountShareMint>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  redeemerShareAccount: Address<TAccountRedeemerShareAccount>;
  redeemerStableAccount: Address<TAccountRedeemerStableAccount>;
  /** Stable mint — required as a `TransferChecked` account. */
  stableMint: Address<TAccountStableMint>;
  redeemer: TransactionSigner<TAccountRedeemer>;
  /** Classic SPL Token program — for the RVS share-mint `Burn` CPI. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** Stable token program — for the stable-coin `TransferChecked` CPI. */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  shares: RedeemInstructionDataArgs["shares"];
};

export function getRedeemInstruction<
  TAccountVaultState extends string,
  TAccountShareMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountRedeemerShareAccount extends string,
  TAccountRedeemerStableAccount extends string,
  TAccountStableMint extends string,
  TAccountRedeemer extends string,
  TAccountTokenProgram extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: RedeemInput<
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountRedeemerShareAccount,
    TAccountRedeemerStableAccount,
    TAccountStableMint,
    TAccountRedeemer,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RedeemInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountShareMint,
  TAccountVaultTokenAccount,
  TAccountRedeemerShareAccount,
  TAccountRedeemerStableAccount,
  TAccountStableMint,
  TAccountRedeemer,
  TAccountTokenProgram,
  TAccountStableTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    redeemerShareAccount: {
      value: input.redeemerShareAccount ?? null,
      isWritable: true,
    },
    redeemerStableAccount: {
      value: input.redeemerStableAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    redeemer: { value: input.redeemer ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("redeemerShareAccount", accounts.redeemerShareAccount),
      getAccountMeta("redeemerStableAccount", accounts.redeemerStableAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("redeemer", accounts.redeemer),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getRedeemInstructionDataEncoder().encode(
      args as RedeemInstructionDataArgs,
    ),
    programAddress,
  } as RedeemInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountRedeemerShareAccount,
    TAccountRedeemerStableAccount,
    TAccountStableMint,
    TAccountRedeemer,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >);
}

export type ParsedRedeemInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    shareMint: TAccountMetas[1];
    vaultTokenAccount: TAccountMetas[2];
    redeemerShareAccount: TAccountMetas[3];
    redeemerStableAccount: TAccountMetas[4];
    /** Stable mint — required as a `TransferChecked` account. */
    stableMint: TAccountMetas[5];
    redeemer: TAccountMetas[6];
    /** Classic SPL Token program — for the RVS share-mint `Burn` CPI. */
    tokenProgram: TAccountMetas[7];
    /** Stable token program — for the stable-coin `TransferChecked` CPI. */
    stableTokenProgram: TAccountMetas[8];
  };
  data: RedeemInstructionData;
};

export function parseRedeemInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRedeemInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      shareMint: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      redeemerShareAccount: getNextAccount(),
      redeemerStableAccount: getNextAccount(),
      stableMint: getNextAccount(),
      redeemer: getNextAccount(),
      tokenProgram: getNextAccount(),
      stableTokenProgram: getNextAccount(),
    },
    data: getRedeemInstructionDataDecoder().decode(instruction.data),
  };
}
