/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultStatePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const SET_CONTROLLER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  137, 23, 18, 149, 54, 29, 195, 165,
]);

export function getSetControllerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_CONTROLLER_DISCRIMINATOR,
  );
}

export type SetControllerInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type SetControllerInstructionData = {
  discriminator: ReadonlyUint8Array;
  controller: Address;
};

export type SetControllerInstructionDataArgs = { controller: Address };

export function getSetControllerInstructionDataEncoder(): FixedSizeEncoder<SetControllerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["controller", getAddressEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_CONTROLLER_DISCRIMINATOR }),
  );
}

export function getSetControllerInstructionDataDecoder(): FixedSizeDecoder<SetControllerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["controller", getAddressDecoder()],
  ]);
}

export function getSetControllerInstructionDataCodec(): FixedSizeCodec<
  SetControllerInstructionDataArgs,
  SetControllerInstructionData
> {
  return combineCodec(
    getSetControllerInstructionDataEncoder(),
    getSetControllerInstructionDataDecoder(),
  );
}

export type SetControllerAsyncInput<
  TAccountVaultState extends string = string,
  TAccountOwner extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  owner: TransactionSigner<TAccountOwner>;
  controller: SetControllerInstructionDataArgs["controller"];
};

export async function getSetControllerInstructionAsync<
  TAccountVaultState extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SetControllerAsyncInput<TAccountVaultState, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetControllerInstruction<TProgramAddress, TAccountVaultState, TAccountOwner>
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetControllerInstructionDataEncoder().encode(
      args as SetControllerInstructionDataArgs,
    ),
    programAddress,
  } as SetControllerInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountOwner
  >);
}

export type SetControllerInput<
  TAccountVaultState extends string = string,
  TAccountOwner extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  owner: TransactionSigner<TAccountOwner>;
  controller: SetControllerInstructionDataArgs["controller"];
};

export function getSetControllerInstruction<
  TAccountVaultState extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: SetControllerInput<TAccountVaultState, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): SetControllerInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountOwner
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetControllerInstructionDataEncoder().encode(
      args as SetControllerInstructionDataArgs,
    ),
    programAddress,
  } as SetControllerInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountOwner
  >);
}

export type ParsedSetControllerInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: SetControllerInstructionData;
};

export function parseSetControllerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetControllerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vaultState: getNextAccount(), owner: getNextAccount() },
    data: getSetControllerInstructionDataDecoder().decode(instruction.data),
  };
}
