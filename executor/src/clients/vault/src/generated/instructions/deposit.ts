/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findShareMintPda, findVaultStatePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const DEPOSIT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  242, 35, 198, 137, 82, 225, 242, 182,
]);

export function getDepositDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DEPOSIT_DISCRIMINATOR);
}

export type DepositInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountShareMint extends string | AccountMeta<string> = string,
  TAccountVaultTokenAccount extends string | AccountMeta<string> = string,
  TAccountDepositorStableAccount extends string | AccountMeta<string> = string,
  TAccountDepositorShareAccount extends string | AccountMeta<string> = string,
  TAccountStableMint extends string | AccountMeta<string> = string,
  TAccountDepositor extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountStableTokenProgram extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountShareMint extends string
        ? WritableAccount<TAccountShareMint>
        : TAccountShareMint,
      TAccountVaultTokenAccount extends string
        ? WritableAccount<TAccountVaultTokenAccount>
        : TAccountVaultTokenAccount,
      TAccountDepositorStableAccount extends string
        ? WritableAccount<TAccountDepositorStableAccount>
        : TAccountDepositorStableAccount,
      TAccountDepositorShareAccount extends string
        ? WritableAccount<TAccountDepositorShareAccount>
        : TAccountDepositorShareAccount,
      TAccountStableMint extends string
        ? ReadonlyAccount<TAccountStableMint>
        : TAccountStableMint,
      TAccountDepositor extends string
        ? WritableSignerAccount<TAccountDepositor> &
            AccountSignerMeta<TAccountDepositor>
        : TAccountDepositor,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountStableTokenProgram extends string
        ? ReadonlyAccount<TAccountStableTokenProgram>
        : TAccountStableTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type DepositInstructionData = {
  discriminator: ReadonlyUint8Array;
  stableAmount: bigint;
};

export type DepositInstructionDataArgs = { stableAmount: number | bigint };

export function getDepositInstructionDataEncoder(): FixedSizeEncoder<DepositInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["stableAmount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DEPOSIT_DISCRIMINATOR }),
  );
}

export function getDepositInstructionDataDecoder(): FixedSizeDecoder<DepositInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["stableAmount", getU64Decoder()],
  ]);
}

export function getDepositInstructionDataCodec(): FixedSizeCodec<
  DepositInstructionDataArgs,
  DepositInstructionData
> {
  return combineCodec(
    getDepositInstructionDataEncoder(),
    getDepositInstructionDataDecoder(),
  );
}

export type DepositAsyncInput<
  TAccountVaultState extends string = string,
  TAccountShareMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountDepositorStableAccount extends string = string,
  TAccountDepositorShareAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountDepositor extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  shareMint?: Address<TAccountShareMint>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  /**
   * Depositor's stable-coin ATA. We don't enforce ATA-ness at the Anchor
   * layer (lets tests use synthetic accounts seeded via setAccount); we
   * only require it's a valid TokenAccount of the configured stable mint.
   */
  depositorStableAccount: Address<TAccountDepositorStableAccount>;
  /**
   * Depositor's share-mint ATA. Created by the test harness (or the
   * frontend in production) ahead of time.
   */
  depositorShareAccount: Address<TAccountDepositorShareAccount>;
  /**
   * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
   * account so the token program can verify mint + decimals on every
   * stable transfer (Token-2022 deprecates plain `Transfer`).
   */
  stableMint: Address<TAccountStableMint>;
  depositor: TransactionSigner<TAccountDepositor>;
  /** Classic SPL Token program — for the RVS share-mint `MintTo` CPI. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** Stable token program — for the stable-coin `TransferChecked` CPI. */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  stableAmount: DepositInstructionDataArgs["stableAmount"];
};

export async function getDepositInstructionAsync<
  TAccountVaultState extends string,
  TAccountShareMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountDepositorStableAccount extends string,
  TAccountDepositorShareAccount extends string,
  TAccountStableMint extends string,
  TAccountDepositor extends string,
  TAccountTokenProgram extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DepositAsyncInput<
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountDepositorStableAccount,
    TAccountDepositorShareAccount,
    TAccountStableMint,
    TAccountDepositor,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DepositInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountDepositorStableAccount,
    TAccountDepositorShareAccount,
    TAccountStableMint,
    TAccountDepositor,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    depositorStableAccount: {
      value: input.depositorStableAccount ?? null,
      isWritable: true,
    },
    depositorShareAccount: {
      value: input.depositorShareAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    depositor: { value: input.depositor ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }
  if (!accounts.shareMint.value) {
    accounts.shareMint.value = await findShareMintPda();
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("depositorStableAccount", accounts.depositorStableAccount),
      getAccountMeta("depositorShareAccount", accounts.depositorShareAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("depositor", accounts.depositor),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getDepositInstructionDataEncoder().encode(
      args as DepositInstructionDataArgs,
    ),
    programAddress,
  } as DepositInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountDepositorStableAccount,
    TAccountDepositorShareAccount,
    TAccountStableMint,
    TAccountDepositor,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >);
}

export type DepositInput<
  TAccountVaultState extends string = string,
  TAccountShareMint extends string = string,
  TAccountVaultTokenAccount extends string = string,
  TAccountDepositorStableAccount extends string = string,
  TAccountDepositorShareAccount extends string = string,
  TAccountStableMint extends string = string,
  TAccountDepositor extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountStableTokenProgram extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  shareMint: Address<TAccountShareMint>;
  vaultTokenAccount: Address<TAccountVaultTokenAccount>;
  /**
   * Depositor's stable-coin ATA. We don't enforce ATA-ness at the Anchor
   * layer (lets tests use synthetic accounts seeded via setAccount); we
   * only require it's a valid TokenAccount of the configured stable mint.
   */
  depositorStableAccount: Address<TAccountDepositorStableAccount>;
  /**
   * Depositor's share-mint ATA. Created by the test harness (or the
   * frontend in production) ahead of time.
   */
  depositorShareAccount: Address<TAccountDepositorShareAccount>;
  /**
   * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
   * account so the token program can verify mint + decimals on every
   * stable transfer (Token-2022 deprecates plain `Transfer`).
   */
  stableMint: Address<TAccountStableMint>;
  depositor: TransactionSigner<TAccountDepositor>;
  /** Classic SPL Token program — for the RVS share-mint `MintTo` CPI. */
  tokenProgram?: Address<TAccountTokenProgram>;
  /** Stable token program — for the stable-coin `TransferChecked` CPI. */
  stableTokenProgram: Address<TAccountStableTokenProgram>;
  stableAmount: DepositInstructionDataArgs["stableAmount"];
};

export function getDepositInstruction<
  TAccountVaultState extends string,
  TAccountShareMint extends string,
  TAccountVaultTokenAccount extends string,
  TAccountDepositorStableAccount extends string,
  TAccountDepositorShareAccount extends string,
  TAccountStableMint extends string,
  TAccountDepositor extends string,
  TAccountTokenProgram extends string,
  TAccountStableTokenProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DepositInput<
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountDepositorStableAccount,
    TAccountDepositorShareAccount,
    TAccountStableMint,
    TAccountDepositor,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): DepositInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountShareMint,
  TAccountVaultTokenAccount,
  TAccountDepositorStableAccount,
  TAccountDepositorShareAccount,
  TAccountStableMint,
  TAccountDepositor,
  TAccountTokenProgram,
  TAccountStableTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    vaultTokenAccount: {
      value: input.vaultTokenAccount ?? null,
      isWritable: true,
    },
    depositorStableAccount: {
      value: input.depositorStableAccount ?? null,
      isWritable: true,
    },
    depositorShareAccount: {
      value: input.depositorShareAccount ?? null,
      isWritable: true,
    },
    stableMint: { value: input.stableMint ?? null, isWritable: false },
    depositor: { value: input.depositor ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    stableTokenProgram: {
      value: input.stableTokenProgram ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount),
      getAccountMeta("depositorStableAccount", accounts.depositorStableAccount),
      getAccountMeta("depositorShareAccount", accounts.depositorShareAccount),
      getAccountMeta("stableMint", accounts.stableMint),
      getAccountMeta("depositor", accounts.depositor),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("stableTokenProgram", accounts.stableTokenProgram),
    ],
    data: getDepositInstructionDataEncoder().encode(
      args as DepositInstructionDataArgs,
    ),
    programAddress,
  } as DepositInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountShareMint,
    TAccountVaultTokenAccount,
    TAccountDepositorStableAccount,
    TAccountDepositorShareAccount,
    TAccountStableMint,
    TAccountDepositor,
    TAccountTokenProgram,
    TAccountStableTokenProgram
  >);
}

export type ParsedDepositInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    shareMint: TAccountMetas[1];
    vaultTokenAccount: TAccountMetas[2];
    /**
     * Depositor's stable-coin ATA. We don't enforce ATA-ness at the Anchor
     * layer (lets tests use synthetic accounts seeded via setAccount); we
     * only require it's a valid TokenAccount of the configured stable mint.
     */
    depositorStableAccount: TAccountMetas[3];
    /**
     * Depositor's share-mint ATA. Created by the test harness (or the
     * frontend in production) ahead of time.
     */
    depositorShareAccount: TAccountMetas[4];
    /**
     * Stable mint (PUSD / mock-PUSD). Required as a `TransferChecked`
     * account so the token program can verify mint + decimals on every
     * stable transfer (Token-2022 deprecates plain `Transfer`).
     */
    stableMint: TAccountMetas[5];
    depositor: TAccountMetas[6];
    /** Classic SPL Token program — for the RVS share-mint `MintTo` CPI. */
    tokenProgram: TAccountMetas[7];
    /** Stable token program — for the stable-coin `TransferChecked` CPI. */
    stableTokenProgram: TAccountMetas[8];
  };
  data: DepositInstructionData;
};

export function parseDepositInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDepositInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 9) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 9,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      shareMint: getNextAccount(),
      vaultTokenAccount: getNextAccount(),
      depositorStableAccount: getNextAccount(),
      depositorShareAccount: getNextAccount(),
      stableMint: getNextAccount(),
      depositor: getNextAccount(),
      tokenProgram: getNextAccount(),
      stableTokenProgram: getNextAccount(),
    },
    data: getDepositInstructionDataDecoder().decode(instruction.data),
  };
}
