/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import {
  findRequestWithdrawalClaimablePda,
  findShareMintPda,
  findVaultStatePda,
  findWithdrawalQueuePda,
} from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_WITHDRAWAL_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([251, 85, 121, 205, 56, 201, 12, 177]);

export function getRequestWithdrawalDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REQUEST_WITHDRAWAL_DISCRIMINATOR,
  );
}

export type RequestWithdrawalInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountWithdrawalQueue extends string | AccountMeta<string> = string,
  TAccountShareMint extends string | AccountMeta<string> = string,
  TAccountRequesterShareAccount extends string | AccountMeta<string> = string,
  TAccountClaimable extends string | AccountMeta<string> = string,
  TAccountRequester extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountWithdrawalQueue extends string
        ? WritableAccount<TAccountWithdrawalQueue>
        : TAccountWithdrawalQueue,
      TAccountShareMint extends string
        ? WritableAccount<TAccountShareMint>
        : TAccountShareMint,
      TAccountRequesterShareAccount extends string
        ? WritableAccount<TAccountRequesterShareAccount>
        : TAccountRequesterShareAccount,
      TAccountClaimable extends string
        ? WritableAccount<TAccountClaimable>
        : TAccountClaimable,
      TAccountRequester extends string
        ? WritableSignerAccount<TAccountRequester> &
            AccountSignerMeta<TAccountRequester>
        : TAccountRequester,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RequestWithdrawalInstructionData = {
  discriminator: ReadonlyUint8Array;
  shares: bigint;
};

export type RequestWithdrawalInstructionDataArgs = { shares: number | bigint };

export function getRequestWithdrawalInstructionDataEncoder(): FixedSizeEncoder<RequestWithdrawalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["shares", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: REQUEST_WITHDRAWAL_DISCRIMINATOR }),
  );
}

export function getRequestWithdrawalInstructionDataDecoder(): FixedSizeDecoder<RequestWithdrawalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["shares", getU64Decoder()],
  ]);
}

export function getRequestWithdrawalInstructionDataCodec(): FixedSizeCodec<
  RequestWithdrawalInstructionDataArgs,
  RequestWithdrawalInstructionData
> {
  return combineCodec(
    getRequestWithdrawalInstructionDataEncoder(),
    getRequestWithdrawalInstructionDataDecoder(),
  );
}

export type RequestWithdrawalAsyncInput<
  TAccountVaultState extends string = string,
  TAccountWithdrawalQueue extends string = string,
  TAccountShareMint extends string = string,
  TAccountRequesterShareAccount extends string = string,
  TAccountClaimable extends string = string,
  TAccountRequester extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  withdrawalQueue?: Address<TAccountWithdrawalQueue>;
  shareMint?: Address<TAccountShareMint>;
  requesterShareAccount: Address<TAccountRequesterShareAccount>;
  claimable?: Address<TAccountClaimable>;
  requester: TransactionSigner<TAccountRequester>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  shares: RequestWithdrawalInstructionDataArgs["shares"];
};

export async function getRequestWithdrawalInstructionAsync<
  TAccountVaultState extends string,
  TAccountWithdrawalQueue extends string,
  TAccountShareMint extends string,
  TAccountRequesterShareAccount extends string,
  TAccountClaimable extends string,
  TAccountRequester extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: RequestWithdrawalAsyncInput<
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountClaimable,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RequestWithdrawalInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountClaimable,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    withdrawalQueue: { value: input.withdrawalQueue ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    requesterShareAccount: {
      value: input.requesterShareAccount ?? null,
      isWritable: true,
    },
    claimable: { value: input.claimable ?? null, isWritable: true },
    requester: { value: input.requester ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }
  if (!accounts.withdrawalQueue.value) {
    accounts.withdrawalQueue.value = await findWithdrawalQueuePda();
  }
  if (!accounts.shareMint.value) {
    accounts.shareMint.value = await findShareMintPda();
  }
  if (!accounts.claimable.value) {
    accounts.claimable.value = await findRequestWithdrawalClaimablePda({
      requester: getAddressFromResolvedInstructionAccount(
        "requester",
        accounts.requester.value,
      ),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("withdrawalQueue", accounts.withdrawalQueue),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("requesterShareAccount", accounts.requesterShareAccount),
      getAccountMeta("claimable", accounts.claimable),
      getAccountMeta("requester", accounts.requester),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRequestWithdrawalInstructionDataEncoder().encode(
      args as RequestWithdrawalInstructionDataArgs,
    ),
    programAddress,
  } as RequestWithdrawalInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountClaimable,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type RequestWithdrawalInput<
  TAccountVaultState extends string = string,
  TAccountWithdrawalQueue extends string = string,
  TAccountShareMint extends string = string,
  TAccountRequesterShareAccount extends string = string,
  TAccountClaimable extends string = string,
  TAccountRequester extends string = string,
  TAccountTokenProgram extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  withdrawalQueue: Address<TAccountWithdrawalQueue>;
  shareMint: Address<TAccountShareMint>;
  requesterShareAccount: Address<TAccountRequesterShareAccount>;
  claimable: Address<TAccountClaimable>;
  requester: TransactionSigner<TAccountRequester>;
  tokenProgram?: Address<TAccountTokenProgram>;
  systemProgram?: Address<TAccountSystemProgram>;
  shares: RequestWithdrawalInstructionDataArgs["shares"];
};

export function getRequestWithdrawalInstruction<
  TAccountVaultState extends string,
  TAccountWithdrawalQueue extends string,
  TAccountShareMint extends string,
  TAccountRequesterShareAccount extends string,
  TAccountClaimable extends string,
  TAccountRequester extends string,
  TAccountTokenProgram extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: RequestWithdrawalInput<
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountClaimable,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RequestWithdrawalInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountWithdrawalQueue,
  TAccountShareMint,
  TAccountRequesterShareAccount,
  TAccountClaimable,
  TAccountRequester,
  TAccountTokenProgram,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    withdrawalQueue: { value: input.withdrawalQueue ?? null, isWritable: true },
    shareMint: { value: input.shareMint ?? null, isWritable: true },
    requesterShareAccount: {
      value: input.requesterShareAccount ?? null,
      isWritable: true,
    },
    claimable: { value: input.claimable ?? null, isWritable: true },
    requester: { value: input.requester ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("withdrawalQueue", accounts.withdrawalQueue),
      getAccountMeta("shareMint", accounts.shareMint),
      getAccountMeta("requesterShareAccount", accounts.requesterShareAccount),
      getAccountMeta("claimable", accounts.claimable),
      getAccountMeta("requester", accounts.requester),
      getAccountMeta("tokenProgram", accounts.tokenProgram),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRequestWithdrawalInstructionDataEncoder().encode(
      args as RequestWithdrawalInstructionDataArgs,
    ),
    programAddress,
  } as RequestWithdrawalInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountShareMint,
    TAccountRequesterShareAccount,
    TAccountClaimable,
    TAccountRequester,
    TAccountTokenProgram,
    TAccountSystemProgram
  >);
}

export type ParsedRequestWithdrawalInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    withdrawalQueue: TAccountMetas[1];
    shareMint: TAccountMetas[2];
    requesterShareAccount: TAccountMetas[3];
    claimable: TAccountMetas[4];
    requester: TAccountMetas[5];
    tokenProgram: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: RequestWithdrawalInstructionData;
};

export function parseRequestWithdrawalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestWithdrawalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      withdrawalQueue: getNextAccount(),
      shareMint: getNextAccount(),
      requesterShareAccount: getNextAccount(),
      claimable: getNextAccount(),
      requester: getNextAccount(),
      tokenProgram: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRequestWithdrawalInstructionDataDecoder().decode(instruction.data),
  };
}
