/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultStatePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const DECREASE_LOCKED_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [225, 108, 17, 119, 79, 199, 5, 69],
);

export function getDecreaseLockedDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    DECREASE_LOCKED_DISCRIMINATOR,
  );
}

export type DecreaseLockedInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      ...TRemainingAccounts,
    ]
  >;

export type DecreaseLockedInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type DecreaseLockedInstructionDataArgs = { amount: number | bigint };

export function getDecreaseLockedInstructionDataEncoder(): FixedSizeEncoder<DecreaseLockedInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: DECREASE_LOCKED_DISCRIMINATOR }),
  );
}

export function getDecreaseLockedInstructionDataDecoder(): FixedSizeDecoder<DecreaseLockedInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getDecreaseLockedInstructionDataCodec(): FixedSizeCodec<
  DecreaseLockedInstructionDataArgs,
  DecreaseLockedInstructionData
> {
  return combineCodec(
    getDecreaseLockedInstructionDataEncoder(),
    getDecreaseLockedInstructionDataDecoder(),
  );
}

export type DecreaseLockedAsyncInput<
  TAccountVaultState extends string = string,
  TAccountController extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  controller: TransactionSigner<TAccountController>;
  amount: DecreaseLockedInstructionDataArgs["amount"];
};

export async function getDecreaseLockedInstructionAsync<
  TAccountVaultState extends string,
  TAccountController extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DecreaseLockedAsyncInput<TAccountVaultState, TAccountController>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  DecreaseLockedInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountController
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("controller", accounts.controller),
    ],
    data: getDecreaseLockedInstructionDataEncoder().encode(
      args as DecreaseLockedInstructionDataArgs,
    ),
    programAddress,
  } as DecreaseLockedInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountController
  >);
}

export type DecreaseLockedInput<
  TAccountVaultState extends string = string,
  TAccountController extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  controller: TransactionSigner<TAccountController>;
  amount: DecreaseLockedInstructionDataArgs["amount"];
};

export function getDecreaseLockedInstruction<
  TAccountVaultState extends string,
  TAccountController extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: DecreaseLockedInput<TAccountVaultState, TAccountController>,
  config?: { programAddress?: TProgramAddress },
): DecreaseLockedInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountController
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("controller", accounts.controller),
    ],
    data: getDecreaseLockedInstructionDataEncoder().encode(
      args as DecreaseLockedInstructionDataArgs,
    ),
    programAddress,
  } as DecreaseLockedInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountController
  >);
}

export type ParsedDecreaseLockedInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    controller: TAccountMetas[1];
  };
  data: DecreaseLockedInstructionData;
};

export function parseDecreaseLockedInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedDecreaseLockedInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { vaultState: getNextAccount(), controller: getNextAccount() },
    data: getDecreaseLockedInstructionDataDecoder().decode(instruction.data),
  };
}
