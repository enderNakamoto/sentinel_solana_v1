/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVaultStatePda, findWithdrawalQueuePda } from "../pdas";
import { VAULT_PROGRAM_ADDRESS } from "../programs";

export const PROCESS_WITHDRAWAL_QUEUE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([219, 56, 155, 23, 212, 254, 244, 195]);

export function getProcessWithdrawalQueueDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    PROCESS_WITHDRAWAL_QUEUE_DISCRIMINATOR,
  );
}

export type ProcessWithdrawalQueueInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountVaultState extends string | AccountMeta<string> = string,
  TAccountWithdrawalQueue extends string | AccountMeta<string> = string,
  TAccountController extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountVaultState extends string
        ? WritableAccount<TAccountVaultState>
        : TAccountVaultState,
      TAccountWithdrawalQueue extends string
        ? WritableAccount<TAccountWithdrawalQueue>
        : TAccountWithdrawalQueue,
      TAccountController extends string
        ? ReadonlySignerAccount<TAccountController> &
            AccountSignerMeta<TAccountController>
        : TAccountController,
      ...TRemainingAccounts,
    ]
  >;

export type ProcessWithdrawalQueueInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type ProcessWithdrawalQueueInstructionDataArgs = {};

export function getProcessWithdrawalQueueInstructionDataEncoder(): FixedSizeEncoder<ProcessWithdrawalQueueInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: PROCESS_WITHDRAWAL_QUEUE_DISCRIMINATOR,
    }),
  );
}

export function getProcessWithdrawalQueueInstructionDataDecoder(): FixedSizeDecoder<ProcessWithdrawalQueueInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getProcessWithdrawalQueueInstructionDataCodec(): FixedSizeCodec<
  ProcessWithdrawalQueueInstructionDataArgs,
  ProcessWithdrawalQueueInstructionData
> {
  return combineCodec(
    getProcessWithdrawalQueueInstructionDataEncoder(),
    getProcessWithdrawalQueueInstructionDataDecoder(),
  );
}

export type ProcessWithdrawalQueueAsyncInput<
  TAccountVaultState extends string = string,
  TAccountWithdrawalQueue extends string = string,
  TAccountController extends string = string,
> = {
  vaultState?: Address<TAccountVaultState>;
  withdrawalQueue?: Address<TAccountWithdrawalQueue>;
  controller: TransactionSigner<TAccountController>;
};

export async function getProcessWithdrawalQueueInstructionAsync<
  TAccountVaultState extends string,
  TAccountWithdrawalQueue extends string,
  TAccountController extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: ProcessWithdrawalQueueAsyncInput<
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountController
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ProcessWithdrawalQueueInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountController
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    withdrawalQueue: { value: input.withdrawalQueue ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.vaultState.value) {
    accounts.vaultState.value = await findVaultStatePda();
  }
  if (!accounts.withdrawalQueue.value) {
    accounts.withdrawalQueue.value = await findWithdrawalQueuePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("withdrawalQueue", accounts.withdrawalQueue),
      getAccountMeta("controller", accounts.controller),
    ],
    data: getProcessWithdrawalQueueInstructionDataEncoder().encode({}),
    programAddress,
  } as ProcessWithdrawalQueueInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountController
  >);
}

export type ProcessWithdrawalQueueInput<
  TAccountVaultState extends string = string,
  TAccountWithdrawalQueue extends string = string,
  TAccountController extends string = string,
> = {
  vaultState: Address<TAccountVaultState>;
  withdrawalQueue: Address<TAccountWithdrawalQueue>;
  controller: TransactionSigner<TAccountController>;
};

export function getProcessWithdrawalQueueInstruction<
  TAccountVaultState extends string,
  TAccountWithdrawalQueue extends string,
  TAccountController extends string,
  TProgramAddress extends Address = typeof VAULT_PROGRAM_ADDRESS,
>(
  input: ProcessWithdrawalQueueInput<
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountController
  >,
  config?: { programAddress?: TProgramAddress },
): ProcessWithdrawalQueueInstruction<
  TProgramAddress,
  TAccountVaultState,
  TAccountWithdrawalQueue,
  TAccountController
> {
  // Program address.
  const programAddress = config?.programAddress ?? VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    vaultState: { value: input.vaultState ?? null, isWritable: true },
    withdrawalQueue: { value: input.withdrawalQueue ?? null, isWritable: true },
    controller: { value: input.controller ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("vaultState", accounts.vaultState),
      getAccountMeta("withdrawalQueue", accounts.withdrawalQueue),
      getAccountMeta("controller", accounts.controller),
    ],
    data: getProcessWithdrawalQueueInstructionDataEncoder().encode({}),
    programAddress,
  } as ProcessWithdrawalQueueInstruction<
    TProgramAddress,
    TAccountVaultState,
    TAccountWithdrawalQueue,
    TAccountController
  >);
}

export type ParsedProcessWithdrawalQueueInstruction<
  TProgram extends string = typeof VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    vaultState: TAccountMetas[0];
    withdrawalQueue: TAccountMetas[1];
    controller: TAccountMetas[2];
  };
  data: ProcessWithdrawalQueueInstructionData;
};

export function parseProcessWithdrawalQueueInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedProcessWithdrawalQueueInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      vaultState: getNextAccount(),
      withdrawalQueue: getNextAccount(),
      controller: getNextAccount(),
    },
    data: getProcessWithdrawalQueueInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
