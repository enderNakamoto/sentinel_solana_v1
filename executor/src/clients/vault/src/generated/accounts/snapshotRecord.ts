/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const SNAPSHOT_RECORD_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [224, 169, 148, 20, 79, 195, 32, 106],
);

export function getSnapshotRecordDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SNAPSHOT_RECORD_DISCRIMINATOR,
  );
}

export type SnapshotRecord = {
  discriminator: ReadonlyUint8Array;
  day: bigint;
  sharePrice: bigint;
  bump: number;
};

export type SnapshotRecordArgs = {
  day: number | bigint;
  sharePrice: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link SnapshotRecordArgs} account data. */
export function getSnapshotRecordEncoder(): FixedSizeEncoder<SnapshotRecordArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["day", getU64Encoder()],
      ["sharePrice", getU64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SNAPSHOT_RECORD_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link SnapshotRecord} account data. */
export function getSnapshotRecordDecoder(): FixedSizeDecoder<SnapshotRecord> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["day", getU64Decoder()],
    ["sharePrice", getU64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link SnapshotRecord} account data. */
export function getSnapshotRecordCodec(): FixedSizeCodec<
  SnapshotRecordArgs,
  SnapshotRecord
> {
  return combineCodec(getSnapshotRecordEncoder(), getSnapshotRecordDecoder());
}

export function decodeSnapshotRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<SnapshotRecord, TAddress>;
export function decodeSnapshotRecord<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<SnapshotRecord, TAddress>;
export function decodeSnapshotRecord<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<SnapshotRecord, TAddress> | MaybeAccount<SnapshotRecord, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getSnapshotRecordDecoder(),
  );
}

export async function fetchSnapshotRecord<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<SnapshotRecord, TAddress>> {
  const maybeAccount = await fetchMaybeSnapshotRecord(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeSnapshotRecord<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<SnapshotRecord, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeSnapshotRecord(maybeAccount);
}

export async function fetchAllSnapshotRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<SnapshotRecord>[]> {
  const maybeAccounts = await fetchAllMaybeSnapshotRecord(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeSnapshotRecord(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<SnapshotRecord>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeSnapshotRecord(maybeAccount),
  );
}

export function getSnapshotRecordSize(): number {
  return 25;
}
