/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  extendClient,
  fixEncoderSize,
  getBytesEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
  SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
  SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
  SolanaError,
  type Address,
  type ClientWithRpc,
  type ClientWithTransactionPlanning,
  type ClientWithTransactionSending,
  type GetAccountInfoApi,
  type GetMultipleAccountsApi,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  addSelfFetchFunctions,
  addSelfPlanAndSendFunctions,
  type SelfFetchFunctions,
  type SelfPlanAndSendFunctions,
} from "@solana/program-client-core";
import {
  getClaimableBalanceCodec,
  getSnapshotRecordCodec,
  getVaultStateCodec,
  getWithdrawalQueueCodec,
  type ClaimableBalance,
  type ClaimableBalanceArgs,
  type SnapshotRecord,
  type SnapshotRecordArgs,
  type VaultState,
  type VaultStateArgs,
  type WithdrawalQueue,
  type WithdrawalQueueArgs,
} from "../accounts";
import {
  getCancelWithdrawalInstructionAsync,
  getCollectInstructionAsync,
  getDecreaseLockedInstructionAsync,
  getDepositInstructionAsync,
  getIncreaseLockedInstructionAsync,
  getInitializeInstructionAsync,
  getProcessWithdrawalQueueInstructionAsync,
  getRecordPremiumIncomeInstructionAsync,
  getRedeemInstructionAsync,
  getRequestWithdrawalInstructionAsync,
  getSendPayoutInstructionAsync,
  getSetControllerInstructionAsync,
  getSnapshotInstructionAsync,
  parseCancelWithdrawalInstruction,
  parseCollectInstruction,
  parseDecreaseLockedInstruction,
  parseDepositInstruction,
  parseIncreaseLockedInstruction,
  parseInitializeInstruction,
  parseProcessWithdrawalQueueInstruction,
  parseRecordPremiumIncomeInstruction,
  parseRedeemInstruction,
  parseRequestWithdrawalInstruction,
  parseSendPayoutInstruction,
  parseSetControllerInstruction,
  parseSnapshotInstruction,
  type CancelWithdrawalAsyncInput,
  type CollectAsyncInput,
  type DecreaseLockedAsyncInput,
  type DepositAsyncInput,
  type IncreaseLockedAsyncInput,
  type InitializeAsyncInput,
  type ParsedCancelWithdrawalInstruction,
  type ParsedCollectInstruction,
  type ParsedDecreaseLockedInstruction,
  type ParsedDepositInstruction,
  type ParsedIncreaseLockedInstruction,
  type ParsedInitializeInstruction,
  type ParsedProcessWithdrawalQueueInstruction,
  type ParsedRecordPremiumIncomeInstruction,
  type ParsedRedeemInstruction,
  type ParsedRequestWithdrawalInstruction,
  type ParsedSendPayoutInstruction,
  type ParsedSetControllerInstruction,
  type ParsedSnapshotInstruction,
  type ProcessWithdrawalQueueAsyncInput,
  type RecordPremiumIncomeAsyncInput,
  type RedeemAsyncInput,
  type RequestWithdrawalAsyncInput,
  type SendPayoutAsyncInput,
  type SetControllerAsyncInput,
  type SnapshotAsyncInput,
} from "../instructions";
import {
  findClaimablePda,
  findRequestWithdrawalClaimablePda,
  findShareMintPda,
  findSnapshotRecordPda,
  findVaultStatePda,
  findWithdrawalQueuePda,
} from "../pdas";

export const VAULT_PROGRAM_ADDRESS =
  "3yzuTtfGYUBsdhXf4QGeq9MUGj5RDNLj3hFPtsWGkj8p" as Address<"3yzuTtfGYUBsdhXf4QGeq9MUGj5RDNLj3hFPtsWGkj8p">;

export enum VaultAccount {
  ClaimableBalance,
  SnapshotRecord,
  VaultState,
  WithdrawalQueue,
}

export function identifyVaultAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([211, 2, 251, 123, 91, 61, 146, 116]),
      ),
      0,
    )
  ) {
    return VaultAccount.ClaimableBalance;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([224, 169, 148, 20, 79, 195, 32, 106]),
      ),
      0,
    )
  ) {
    return VaultAccount.SnapshotRecord;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([228, 196, 82, 165, 98, 210, 235, 152]),
      ),
      0,
    )
  ) {
    return VaultAccount.VaultState;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([54, 56, 158, 88, 232, 203, 241, 163]),
      ),
      0,
    )
  ) {
    return VaultAccount.WithdrawalQueue;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_ACCOUNT,
    { accountData: data, programName: "vault" },
  );
}

export enum VaultInstruction {
  CancelWithdrawal,
  Collect,
  DecreaseLocked,
  Deposit,
  IncreaseLocked,
  Initialize,
  ProcessWithdrawalQueue,
  RecordPremiumIncome,
  Redeem,
  RequestWithdrawal,
  SendPayout,
  SetController,
  Snapshot,
}

export function identifyVaultInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): VaultInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([183, 104, 181, 250, 28, 128, 210, 70]),
      ),
      0,
    )
  ) {
    return VaultInstruction.CancelWithdrawal;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([208, 47, 194, 155, 17, 98, 82, 236]),
      ),
      0,
    )
  ) {
    return VaultInstruction.Collect;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([225, 108, 17, 119, 79, 199, 5, 69]),
      ),
      0,
    )
  ) {
    return VaultInstruction.DecreaseLocked;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([242, 35, 198, 137, 82, 225, 242, 182]),
      ),
      0,
    )
  ) {
    return VaultInstruction.Deposit;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([64, 159, 200, 10, 129, 240, 41, 244]),
      ),
      0,
    )
  ) {
    return VaultInstruction.IncreaseLocked;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237]),
      ),
      0,
    )
  ) {
    return VaultInstruction.Initialize;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([219, 56, 155, 23, 212, 254, 244, 195]),
      ),
      0,
    )
  ) {
    return VaultInstruction.ProcessWithdrawalQueue;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([179, 2, 245, 151, 58, 189, 86, 27]),
      ),
      0,
    )
  ) {
    return VaultInstruction.RecordPremiumIncome;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([184, 12, 86, 149, 70, 196, 97, 225]),
      ),
      0,
    )
  ) {
    return VaultInstruction.Redeem;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([251, 85, 121, 205, 56, 201, 12, 177]),
      ),
      0,
    )
  ) {
    return VaultInstruction.RequestWithdrawal;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([113, 130, 45, 158, 96, 145, 152, 235]),
      ),
      0,
    )
  ) {
    return VaultInstruction.SendPayout;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([137, 23, 18, 149, 54, 29, 195, 165]),
      ),
      0,
    )
  ) {
    return VaultInstruction.SetController;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([144, 236, 6, 133, 233, 160, 21, 94]),
      ),
      0,
    )
  ) {
    return VaultInstruction.Snapshot;
  }
  throw new SolanaError(
    SOLANA_ERROR__PROGRAM_CLIENTS__FAILED_TO_IDENTIFY_INSTRUCTION,
    { instructionData: data, programName: "vault" },
  );
}

export type ParsedVaultInstruction<
  TProgram extends string = "3yzuTtfGYUBsdhXf4QGeq9MUGj5RDNLj3hFPtsWGkj8p",
> =
  | ({
      instructionType: VaultInstruction.CancelWithdrawal;
    } & ParsedCancelWithdrawalInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.Collect;
    } & ParsedCollectInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.DecreaseLocked;
    } & ParsedDecreaseLockedInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.Deposit;
    } & ParsedDepositInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.IncreaseLocked;
    } & ParsedIncreaseLockedInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.Initialize;
    } & ParsedInitializeInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.ProcessWithdrawalQueue;
    } & ParsedProcessWithdrawalQueueInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.RecordPremiumIncome;
    } & ParsedRecordPremiumIncomeInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.Redeem;
    } & ParsedRedeemInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.RequestWithdrawal;
    } & ParsedRequestWithdrawalInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.SendPayout;
    } & ParsedSendPayoutInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.SetController;
    } & ParsedSetControllerInstruction<TProgram>)
  | ({
      instructionType: VaultInstruction.Snapshot;
    } & ParsedSnapshotInstruction<TProgram>);

export function parseVaultInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultInstruction<TProgram> {
  const instructionType = identifyVaultInstruction(instruction);
  switch (instructionType) {
    case VaultInstruction.CancelWithdrawal: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.CancelWithdrawal,
        ...parseCancelWithdrawalInstruction(instruction),
      };
    }
    case VaultInstruction.Collect: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.Collect,
        ...parseCollectInstruction(instruction),
      };
    }
    case VaultInstruction.DecreaseLocked: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.DecreaseLocked,
        ...parseDecreaseLockedInstruction(instruction),
      };
    }
    case VaultInstruction.Deposit: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.Deposit,
        ...parseDepositInstruction(instruction),
      };
    }
    case VaultInstruction.IncreaseLocked: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.IncreaseLocked,
        ...parseIncreaseLockedInstruction(instruction),
      };
    }
    case VaultInstruction.Initialize: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.Initialize,
        ...parseInitializeInstruction(instruction),
      };
    }
    case VaultInstruction.ProcessWithdrawalQueue: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.ProcessWithdrawalQueue,
        ...parseProcessWithdrawalQueueInstruction(instruction),
      };
    }
    case VaultInstruction.RecordPremiumIncome: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.RecordPremiumIncome,
        ...parseRecordPremiumIncomeInstruction(instruction),
      };
    }
    case VaultInstruction.Redeem: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.Redeem,
        ...parseRedeemInstruction(instruction),
      };
    }
    case VaultInstruction.RequestWithdrawal: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.RequestWithdrawal,
        ...parseRequestWithdrawalInstruction(instruction),
      };
    }
    case VaultInstruction.SendPayout: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.SendPayout,
        ...parseSendPayoutInstruction(instruction),
      };
    }
    case VaultInstruction.SetController: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.SetController,
        ...parseSetControllerInstruction(instruction),
      };
    }
    case VaultInstruction.Snapshot: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: VaultInstruction.Snapshot,
        ...parseSnapshotInstruction(instruction),
      };
    }
    default:
      throw new SolanaError(
        SOLANA_ERROR__PROGRAM_CLIENTS__UNRECOGNIZED_INSTRUCTION_TYPE,
        { instructionType: instructionType as string, programName: "vault" },
      );
  }
}

export type VaultPlugin = {
  accounts: VaultPluginAccounts;
  instructions: VaultPluginInstructions;
  pdas: VaultPluginPdas;
};

export type VaultPluginAccounts = {
  claimableBalance: ReturnType<typeof getClaimableBalanceCodec> &
    SelfFetchFunctions<ClaimableBalanceArgs, ClaimableBalance>;
  snapshotRecord: ReturnType<typeof getSnapshotRecordCodec> &
    SelfFetchFunctions<SnapshotRecordArgs, SnapshotRecord>;
  vaultState: ReturnType<typeof getVaultStateCodec> &
    SelfFetchFunctions<VaultStateArgs, VaultState>;
  withdrawalQueue: ReturnType<typeof getWithdrawalQueueCodec> &
    SelfFetchFunctions<WithdrawalQueueArgs, WithdrawalQueue>;
};

export type VaultPluginInstructions = {
  cancelWithdrawal: (
    input: CancelWithdrawalAsyncInput,
  ) => ReturnType<typeof getCancelWithdrawalInstructionAsync> &
    SelfPlanAndSendFunctions;
  collect: (
    input: CollectAsyncInput,
  ) => ReturnType<typeof getCollectInstructionAsync> & SelfPlanAndSendFunctions;
  decreaseLocked: (
    input: DecreaseLockedAsyncInput,
  ) => ReturnType<typeof getDecreaseLockedInstructionAsync> &
    SelfPlanAndSendFunctions;
  deposit: (
    input: DepositAsyncInput,
  ) => ReturnType<typeof getDepositInstructionAsync> & SelfPlanAndSendFunctions;
  increaseLocked: (
    input: IncreaseLockedAsyncInput,
  ) => ReturnType<typeof getIncreaseLockedInstructionAsync> &
    SelfPlanAndSendFunctions;
  initialize: (
    input: InitializeAsyncInput,
  ) => ReturnType<typeof getInitializeInstructionAsync> &
    SelfPlanAndSendFunctions;
  processWithdrawalQueue: (
    input: ProcessWithdrawalQueueAsyncInput,
  ) => ReturnType<typeof getProcessWithdrawalQueueInstructionAsync> &
    SelfPlanAndSendFunctions;
  recordPremiumIncome: (
    input: RecordPremiumIncomeAsyncInput,
  ) => ReturnType<typeof getRecordPremiumIncomeInstructionAsync> &
    SelfPlanAndSendFunctions;
  redeem: (
    input: RedeemAsyncInput,
  ) => ReturnType<typeof getRedeemInstructionAsync> & SelfPlanAndSendFunctions;
  requestWithdrawal: (
    input: RequestWithdrawalAsyncInput,
  ) => ReturnType<typeof getRequestWithdrawalInstructionAsync> &
    SelfPlanAndSendFunctions;
  sendPayout: (
    input: SendPayoutAsyncInput,
  ) => ReturnType<typeof getSendPayoutInstructionAsync> &
    SelfPlanAndSendFunctions;
  setController: (
    input: SetControllerAsyncInput,
  ) => ReturnType<typeof getSetControllerInstructionAsync> &
    SelfPlanAndSendFunctions;
  snapshot: (
    input: SnapshotAsyncInput,
  ) => ReturnType<typeof getSnapshotInstructionAsync> &
    SelfPlanAndSendFunctions;
};

export type VaultPluginPdas = {
  vaultState: typeof findVaultStatePda;
  withdrawalQueue: typeof findWithdrawalQueuePda;
  shareMint: typeof findShareMintPda;
  claimable: typeof findClaimablePda;
  requestWithdrawalClaimable: typeof findRequestWithdrawalClaimablePda;
  snapshotRecord: typeof findSnapshotRecordPda;
};

export type VaultPluginRequirements = ClientWithRpc<
  GetAccountInfoApi & GetMultipleAccountsApi
> &
  ClientWithTransactionPlanning &
  ClientWithTransactionSending;

export function vaultProgram() {
  return <T extends VaultPluginRequirements>(
    client: T,
  ): Omit<T, "vault"> & { vault: VaultPlugin } => {
    return extendClient(client, {
      vault: <VaultPlugin>{
        accounts: {
          claimableBalance: addSelfFetchFunctions(
            client,
            getClaimableBalanceCodec(),
          ),
          snapshotRecord: addSelfFetchFunctions(
            client,
            getSnapshotRecordCodec(),
          ),
          vaultState: addSelfFetchFunctions(client, getVaultStateCodec()),
          withdrawalQueue: addSelfFetchFunctions(
            client,
            getWithdrawalQueueCodec(),
          ),
        },
        instructions: {
          cancelWithdrawal: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCancelWithdrawalInstructionAsync(input),
            ),
          collect: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getCollectInstructionAsync(input),
            ),
          decreaseLocked: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getDecreaseLockedInstructionAsync(input),
            ),
          deposit: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getDepositInstructionAsync(input),
            ),
          increaseLocked: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getIncreaseLockedInstructionAsync(input),
            ),
          initialize: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getInitializeInstructionAsync(input),
            ),
          processWithdrawalQueue: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getProcessWithdrawalQueueInstructionAsync(input),
            ),
          recordPremiumIncome: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getRecordPremiumIncomeInstructionAsync(input),
            ),
          redeem: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getRedeemInstructionAsync(input),
            ),
          requestWithdrawal: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getRequestWithdrawalInstructionAsync(input),
            ),
          sendPayout: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getSendPayoutInstructionAsync(input),
            ),
          setController: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getSetControllerInstructionAsync(input),
            ),
          snapshot: (input) =>
            addSelfPlanAndSendFunctions(
              client,
              getSnapshotInstructionAsync(input),
            ),
        },
        pdas: {
          vaultState: findVaultStatePda,
          withdrawalQueue: findWithdrawalQueuePda,
          shareMint: findShareMintPda,
          claimable: findClaimablePda,
          requestWithdrawalClaimable: findRequestWithdrawalClaimablePda,
          snapshotRecord: findSnapshotRecordPda,
        },
      },
    });
  };
}
