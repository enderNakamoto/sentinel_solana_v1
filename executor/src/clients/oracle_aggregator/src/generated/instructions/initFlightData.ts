/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda, findFlightDataPda } from "../pdas";
import { ORACLE_AGGREGATOR_PROGRAM_ADDRESS } from "../programs";

export const INIT_FLIGHT_DATA_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([23, 186, 234, 126, 51, 4, 207, 229]);

export function getInitFlightDataDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INIT_FLIGHT_DATA_DISCRIMINATOR,
  );
}

export type InitFlightDataInstruction<
  TProgram extends string = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountFlightData extends string | AccountMeta<string> = string,
  TAccountAuthorizedConsumer extends string | AccountMeta<string> = string,
  TAccountRentPayer extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountFlightData extends string
        ? WritableAccount<TAccountFlightData>
        : TAccountFlightData,
      TAccountAuthorizedConsumer extends string
        ? ReadonlySignerAccount<TAccountAuthorizedConsumer> &
            AccountSignerMeta<TAccountAuthorizedConsumer>
        : TAccountAuthorizedConsumer,
      TAccountRentPayer extends string
        ? WritableSignerAccount<TAccountRentPayer> &
            AccountSignerMeta<TAccountRentPayer>
        : TAccountRentPayer,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitFlightDataInstructionData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
};

export type InitFlightDataInstructionDataArgs = {
  flightId: string;
  date: number | bigint;
};

export function getInitFlightDataInstructionDataEncoder(): Encoder<InitFlightDataInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INIT_FLIGHT_DATA_DISCRIMINATOR }),
  );
}

export function getInitFlightDataInstructionDataDecoder(): Decoder<InitFlightDataInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
  ]);
}

export function getInitFlightDataInstructionDataCodec(): Codec<
  InitFlightDataInstructionDataArgs,
  InitFlightDataInstructionData
> {
  return combineCodec(
    getInitFlightDataInstructionDataEncoder(),
    getInitFlightDataInstructionDataDecoder(),
  );
}

export type InitFlightDataAsyncInput<
  TAccountConfig extends string = string,
  TAccountFlightData extends string = string,
  TAccountAuthorizedConsumer extends string = string,
  TAccountRentPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config?: Address<TAccountConfig>;
  flightData?: Address<TAccountFlightData>;
  /**
   * Authority. In production this is the controller_program's
   * `ControllerConfig` PDA (signed via `invoke_signed`). In Phase 4
   * unit tests it's a regular keypair set by `set_authorized_consumer`.
   */
  authorizedConsumer: TransactionSigner<TAccountAuthorizedConsumer>;
  /**
   * Rent-payer for the new `FlightData` PDA. Must be a system-owned
   * signer (a PDA cannot be a system_program::create_account payer).
   * In production this is the traveler making the first-buy via the
   * controller's CPI chain. See Phase 5 D18.
   */
  rentPayer: TransactionSigner<TAccountRentPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: InitFlightDataInstructionDataArgs["flightId"];
  date: InitFlightDataInstructionDataArgs["date"];
};

export async function getInitFlightDataInstructionAsync<
  TAccountConfig extends string,
  TAccountFlightData extends string,
  TAccountAuthorizedConsumer extends string,
  TAccountRentPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
>(
  input: InitFlightDataAsyncInput<
    TAccountConfig,
    TAccountFlightData,
    TAccountAuthorizedConsumer,
    TAccountRentPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitFlightDataInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountFlightData,
    TAccountAuthorizedConsumer,
    TAccountRentPayer,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORACLE_AGGREGATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    flightData: { value: input.flightData ?? null, isWritable: true },
    authorizedConsumer: {
      value: input.authorizedConsumer ?? null,
      isWritable: false,
    },
    rentPayer: { value: input.rentPayer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.flightData.value) {
    accounts.flightData.value = await findFlightDataPda({
      flightId: getNonNullResolvedInstructionInput("flightId", args.flightId),
      date: getNonNullResolvedInstructionInput("date", args.date),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("flightData", accounts.flightData),
      getAccountMeta("authorizedConsumer", accounts.authorizedConsumer),
      getAccountMeta("rentPayer", accounts.rentPayer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitFlightDataInstructionDataEncoder().encode(
      args as InitFlightDataInstructionDataArgs,
    ),
    programAddress,
  } as InitFlightDataInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountFlightData,
    TAccountAuthorizedConsumer,
    TAccountRentPayer,
    TAccountSystemProgram
  >);
}

export type InitFlightDataInput<
  TAccountConfig extends string = string,
  TAccountFlightData extends string = string,
  TAccountAuthorizedConsumer extends string = string,
  TAccountRentPayer extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  config: Address<TAccountConfig>;
  flightData: Address<TAccountFlightData>;
  /**
   * Authority. In production this is the controller_program's
   * `ControllerConfig` PDA (signed via `invoke_signed`). In Phase 4
   * unit tests it's a regular keypair set by `set_authorized_consumer`.
   */
  authorizedConsumer: TransactionSigner<TAccountAuthorizedConsumer>;
  /**
   * Rent-payer for the new `FlightData` PDA. Must be a system-owned
   * signer (a PDA cannot be a system_program::create_account payer).
   * In production this is the traveler making the first-buy via the
   * controller's CPI chain. See Phase 5 D18.
   */
  rentPayer: TransactionSigner<TAccountRentPayer>;
  systemProgram?: Address<TAccountSystemProgram>;
  flightId: InitFlightDataInstructionDataArgs["flightId"];
  date: InitFlightDataInstructionDataArgs["date"];
};

export function getInitFlightDataInstruction<
  TAccountConfig extends string,
  TAccountFlightData extends string,
  TAccountAuthorizedConsumer extends string,
  TAccountRentPayer extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
>(
  input: InitFlightDataInput<
    TAccountConfig,
    TAccountFlightData,
    TAccountAuthorizedConsumer,
    TAccountRentPayer,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitFlightDataInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountFlightData,
  TAccountAuthorizedConsumer,
  TAccountRentPayer,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORACLE_AGGREGATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    flightData: { value: input.flightData ?? null, isWritable: true },
    authorizedConsumer: {
      value: input.authorizedConsumer ?? null,
      isWritable: false,
    },
    rentPayer: { value: input.rentPayer ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("flightData", accounts.flightData),
      getAccountMeta("authorizedConsumer", accounts.authorizedConsumer),
      getAccountMeta("rentPayer", accounts.rentPayer),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitFlightDataInstructionDataEncoder().encode(
      args as InitFlightDataInstructionDataArgs,
    ),
    programAddress,
  } as InitFlightDataInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountFlightData,
    TAccountAuthorizedConsumer,
    TAccountRentPayer,
    TAccountSystemProgram
  >);
}

export type ParsedInitFlightDataInstruction<
  TProgram extends string = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    flightData: TAccountMetas[1];
    /**
     * Authority. In production this is the controller_program's
     * `ControllerConfig` PDA (signed via `invoke_signed`). In Phase 4
     * unit tests it's a regular keypair set by `set_authorized_consumer`.
     */
    authorizedConsumer: TAccountMetas[2];
    /**
     * Rent-payer for the new `FlightData` PDA. Must be a system-owned
     * signer (a PDA cannot be a system_program::create_account payer).
     * In production this is the traveler making the first-buy via the
     * controller's CPI chain. See Phase 5 D18.
     */
    rentPayer: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: InitFlightDataInstructionData;
};

export function parseInitFlightDataInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitFlightDataInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      flightData: getNextAccount(),
      authorizedConsumer: getNextAccount(),
      rentPayer: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitFlightDataInstructionDataDecoder().decode(instruction.data),
  };
}
