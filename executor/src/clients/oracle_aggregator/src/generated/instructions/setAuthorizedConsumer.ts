/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { ORACLE_AGGREGATOR_PROGRAM_ADDRESS } from "../programs";

export const SET_AUTHORIZED_CONSUMER_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([200, 90, 171, 248, 67, 225, 59, 75]);

export function getSetAuthorizedConsumerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_AUTHORIZED_CONSUMER_DISCRIMINATOR,
  );
}

export type SetAuthorizedConsumerInstruction<
  TProgram extends string = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountOwner extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? WritableAccount<TAccountConfig>
        : TAccountConfig,
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      ...TRemainingAccounts,
    ]
  >;

export type SetAuthorizedConsumerInstructionData = {
  discriminator: ReadonlyUint8Array;
  consumer: Address;
};

export type SetAuthorizedConsumerInstructionDataArgs = { consumer: Address };

export function getSetAuthorizedConsumerInstructionDataEncoder(): FixedSizeEncoder<SetAuthorizedConsumerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["consumer", getAddressEncoder()],
    ]),
    (value) => ({
      ...value,
      discriminator: SET_AUTHORIZED_CONSUMER_DISCRIMINATOR,
    }),
  );
}

export function getSetAuthorizedConsumerInstructionDataDecoder(): FixedSizeDecoder<SetAuthorizedConsumerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["consumer", getAddressDecoder()],
  ]);
}

export function getSetAuthorizedConsumerInstructionDataCodec(): FixedSizeCodec<
  SetAuthorizedConsumerInstructionDataArgs,
  SetAuthorizedConsumerInstructionData
> {
  return combineCodec(
    getSetAuthorizedConsumerInstructionDataEncoder(),
    getSetAuthorizedConsumerInstructionDataDecoder(),
  );
}

export type SetAuthorizedConsumerAsyncInput<
  TAccountConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  config?: Address<TAccountConfig>;
  owner: TransactionSigner<TAccountOwner>;
  consumer: SetAuthorizedConsumerInstructionDataArgs["consumer"];
};

export async function getSetAuthorizedConsumerInstructionAsync<
  TAccountConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
>(
  input: SetAuthorizedConsumerAsyncInput<TAccountConfig, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetAuthorizedConsumerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOwner
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORACLE_AGGREGATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetAuthorizedConsumerInstructionDataEncoder().encode(
      args as SetAuthorizedConsumerInstructionDataArgs,
    ),
    programAddress,
  } as SetAuthorizedConsumerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOwner
  >);
}

export type SetAuthorizedConsumerInput<
  TAccountConfig extends string = string,
  TAccountOwner extends string = string,
> = {
  config: Address<TAccountConfig>;
  owner: TransactionSigner<TAccountOwner>;
  consumer: SetAuthorizedConsumerInstructionDataArgs["consumer"];
};

export function getSetAuthorizedConsumerInstruction<
  TAccountConfig extends string,
  TAccountOwner extends string,
  TProgramAddress extends Address = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
>(
  input: SetAuthorizedConsumerInput<TAccountConfig, TAccountOwner>,
  config?: { programAddress?: TProgramAddress },
): SetAuthorizedConsumerInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountOwner
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? ORACLE_AGGREGATOR_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: true },
    owner: { value: input.owner ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("owner", accounts.owner),
    ],
    data: getSetAuthorizedConsumerInstructionDataEncoder().encode(
      args as SetAuthorizedConsumerInstructionDataArgs,
    ),
    programAddress,
  } as SetAuthorizedConsumerInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountOwner
  >);
}

export type ParsedSetAuthorizedConsumerInstruction<
  TProgram extends string = typeof ORACLE_AGGREGATOR_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    config: TAccountMetas[0];
    owner: TAccountMetas[1];
  };
  data: SetAuthorizedConsumerInstructionData;
};

export function parseSetAuthorizedConsumerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetAuthorizedConsumerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { config: getNextAccount(), owner: getNextAccount() },
    data: getSetAuthorizedConsumerInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
