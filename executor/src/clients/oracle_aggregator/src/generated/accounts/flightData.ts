/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getFlightStatusDecoder,
  getFlightStatusEncoder,
  type FlightStatus,
  type FlightStatusArgs,
} from "../types";

export const FLIGHT_DATA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  185, 125, 170, 6, 17, 60, 173, 151,
]);

export function getFlightDataDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(FLIGHT_DATA_DISCRIMINATOR);
}

export type FlightData = {
  discriminator: ReadonlyUint8Array;
  flightId: string;
  date: bigint;
  status: FlightStatus;
  /**
   * `0` is the unset sentinel for both arrival times. Real-world ETAs
   * are far-future timestamps, so `0` is unambiguous in practice.
   */
  estimatedArrivalTime: bigint;
  actualArrivalTime: bigint;
  bump: number;
};

export type FlightDataArgs = {
  flightId: string;
  date: number | bigint;
  status: FlightStatusArgs;
  /**
   * `0` is the unset sentinel for both arrival times. Real-world ETAs
   * are far-future timestamps, so `0` is unambiguous in practice.
   */
  estimatedArrivalTime: number | bigint;
  actualArrivalTime: number | bigint;
  bump: number;
};

/** Gets the encoder for {@link FlightDataArgs} account data. */
export function getFlightDataEncoder(): Encoder<FlightDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["flightId", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["date", getU64Encoder()],
      ["status", getFlightStatusEncoder()],
      ["estimatedArrivalTime", getI64Encoder()],
      ["actualArrivalTime", getI64Encoder()],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FLIGHT_DATA_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link FlightData} account data. */
export function getFlightDataDecoder(): Decoder<FlightData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["flightId", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["date", getU64Decoder()],
    ["status", getFlightStatusDecoder()],
    ["estimatedArrivalTime", getI64Decoder()],
    ["actualArrivalTime", getI64Decoder()],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link FlightData} account data. */
export function getFlightDataCodec(): Codec<FlightDataArgs, FlightData> {
  return combineCodec(getFlightDataEncoder(), getFlightDataDecoder());
}

export function decodeFlightData<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<FlightData, TAddress>;
export function decodeFlightData<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<FlightData, TAddress>;
export function decodeFlightData<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<FlightData, TAddress> | MaybeAccount<FlightData, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getFlightDataDecoder(),
  );
}

export async function fetchFlightData<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<FlightData, TAddress>> {
  const maybeAccount = await fetchMaybeFlightData(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeFlightData<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<FlightData, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeFlightData(maybeAccount);
}

export async function fetchAllFlightData(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<FlightData>[]> {
  const maybeAccounts = await fetchAllMaybeFlightData(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeFlightData(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<FlightData>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeFlightData(maybeAccount));
}
