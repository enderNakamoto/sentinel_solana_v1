{
  "name": "@sentinel/executor",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bash ../scripts/run.sh run-cron",
    "start": "bash ../scripts/run.sh run-cron",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist node_modules src/idl src/clients src/scripts/dist"
  },
  "dependencies": {
    "@solana-program/system": "^0.12.0",
    "@solana-program/token": "^0.13.0",
    "@solana/kit": "^6.8.0",
    "@solana/kit-plugin-rpc": "^0.11.0",
    "@solana/kit-plugin-signer": "^0.10.0",
    "@solana/program-client-core": "^6.8.0",
    "@solana/spl-token": "^0.4.13",
    "@solana/web3.js": "^1.95.0",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "node-cron": "^3.0.3",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
